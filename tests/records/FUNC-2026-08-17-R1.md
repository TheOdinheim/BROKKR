# Functionality Test Record — FUNC-2026-08-17-R1

**Phase:** 1 (revision) — `brokkr-core` Rev 1.14 `Context` custody fields
**Date:** 17 August 2026
**Commands:** `cargo test --offline --locked -p brokkr-core`; standalone `rustc` on the reasoner doctest
snippets; `cargo build/test --offline --locked --workspace`
**Nature:** types-only field addition — no new runtime tests (the fields carry no logic to exercise;
clearance/egress is Phase 8.5). Verification is: brokkr-core still builds and all its tests pass, the
doctest count is unchanged, both reasoner doctests emit the code the compiler actually emits, nothing
outside core constructs a `Context`, and the workspace stays green.

## Result: brokkr-core 15 + 3 + 6 + 12 doctests pass; workspace 141 passed, 0 failed

```
=== brokkr-core ===
     tests/negative_invariants.rs   : 15 passed   (the two Context constructions given the 4 new fields)
     tests/resolution_encoding.rs   :  3 passed
     tests/risk_shape.rs            :  6 passed
   Doc-tests brokkr_core            : 12 passed        (baseline 12 → 12, unchanged)
     - reasoner::ClearedContext (line 60) - compile fail ... ok   (I-12, now E0451)
     - reasoner::ClearedContext (line 77) - compile fail ... ok   (I-12, E0308)

=== workspace ===
cargo build --workspace          → all libs build
cargo build --workspace --tests  → no errors (nothing outside brokkr-core constructs a Context)
cargo test  --workspace          → 141 passed; 0 failed
```

## Doctest error codes — verified by standalone compile (not assumed)

`compile_fail` passes on **any** compile error and does not enforce the annotated code, so each snippet was
compiled standalone against the built `brokkr-core` rlib:

| Snippet | Emitted code | Meaning |
|---|---|---|
| `ClearedContext { inner: Context { …5 fields… } }` | **`error[E0451]`** | field `inner` is private **in a struct-literal construction** — the annotation was corrected `E0616` → `E0451`; **not** `E0063`, so the missing-fields trap is avoided |
| `r.propose(&raw, scope)` where `raw: Context` | **`error[E0308]`** | expected `&ClearedContext`, found `&Context` — unchanged |

The pre-Rev-1.14 `E0616` annotation was wrong (E0616 is private-field *access*, not *construction*) and was
never enforced by rustdoc; the mandated verification caught it and it now states the true code. Both
doctests still prove I-12 (an ungoverned `Context` cannot reach a model).

## What this establishes

| Check | Result |
|---|---|
| `brokkr-core` builds `--locked` | clean |
| existing brokkr-core tests unchanged | 15 + 3 + 6 pass (the I-12 test now builds a full `Context`) |
| **doctest count before → after** | **12 → 12** (both reasoner doctests edited in place; none added) |
| **reasoner doctest codes** | E0451 + E0308, verified by standalone compile |
| **nothing outside core constructs a `Context`** | `cargo build --workspace --tests` clean; no crate broke (BIFRÖST is Phase 8.5) |
| workspace test run | **141 passed, 0 failed** (unchanged) |
| clippy `--all-targets` (§6 deny-list) | 0 |
| `cargo fmt -- --check` | clean |
| `#![no_std]` + `#![forbid(unsafe_code)]` | preserved |
| no new dependency (I-5) | `cargo tree -e no-dev -p brokkr-core` = `brokkr-core` alone; `reasoner`→`barrier` only, no cycle |

## What was NOT tested here, and why (§7)

- **Building an egress flow from a `Context`, calling the barrier, issuing/verifying the BCR** — that
  enforcement is Phase 8.5 (`brokkr-bifrost`); this revision places the type surface only.
- **A classification derived from the payload** — there is nothing to test: no code inspects `payload` to
  derive `classification` (I-12 confirmed structurally, not by a test).

— End of functionality test record FUNC-2026-08-17-R1.
