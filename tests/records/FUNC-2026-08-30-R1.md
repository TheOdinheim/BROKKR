# Verification Record — /security-review Audit Disposition

**Record ID:** FUNC-2026-08-30-R1
**Date:** 2026-08-30
**Type:** Verification of an external read-only audit's findings against source (no code changed, no new tests). Per CLAUDE.md §7, each disposition points at a line the reader can open — the reviewer's summary is not evidence.
**Author:** Claude Code (reviewer)
**Full analysis:** `reports/SECURITY-REVIEW-2026-08-30-R1.md`; trust boundary in `reports/SECURITY-BOUNDARIES-2026-08-30-R1.md`.

## What was checked, and against what

The Claude Code `/security-review` audit of the whole workspace produced 5 findings. Each was verified against the actual source before disposition.

| Finding | Claim | Verified against source | Disposition |
|---|---|---|---|
| M-1 | FFI `_New` return not null-checked | `ffi.rs:233,386,597,691`; `tls.rs:185,189,217,256` — null check present at every site | **false positive** |
| M-2 | signature buffer undersized → overflow | `ffi.rs:410,519` — `vec![0u8; self.sig_size()?]` (runtime-sized, then truncated) | **false positive** |
| M-3 | hash chain excludes the signature → key-holder can modify + re-sign undetected | `saga.rs:216,233` — chain links over `record_signed_content` (excludes `signatures`) | **real — architectural trust boundary** |
| M-4 | `read_until_close` unbounded → OOM | `tls.rs:140,303-305` — `MAX_RESPONSE_BYTES` enforced in the read loop (F-19) | **false positive** |
| I-1 | FFI struct layout mismatch (no `repr(C)`) | `ffi.rs` 6× `repr(C…)`, `tls.rs` 3× — all opaque/buffer structs `#[repr(C)]` | **false positive** |

## Result

**4 false positives** (each refuted by an existing control at the cited line) and **1 real finding, M-3**, disposed as a **known trust boundary** — SAGA's integrity against a *key-holding* insider rests on signing-key custody (OQGF-R-6) plus an external witness (OQGF-R-5), not on the hash chain, which by design covers ordering/completeness while the dual-family signature covers authorship. M-3 is documented alongside F-30 (SAGA end-truncation), with which it shares its mitigation.

## Workspace state

No source modified; markdown-only change. Last verified build state stands from Phase 15-FIX: **368 passed, 0 failed**; clippy `--workspace --all-targets` and fmt clean. This record documents a read-only audit disposition, not a new test run.

## Environment

Read-only review; real wolfSSL v5.9.2 (non-FIPS) codebase as built. Evidence is source line references, independently openable.
