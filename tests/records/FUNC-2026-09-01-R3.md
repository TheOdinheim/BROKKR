# Test Record — Phase 16-FIX R2 (DAP-directed F-33 + F-34 closure) — FUNC-2026-09-01-R3

**Record ID:** FUNC-2026-09-01-R3
**Date:** 2026-09-01
**Phase:** 16-FIX R2 (construction, DAP-directed)
**Author:** Claude Code (builder)

---

## Result

- **Workspace GREEN — 408 passed, 0 failed** (406 + 2 net new regressions).
- `cargo clippy -p brokkr-core -p brokkr-barrier -p brokkr-sentinel -p brokkr-audit -p brokkr-cli --all-targets` + `cargo fmt --all -- --check` — clean (one documented `#[allow(clippy::large_enum_variant)]` on `AuditEvent`).
- No new dependency; no cert/key material staged.

## Fixes and regressions

| Finding | Code path | Regression (fails without the fix) |
|---|---|---|
| F-33 | `brokkr-core` `permissive()` hard `assert!(validate().is_ok())` | `redteam_16::fix_permissive_validates` |
| F-34 | `brokkr-core` `Destination::Network { .., port, protocol, .. }`; `brokkr-cli` `check_egress` matches host+port+protocol; audit + sentinel canonical encoders encode port/protocol | `redteam_16::fix_egress_all_three_match_allowed`, `fix_egress_port_mismatch_denied`, `fix_egress_protocol_mismatch_denied` |

## Ripple (core-type change, all pass)

- `brokkr-barrier/tests/barrier.rs`, `brokkr-cli/tests/{capability,redteam_16}.rs` — `Destination::Network` construction updated with port/protocol (`network_dest_full` helper added).
- `brokkr-audit` + `brokkr-sentinel` chain/canonical tests — pass with the enlarged, still-injective `Destination` encoding.
- `f34_egress_ignores_port_and_protocol` converted in place to the fixed behaviour (renamed, not deleted).

## Deviations from the DAP's literal spec (flagged, relax nothing)

- `channel: ChannelStrength` **retained** on `Destination::Network` (HÚÐ condition 8 reads it — `huth.rs:145`).
- `host: Host` **retained** (not `String`; `DestinationClass` comparison at `huth.rs:282`).
- `assert!` used instead of `.expect()` in `permissive()` (§6 denies `expect_used`).

## Conformance

`conformance/CONF-2026-09-01-P16FIX-R2.md` — OQGF-P-12.4 and OQGF-P-12.1/2 move PARTIAL → satisfied (forward). F-32 remains the narrower residual.

## Artifacts

- Report: `reports/PHASE-16FIX-2026-09-01-R2.md`
- Conformance: `conformance/CONF-2026-09-01-P16FIX-R2.md`
