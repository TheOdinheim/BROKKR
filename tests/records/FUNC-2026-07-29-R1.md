# Functionality Test Record — FUNC-2026-07-29-R1

**Phase:** 1 revision R3 (`brokkr-core` — the barrier surface, Rev 1.6)
**Date:** 29 July 2026
**Commands:** `cargo test --locked -p brokkr-core` and `cargo test --locked --workspace`
**Backend:** brokkr-core is `#![no_std] + alloc`, zero dependencies. Downstream crates test against
real wolfSSL v5.9.2 (non-FIPS), unchanged.

## Result: all pass; downstream unchanged; doctest count unchanged (12 → 12)

### brokkr-core

```
     Running unittests src/lib.rs         — 0 passed
     Running tests/negative_invariants.rs — 12 passed, 0 failed
     Running tests/risk_shape.rs          — 6 passed, 0 failed
   Doc-tests brokkr_core                  — 12 passed, 0 failed
```

### Workspace (downstream STILL PASSES, UNCHANGED)

`cargo test --locked --workspace` — every crate green: `brokkr-crypto` (6), `brokkr-intent` (8 + 6),
`brokkr-gate` (10), `brokkr-genome` (13 + 1 doctest). No downstream test modified; none broke.

## Doctest count — before and after

| | Before (committed HEAD) | After |
|---|---|---|
| `brokkr-core` running doctests | **12** | **12** |

Unchanged, measured by stashing the change and running `--doc` against HEAD (12), then again with
the change (12). Expected: this revision adds/reshapes types and adds no doctest.

## The BarrierVerdict compile-fail doctest (still passes, same reason)

`BarrierVerdict` is **not changed** by this revision — only `BoundaryFlow` (the gate's input) is
reshaped. Its `compile_fail,E0599` doctest (`Deny.into_allow()` — no such method) still passes, and
for the same reason (I-2: no `Deny → Allow`). Verified by running.

## Why nothing downstream broke — investigated per instruction

A workspace grep showed **no `BoundaryFlow` construction anywhere** outside `barrier.rs`/`lib.rs`:
`reasoner.rs` uses `BarrierVerdict` and `Destination` (both unchanged), not `BoundaryFlow`; and
`brokkr-core/tests/negative_invariants.rs` constructs `BarrierVerdict` and `Destination` but **not**
`BoundaryFlow`. So the reshape of `BoundaryFlow` from struct to enum has no construction site to
break — confirmed by building (`negative_invariants.rs` compiles and its 12 tests pass unchanged).
No brokkr-core test needed editing.

## Discipline checks

- Clippy (§6 deny-list), `--all-targets`: **0** warnings/errors.
- `cargo build --locked -p brokkr-core`: clean. `cargo fmt`: clean.
- **No external dependency** — `[dependencies]` empty; `cargo tree` shows no internal/external dep
  (I-5).
- `#![no_std]` preserved: the new types use `crate::*` types and `alloc::vec::Vec` only.

## What was NOT done

- **No evaluation logic, no gate, no resolver** — types only (Phase 6 implements `Barrier`).
- **`BarrierVerdict` unchanged**; **`Destination` unchanged** (reasoner.rs and the negative tests
  use it).
- **`OriginId` not added to the ids re-export** — matching the convention that `string_id!` types
  (`DatumRef`, `Host`, `ModelEndpointId`, `RiskAcceptanceId`, …) are accessed via `brokkr_core::ids::`
  and not re-exported at crate root.

— End of functionality test record FUNC-2026-07-29-R1.
