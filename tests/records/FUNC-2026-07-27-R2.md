# Functionality Test Record — FUNC-2026-07-27-R2

**Phase:** 1 revision R2 (`brokkr-core` — the capability vocabulary field, Rev 1.5)
**Date:** 27 July 2026
**Commands:** `cargo test --locked -p brokkr-core` and `cargo test --locked --workspace`
**Backend:** brokkr-core is `#![no_std] + alloc`, zero dependencies. Downstream crates test against
real wolfSSL v5.9.2 (non-FIPS), unchanged.

## Result: all pass; downstream unchanged; doctest count unchanged (12 → 12)

### brokkr-core

```
     Running unittests src/lib.rs        — 0 passed
     Running tests/negative_invariants.rs — 12 passed, 0 failed
     Running tests/risk_shape.rs          — 6 passed, 0 failed
   Doc-tests brokkr_core                  — 12 passed, 0 failed
```

### Workspace (downstream STILL PASSES, UNCHANGED)

`cargo test --locked --workspace` — every crate green: `brokkr-crypto` (6), `brokkr-intent`
(8 + 6 + …), `brokkr-gate` (10). No downstream test modified; none broke. Nothing constructs a
`PolicyRegister` anywhere (Phase 5 unbuilt), confirmed by grep — so no downstream break was possible.

## Doctest count — before and after

| | Before (committed HEAD) | After |
|---|---|---|
| `brokkr-core` running doctests | **12** | **12** |

Unchanged, as expected: the revision adds a struct field, not a doctest. Measured by stashing the
change and running `--doc` against HEAD (12), then again with the change (12).

## The three genome compile-fail doctests (still pass, arity unaffected)

`Genome::new`'s arity is **unchanged at 10** — `PolicyRegister` is constructed separately and passed
as one argument, so adding a field to it does not touch any `Genome::new` call. Verified by running:
all three `compile_fail,E0308` fences in `genome.rs` still pass (signature-`None` 10th arg,
roots-`None` 6th arg, and `ModelEndpoint::new` client_cert-`None` 3rd arg). The E0308 they prove is
untouched.

## Discipline checks

- Clippy (§6 deny-list), `--all-targets`: **0** warnings/errors.
- `cargo build --locked -p brokkr-core`: clean. `cargo fmt`: clean.
- **No external dependency added** — `[dependencies]` remains empty (I-5).
- **No import added** — `Capability` was already imported (used by `ToolEntry` / `InvariantEntry`).
- `#![no_std]` preserved; the field is `alloc::vec::Vec<Capability>`, both already in scope.

## What was NOT done

- **No promotion gate, no predicate check, no `Genome::new` change.** One field only.
- **No `PolicyRegister::new`** — the struct has no constructor (plain public fields), so none to
  update. Confirmed by grep (`impl PolicyRegister` absent).

— End of functionality test record FUNC-2026-07-27-R2.
