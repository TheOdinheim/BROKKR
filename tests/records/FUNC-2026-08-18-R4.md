# Functionality Test Record — FUNC-2026-08-18-R4

**Phase:** 8 (revision) — the evaluation loop, grant retention, I-13 clock removal, EIR reorder
**Date:** 18 August 2026
**Commands:** `cargo test --locked -p brokkr-sentinel`; `--workspace`; the exhaustive comparison scan.
**Backend:** real wolfSSL v5.9.2 (non-FIPS) through `brokkr-crypto` for all signatures; no mocks.

## Result: sentinel 27 passed (was 21); WHOLE WORKSPACE 159 passed, 0 failed

```
cargo test --locked -p brokkr-sentinel  → 27 passed; 0 failed
cargo test --locked --workspace         → 159 passed; 0 failed   (baseline 152)
```

## New tests (the loop and the reorder, none writable before)

| Test | Proves |
|---|---|
| **`test_oqgf_i_6_registered_detector_fires_on_observation`** | THE TEST THAT COULD NOT HAVE BEEN WRITTEN BEFORE — a registered detector firing on a live observation now has a path (`observe`); it produces a `Detection` with a raise-only Signal. Phase 8 never ran detectors outside screening. |
| `test_oqgf_p_4_live_grant_suppresses_and_records` | a live in-scope grant suppresses the firing; the `Detection` is still returned with `suppressed_by: Some(..)` and no Signal (discarding it would hide the grant). |
| **`test_oqgf_p_4_i13_expired_grant_stops_suppressing`** | THE LOAD-BEARING TEST (fails toward blindness). ONE HEIMDALL, same grant, same firing, `observe` twice: suppressed at `now=1000` (< expiry 5000), **raising** at `now=10000` (> expiry). A held clock is one stored value and cannot produce two outcomes from one instance. |
| `test_oqgf_p_4_out_of_scope_grant_does_not_suppress` | a live grant scoped to a different detector does not silence this one. |
| **`test_oqgf_p_8_5_expired_before_replay_reports_expired`** | THE REORDER PROOF. A decision accepted once, re-presented later expired, now reports `Expired` (not `ReplayedNonce`) — the arm unreachable before Rev 1.16. Fails against the old replay-first order. |
| `test_oqgf_p_8_3_i13_hysteresis_ages` | EIR two-verdict: one instance, one escalation, `may_resolve` NotYet before the hold window, Eligible after — the aging clock actually ages (impossible against a held clock). |

## Changed tests (reviewed every one; reported)

- **Every `heimdall_with`/`Eir::new` call** dropped its `now` argument (the field is gone); the time
  moved to the per-call methods (`observe`, `resolve`, `may_resolve`, `scan_chronic`).
- **`test_oqgf_p_4_expired_grant_refused` → `..._expired_grant_is_retained_not_refused_at_issuance`:**
  the expectation **inverted** — grant_heuristic no longer checks expiry (it retains); an expired
  grant is now accepted (Ok) and simply suppresses nothing in the loop. The test asserts both.
- **`test_oqgf_m_12_...` and `test_forged_signal_cannot_stand_down`:** now call `observe` (the public
  `reconcile` is gone), extracting the raise-only Signal from the returned `Detection`.
- **`test_oqgf_p_8_5_expired_decision_is_refused → ..._reports_expired`;
  `..._replayed_nonce_is_refused → ..._still_refused`** (renamed to the prompt's names; `now` moved
  to the `resolve` call).

## Invariant / discipline checks

- **I-13:** `brokkr-sentinel` holds no clock. `self.now`/`st.now`/`Mutex<Timestamp>`/`fn now(` empty;
  no `now` field on either state struct. **Exhaustive comparison table CLEAN** — all 13
  expiry/elapsed comparisons across the workspace read a per-call `now` (barrier ×3, genome, sentinel
  heimdall ×1, sentinel eir ×5 incl. the `classify`-fed rows a grep misses). This is RISK-2026-0006's
  corrected closure evidence: a table tracing every comparison's source, not a pattern grep.
- **I-9:** no permitting path — `DetectionVerdict` = `Clear|Fired`, `PostureEffect` = `Raise` only.
- `clippy --all-targets` clean; `fmt` clean. **I-5/I-6:** deps `{core, crypto}`, no model call, no
  `unsafe`. No new dependency.

## What was NOT tested, and why (§7)

- **The M-12 per-supplier reconciliation statistic** — Phase 11 (no executed actions until then);
  `Hop.executed` is `None` before the executor. The mechanism is tested; the statistic is not.
- **A production Self Set** — Phase 8 ships the machinery; a real baseline comes from deployment.

— End of functionality test record FUNC-2026-08-18-R4.
