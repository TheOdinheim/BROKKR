# Test Record — Phase 14A (White-box Red Team, High Noise)

**Record ID:** FUNC-2026-08-23-R2
**Date:** 2026-08-23
**Crates:** `brokkr-core` (gate/reasoner doctests), `brokkr-tools` (doctest), `brokkr-cli` (`tests/redteam_14a.rs`)
**Author:** Claude Code (builder / red team)
**Full analysis:** `reports/REDTEAM-14A-2026-08-23-R1.md`

## Commands and results

| Command | Result |
|---|---|
| `cargo test --locked --workspace` | **301 passed, 0 failed** (291 baseline + 6 runtime + 4 new compile_fail doctests) |
| `cargo test -p brokkr-cli --test redteam_14a` | **6 passed** (2.3 always-grant; 3.1/3.1b/3.2/3.3 SAGA; 5.1 witness) |
| `cargo test -p brokkr-core --doc` | passed (incl. new 14A doctests: 1.2 no-Default, 2.1 gate override, 2.2 clear override) |
| `cargo test -p brokkr-tools --doc` | passed (incl. new 14A 1.5 transmute-under-forbid) |
| `cargo clippy --workspace --all-targets` | 0 warnings / 0 errors |
| `cargo fmt --all -- --check` | clean |

## Attacks and how they were proven

**Structural (compile_fail doctests, co-located on the targeted types):**
- 1.1 `AuthorizedAction{action}` → E0451 (existing); 1.2 `::default()` → E0599 (**new**); 1.3 `ClearedContext{inner}` → E0451 (existing); 1.5 transmute under `#![forbid(unsafe_code)]` → E0133 (**new**).
- 2.1 override `authorize` returning `Granted` → E0624 (**new**, `mint` private); 2.2 override `clear` → E0624 (**new**).
- 4.1 call `AuthorizedAction::mint` cross-crate → E0624 (existing).
- I-12: `propose(&raw Context)` → E0308 (existing); F-4: sign through `&` → E0596, `&DualKeyPair` not `Send` → E0277 (existing).

**Runtime (`redteam_14a.rs`, real dual-family PQC):**
- 2.3 `AlwaysGrant` gate → mints a genuine token (documented: type system prevents forging, not misconfiguration).
- 3.1 `records()` returns a clone; mutating it leaves the store `Intact`. 3.1b tampered record via `from_records` → `verify_chain` = `Broken`. 3.2 `append` returns seq 0,1 (auto-assigned; no seq param). 3.3 deleted record → `Broken`.
- 5.1 compile-witness that the composition root links both the untrusted and governance sides.

**Facts captured for the report (not runtime tests):**
- §5.1 `cargo tree`: all 10 governance crates CLEAN of reasoner/tools/cli; reasoner's *direct* deps = core+bifrost (crypto only transitively via bifrost — F-11); only brokkr-cli reverse-deps reasoner/tools.
- §6.1 grep: every real `unsafe` block is in `brokkr-crypto` (ffi.rs 44 sites, tls.rs 12/12 SAFETY); all 12 non-crypto crates `#![forbid(unsafe_code)]`. §6.3 the sole no-SAFETY site is the `unsafe extern "C"` declaration block (no operation; not a finding).

## Findings

0 critical, 0 high. **F-11** (info) — the guarantee is a *private minter*, not a *sealed trait*: `authorize`/`clear` can be overridden but the override cannot mint (E0624); a precision correction to the task's Step-0 wording, not a defect. **F-12** (info) — `forbid(unsafe_code)` is a crate attribute a doctest does not inherit; the transmute doctest was written to carry the attribute itself so it proves the real (crate-level) defense. Both are white-box subtleties, named.

## Structural / hygiene

- New compile_fail doctests verified to fail for the right reason (E0451/E0599/E0624/E0133) — each `cargo test --doc` passes, meaning each block correctly does **not** compile.
- No production logic changed — only doctests added to `gate.rs`/`reasoner.rs`/`tools/lib.rs` and a new integration test file.
- Held-clock grep unaffected; no `unsafe` added anywhere; no new dependency.

## Environment

Rust on PATH; real wolfSSL v5.9.2 (non-FIPS) via `.cargo/config.toml` rpath. Hermetic — no live model, no network.
