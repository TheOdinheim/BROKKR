# Test Record — Phase 4 Revision R2 (SINDRI conjuncts 3 and 4)

**Record ID:** FUNC-2026-08-20-R2
**Date:** 2026-08-20
**Crate:** `brokkr-gate` (SINDRI)
**Author:** Claude Code (builder)

## Commands and results

| Command | Result |
|---|---|
| `cargo build --locked --workspace` | green |
| `cargo test --locked -p brokkr-gate` | **19 passed, 0 failed** (was 10; +9) — real dual-family crypto |
| `cargo test --locked --workspace` | **193 passed, 0 failed** (40 result targets; prior 184 + 9) |
| `cargo clippy -p brokkr-gate --all-targets` | Finished, no warnings |
| `cargo fmt --all -- --check` | clean |

## The 9 new tests (conjuncts 3 and 4)

Each passes Signals 1+2 (a declared `principal`, a real signed root-only chain with a controlled
scope + invariant set) so it reaches the new code, and drives a `MockGenome` (`None` for unknowns):

- **Conjunct 3 (action in scope):** `tool_not_in_genome_yields_out_of_scope` (→ `OutOfScope`),
  `tool_capability_missing_from_scope` (tool needs `write`, scope `{read}` → `OutOfScope`),
  `tool_all_capabilities_in_scope_no_invariants` (→ `Granted`).
- **Conjunct 4 (respects invariants):** `invariant_with_no_predicate_yields_violated` (→
  `InvariantViolated`), `tool_requires_forbidden_capability` (→ `InvariantViolated`),
  `tool_has_forbidden_privilege` (privilege `Privileged`, invariant forbids `[Privileged]` →
  `InvariantViolated`), `tool_passes_all_invariants` (→ `Granted`), `multiple_invariants_all_pass`
  (→ `Granted`), `multiple_invariants_second_fails` (→ `InvariantViolated`).

## Existing tests (case 10 — constructor migration only)

All 10 pre-existing Signal-1/Signal-2 tests were updated for the new `Sindri::new(resolver,
genome_resolver)` signature only — the second argument is a `PermissiveGenome` (resolves every tool
to empty requirements/`Unprivileged` and every invariant to empty forbids), so conjuncts 3/4 do not
change their verdicts. **No existing assertion changed.** The positive grants still grant (the
2-hop chain's `write` tool needs no capability under the permissive genome, and its invariants
forbid nothing); the negatives still fail at Signal 1 or 2, before the new conjuncts.

## Structural / hygiene

- `OutOfScope`/`InvariantViolated` are **constructed in reachable `evaluate` paths** (sindri.rs
  166/170 and 182/186/191) and exercised by the tests above — not merely defined in the enum.
- Code order matches the §6.4 flowchart: signal_1 → signal_2 → conjunct 3 → conjunct 4 → `Ok(())`.
- Held-clock grep across `brokkr-gate/src` — **empty** (I-13 unaffected; `now` still per-call).
- No `brokkr-core` type change, no new crate, no new external dependency; `authorize` untouched.
- `#![forbid(unsafe_code)]`; no `unwrap`/`expect`/`panic` in `src`.

## Environment

- Rust on PATH; real crypto backend (wolfSSL rpath via `.cargo/config.toml`) — the 9 new tests use
  real `sign_root`/`attenuate_signed` and dual-family verify, ~23s for the crate.
