# Functionality Test Record — FUNC-2026-07-27-R1

**Phase:** 1 revision (`brokkr-core` — REGIN genome surface, Rev 1.4)
**Date:** 27 July 2026
**Commands:** `cargo test --locked -p brokkr-core` and `cargo test --locked --workspace`
**Backend:** brokkr-core is `#![no_std] + alloc`, zero dependencies — no crypto involved in its
own tests; downstream crates test against real wolfSSL v5.9.2 (non-FIPS), unchanged.

## Result: all pass; downstream unchanged

### brokkr-core (unit + integration + doc)

```
     Running unittests src/lib.rs
test result: ok. 0 passed; 0 failed
     Running tests/negative_invariants.rs
test result: ok. 12 passed; 0 failed
     Running tests/risk_shape.rs
test result: ok. 6 passed; 0 failed
   Doc-tests brokkr_core
test result: ok. 12 passed; 0 failed
```

### Workspace (downstream crates STILL PASS, UNCHANGED)

`cargo test --locked --workspace` — every crate green: `brokkr-crypto` (6 + …), `brokkr-intent`
(8 + 6 + …), `brokkr-gate` (10). No downstream test was modified; none broke.

## Doctest count — before and after (the load-bearing part)

| | Before (committed HEAD) | After (this revision) |
|---|---|---|
| `genome.rs` `compile_fail` doctest fences | **2** | **3** |
| `brokkr-core` total running doctests | **11** | **12** |

The `lib.rs` "compile_fail" is a **prose mention** (0 fences), which is why the totals are 11/12,
not 12/13. The `cargo test --doc` count (**12 passed**) confirms the new doctest **runs** — a
doctest that silently did not run would leave the count at 11.

## The three genome compile-fail doctests (I-10, verified by execution)

| Doctest | Arity | `None` at | Proves | Error code |
|---|---|---|---|---|
| `ModelEndpoint::new(…None…)` | 7 args | 3rd (`client_cert`) | I-11 — one-sided TLS unrepresentable | `E0308` (unchanged) |
| `Genome::new(…None)` | **10 args** (was 8) | 10th (`signature`) | I-10 — unsigned genome unrepresentable | `E0308` (updated arity so it fails on the **signature type**, not arity `E0061`) |
| `Genome::new(…None…)` **NEW** | 10 args | 6th (`roots`) | **I-10 extended** — a genome missing the new `roots` register is unrepresentable | `E0308` (`expected RootsOfTrust, found Option<_>`) |

All three pass, so rustdoc confirmed the compiler emits **E0308** for each — the codes are verified,
not guessed. Had the Genome arity not been updated, that doctest would have failed for `E0061`
(arity) and proven nothing.

## Discipline checks

- Clippy (§6 deny-list), `--all-targets`: **0** warnings/errors.
- `cargo build --locked -p brokkr-core`: clean. `cargo fmt`: clean.
- **No external dependency added** — `brokkr-core/Cargo.toml` `[dependencies]` remains empty
  (I-5: the spine depends on nothing).
- `#![no_std]` preserved (all new types use `alloc::{vec::Vec, string::String}` and committed
  in-crate types only).

## What was updated, and why (§7 honesty)

- **`brokkr-core/tests/negative_invariants.rs`** — the `trust_score()` helper gained
  `reconciliation_pass_rate: Score(0)` (a placeholder, matching the field's PARTIAL/unmeasured
  status). This is brokkr-core's **own** test (not a downstream crate); the new required field on
  `VendorTrustScore` mandated it. Investigated per instruction: the E0063 break was **only** this
  one helper — no other construction of any extended type exists in the workspace, confirming
  "nothing downstream consumes `Genome`."

## What was NOT done

- **No promotion gate, no resolver, no evaluation logic** — types only (Phase 5).
- **No construction-time invariant check** (ARCH §6.2 line 362) — that is SKULD/Phase 5.
- **`reconciliation_pass_rate` is not populated from measurement** — HEIMDALL, Phase 8.

— End of functionality test record FUNC-2026-07-27-R1.
