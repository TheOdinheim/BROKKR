# Test Record — Phase 13-FIX (Harden Black-Box Findings)

**Record ID:** FUNC-2026-08-23-R1
**Date:** 2026-08-23
**Crates:** `brokkr-crypto`, `brokkr-tools`, `brokkr-reasoner`, `brokkr-audit`, `brokkr-sentinel`, `brokkr-intent`, `brokkr-cli`
**Author:** Claude Code (builder)
**Full analysis:** `reports/PHASE-13FIX-2026-08-23-R1.md`

## Commands and results

| Command | Result |
|---|---|
| `cargo build --workspace` | green (no new external dep) |
| `cargo test --locked --workspace` | **291 passed, 0 failed** (baseline 276 + 11 fix + F-4/F-6 additions) |
| `cargo test -p brokkr-cli --test redteam_fix` | **11 passed** (F-1/F-2/F-3/F-5/F-7/F-8/F-9/F-10) |
| `cargo test -p brokkr-cli --test redteam_13a` | **22 passed** (updated: 2.4 replay→denied, 5.6 oversize→denied) |
| `cargo test -p brokkr-cli --test redteam_13b` | **17 passed** (3.6 already F-6-updated) |
| `cargo test -p brokkr-cli --test redteam_13c` | **18 passed** (updated: 1.4 backward→error, 2.2→rate-limited, 3.3→sanitized) |
| `cargo test -p brokkr-reasoner` | passed (F-6 parser tests: prose→unknown, strict works) |
| `cargo test -p brokkr-crypto --doc` | F-4 compile-fail doctests pass (E0596, E0277) |
| `cargo clippy --workspace --all-targets` | 0 warnings / 0 errors |
| `cargo fmt --all -- --check` | clean |

## The 11 orchestrator/tool fix-regression tests (`redteam_fix.rs`)

Each fails without its fix, passes with it. Built on a **production-guarded** orchestrator (vs the permissive spine-verifying rigs).

- **F-1** `fix_f1_replay_denied` — same chain twice: 1st executes, 2nd `Denied{Gate,"replay"}`, tool count 1.
- **F-2** `fix_f2_path_traversal_denied_by_tool` (`../../../etc/passwd`→Err), `fix_f2_legitimate_path_within_root` (in-root→Ok).
- **F-3** `fix_f3_oversized_detail_denied` (2 MiB→`Denied{Gate,"size limit"}`, tool 0), `fix_f3_normal_detail_allowed`.
- **F-5** `fix_f5_genome_denial_presents_as_gate` — undeclared tool→`Gate` (not Genome); SAGA still records the genome reason.
- **F-7** `fix_f7_rate_limit_enforced` — 5/60 s, 6th `Denied{Gate,"rate limit"}`, tool count 5.
- **F-8** `fix_f8_control_chars_stripped` (NUL/ESC/DEL/ANSI gone, surrounding text kept), `fix_f8_unit_newline_and_tab_preserved`.
- **F-9** `fix_f9_backward_clock_rejected` — now=500 after now=1000 → `Error{"non-monotonic"}`.
- **F-10** `fix_f10_undeclared_capability_reported` — `validate_scope` returns the stray cap; clean scope→empty.

## Red-team assertion updates (behavior now hardened)

| Test | Was | Now |
|---|---|---|
| 13A `attack_2_4` | both replays execute | 2nd denied (F-1) |
| 13A `attack_5_6` | 10 MiB granted | denied, size limit (F-3) |
| 13B `attack_3_6` + ollama parser tests | prose→write_file | prose→`unknown` (F-6) |
| 13C `attack_1_4` | backward clock granted | `Error` non-monotonic (F-9) |
| 13C `attack_2_2` | 50 granted | 6th rate-limited (F-7) |
| 13C `attack_3_3` | NUL/ANSI recorded verbatim | stripped (F-8); 3.1 still passes (newline kept) |

The three red-team `build()` rigs and the Phase-11 `orchestrator.rs` `build()` now append `.with_guards(Guards::permissive())` — they verify the **spine**, so they opt out of the driver guards (which have their own tests). The unconditional F-3/F-8 still apply to them.

## F-4 ripple (compiler-verified)

`sign_dual(&mut self)` + `DualKeyPair: !Sync` forced updates at 71 call sites across 20 files (signing params → `&mut`, keypair bindings → `mut`, SAGA/HEIMDALL signer → `Mutex`). The workspace does not compile until every site is fixed, so there is no silent miss. `cargo fix` cleaned the resulting unused-`mut` warnings.

## Structural / hygiene

- **Held-clock grep** (`brokkr-cli/src`): only `Orchestrator::now()` (the §4 reader). F-9's `last_now` is remembered *previous* time, not a held evaluation clock — I-13 intact.
- **`unsafe`**: F-4 adds none to `brokkr-cli`/`brokkr-tools`; the crypto change is in `sign.rs` (no new `unsafe` — a marker field + `&mut`).
- No new external dependency; `certs/` remains git-ignored.

## Environment

Rust on PATH; real wolfSSL v5.9.2 (non-FIPS) via `.cargo/config.toml` rpath. Hermetic — no live model, no network (the live tests remain `#[ignore]`).
