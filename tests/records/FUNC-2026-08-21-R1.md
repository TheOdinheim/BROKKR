# Test Record — Phase 12 (Real BIFRÖST + Real MÍMIR)

**Record ID:** FUNC-2026-08-21-R1
**Date:** 2026-08-21
**Crates:** `brokkr-crypto`, `brokkr-bifrost`, `brokkr-reasoner`, `brokkr-cli`
**Author:** Claude Code (builder)

## Commands and results

| Command | Result |
|---|---|
| `cargo build --locked --workspace` | green (no new external dep; lockfile unchanged) |
| `cargo test --locked --workspace` | **213 passed, 0 failed** (204 prior + 9 new reasoner unit tests; real crypto; the two live tests are `#[ignore]`) |
| `cargo test -p brokkr-crypto --test tls_smoke -- --ignored --nocapture` | **1 passed** — live mTLS handshake; negotiated `SECP256R1`; real `/api/tags` response |
| `cargo test -p brokkr-cli --test live_model -- --ignored --nocapture` | **1 passed** (26s) — **Executed**, real file written by the governed cycle |
| `cargo clippy --workspace --all-targets` | 0 warnings / 0 errors |
| `cargo fmt --all -- --check` | clean |

## New hermetic unit tests — `brokkr-reasoner::ollama` (9, run in the workspace suite)

Pure functions, no infrastructure:
- `extracts_response_field_from_a_full_http_response`, `missing_field_is_none_and_error_field_is_detected`, `decodes_common_escapes`, `json_escape_roundtrips_through_the_extractor` — the manual JSON extraction (escape-aware, error-field detection, round-trip).
- `parses_tool_and_path_lines`, `parse_is_case_insensitive_and_strips_quotes`, `unparseable_response_yields_unknown_tool_gate_will_deny`, `lenient_parse_recovers_a_shell_style_response`, `strict_form_still_wins_over_lenient` — the action parser, including the **exact bash-script shape** `llama3.2:3b` produced before the lenient fallback, and the `unknown`-tool fail-closed path.

## Live tests (`#[ignore]` — require ollama + nginx mTLS gateway + dev certs)

### `tls_smoke` (brokkr-crypto)
A real wolfSSL TLS 1.3 mutual-auth handshake to `localhost:8443`; asserts a negotiated group name is reported (`SECP256R1`), writes a real `GET /api/tags`, reads the response, asserts it is HTTP and mentions `llama3.2`. **Proves the TLS FFI end to end.**

### `live_model` (brokkr-cli) — the full governed cycle with a real model
One hop, every subsystem real except the model is self-hosted (which is the point): real BIFRÖST mTLS + negotiated-group readback, real MÍMIR ollama client through BIFRÖST, real SINDRI (dual-family PQC chain verification), real SAGA (dual-signed records), real HEIMDALL, real HÚÐ, real filesystem tool.

Observed:
```
negotiated group: raw="SECP256R1" -> NamedGroup=Secp384r1 -> strength=Classical
hop result: Executed { output: "wrote 11 bytes to /tmp/brokkr-phase12-test.txt" }
model rationale (raw response):
TOOL: write_file
PATH: /tmp/brokkr-phase12-test.txt
```
Asserts: MÍMIR's proposal (carrying the model's actual response) is recorded in SAGA; on `Executed`, the file exists with the expected content and HEIMDALL received `Observation::Hop { executed: Some(..) }`. The file is cleaned up on drop. **Both outcomes are covered** — `Executed` (compliant model) and `Denied{Genome}` (non-compliant model, proposal still recorded, spine held), so BIFRÖST + MÍMIR are proven either way.

## Structural / hygiene

- **`unsafe` confinement (CLAUDE.md §6):** the 16 `unsafe`/SAFETY sites are all in `brokkr-crypto/src/tls.rs`. `grep unsafe` across `brokkr-bifrost/src` and `brokkr-reasoner/src` finds only doc text and the `#![forbid(unsafe_code)]` attribute — no `unsafe` blocks. Both crates keep `#![forbid(unsafe_code)]`.
- **Held-clock grep** over `brokkr-crypto/src/tls.rs`, `brokkr-bifrost/src/mtls.rs`, `brokkr-reasoner/src/ollama.rs` — none. The new modules evaluate no time and hold no clock (I-13 intact).
- **Dependency direction (I-5/I-6):** `cargo tree` — `brokkr-reasoner` depends on `brokkr-bifrost` and **not** `brokkr-crypto` (its only network path is through BIFRÖST); `brokkr-bifrost` now depends directly on `brokkr-crypto`. No governance crate depends on reasoner/tools/cli; no cycle.
- **No new external dependency** — manual HTTP/JSON; `Cargo.lock` unchanged.

## Environment

- Rust on PATH; real wolfSSL v5.9.2 (`libwolfssl.so.45`, non-FIPS) via `.cargo/config.toml` rpath.
- Infrastructure for the live tests: ollama `llama3.2:3b` on `:11434`; nginx mTLS gateway on `:8443` (TLS 1.3, `ssl_verify_client on`); dev certs (PEM, EC P-384) at `~/BROKKR/certs/`. The gateway negotiates **classical** TLS (no PQC) — the honest, expected channel strength.
