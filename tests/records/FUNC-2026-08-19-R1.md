# Test Record — brokkr-core Revision R11 (KVASIR type surface)

**Record ID:** FUNC-2026-08-19-R1
**Date:** 2026-08-19
**Crate:** `brokkr-core` (types only, ARCH Rev 1.17 §6.12)
**Author:** Claude Code (builder)

## Commands and results

| Command | Result |
|---|---|
| `cargo build --locked -p brokkr-core` | Finished, clean |
| `cargo test --locked -p brokkr-core` | lib 0 + integration 16 + resolution-encoding 3 + risk_shape 6 + doc 14 — **all pass, 0 failed** |
| `cargo test --locked -p brokkr-core --doc` (HEAD baseline via `git stash`) | 14 pass |
| `cargo test --locked -p brokkr-core --doc` (working) | 14 pass |
| `cargo clippy --locked -p brokkr-core --all-targets` | Finished, no warnings |
| `cargo fmt --all -- --check` | clean |
| `cargo build --locked --workspace` | **1 defect (expected):** `brokkr-sentinel heimdall.rs:406` E0063 missing `produced_at` |

## Doctest count

**14 → 14, unchanged.** `adapt.rs` carries no doctests; this revision added none and altered none.
No `compile_fail` annotation was touched by the `activate` arity change (no doctest calls
`MaturationPipeline::activate`/`select`; grep-confirmed). Therefore **no E0061-masking hazard** this
revision, and no adapt.rs doctest error code to report (there are none to run).

## In-scope test-double changes (brokkr-core `tests/negative_invariants.rs`)

- `MockController::screen` `ScreenPass` literal → `+ produced_at: Timestamp(10)`.
- `provenance()` `ScreenPass` literal → `+ produced_at: Timestamp(10)`.
- `MockPipeline::select` `SelectionPass` literal → `+ produced_at: Timestamp(20)`.
- `MockPipeline::activate` signature → `+ _now: Timestamp`.
- `test_i9_*` two `.activate(..)` callers → `+ Timestamp(30)`.

`test_i9_refined_detector_is_heuristic_and_activation_is_gated` still passes and still proves:
`response_class() == Heuristic`, `activate → FailsTolerance` (fails_tolerance), `activate →
NeedsApproval` (no approval).

## Workspace ripple (out of scope, reported not fixed)

The **only** cross-scope break: `brokkr-sentinel/src/heimdall.rs:406` constructs
`ScreenPass { version }` without the new `produced_at` (E0063). Verified exhaustively:
- Only `ScreenPass` construction outside core (grep across the tree).
- `brokkr-sentinel` does not use `SelectionPass`, `activate`, or `MaturationPipeline` (grep), so no
  other break.
- `brokkr-adapt` does not exist.

`cargo test -p brokkr-sentinel --no-run` fails only because the lib fails — no separate test-only
break. Not fixed (scope lock); requires a companion `brokkr-sentinel` revision (see report §4).

## Environment

- Rust on PATH; real crypto backend (wolfSSL rpath via `.cargo/config.toml`).
- `#![no_std]` + `#![forbid(unsafe_code)]` preserved; no new external dependency.
