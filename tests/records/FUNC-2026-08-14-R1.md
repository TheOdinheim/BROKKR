# Functionality Test Record — FUNC-2026-08-14-R1

**Phase:** 1 (revision) — `brokkr-core` Rev 1.13 `ResolveError` variants
**Date:** 14 August 2026
**Commands:** `cargo test --offline --locked -p brokkr-core`; `cargo build --offline --locked --workspace`
and `--workspace --tests` (to confirm the single pre-existing error)
**Nature:** types-only enum-variant addition — no new runtime tests (the variants carry no logic to
exercise at this phase; enforcement is the adoption revision). Verification is: brokkr-core still builds and
all its tests still pass unchanged, the doctest count is unchanged, no `ResolveError` match broke, the I-8
doctests are unaffected, and the workspace error list is still the single pre-existing sentinel error.

## Result: brokkr-core 15 + 3 + 6 + 12 doctests pass; workspace error list unchanged

```
=== brokkr-core ===
     tests/negative_invariants.rs   : 15 passed
     tests/resolution_encoding.rs   :  3 passed
     tests/risk_shape.rs            :  6 passed
   Doc-tests brokkr_core            : 12 passed        (baseline 12 → 12, unchanged)
     - resolution::EscalationType (line 35) - compile fail ... ok   (I-8, unaffected)
     - resolution::ResolutionDecision (line 95) - compile fail ... ok   (I-8, unaffected)

=== workspace ===
cargo build --workspace          → all 8 libs build (brokkr-sentinel & brokkr-audit use
                                    ResolveError only by construction/comparison — no match broke)
cargo build --workspace --tests  → error[E0061] this function takes 7 arguments but 5 supplied
                                    --> brokkr-sentinel/tests/sentinel.rs:197:17
                                    (the SINGLE pre-existing error, from the Rev 1.12 core revision)
```

## What this establishes

| Check | Result |
|---|---|
| `brokkr-core` builds `--locked` | clean |
| existing brokkr-core tests unchanged | 15 + 3 + 6 pass |
| **doctest count before → after** | **12 → 12** (an enum variant adds no doctest) |
| **no `ResolveError` match broke** | every `ResolveError` use constructs or compares a variant — no exhaustive match exists, so no non-exhaustive-match error |
| **I-8 doctests unaffected** | both compile_fail doctests pass (verified by running); they concern struct-construction arity, orthogonal to the enum |
| **workspace error list** | still exactly one error — `sentinel.rs:197` E0061, pre-existing, the adoption revision's to fix; **nothing new introduced** |
| clippy `--all-targets` (§6 deny-list) | 0 |
| `cargo fmt -- --check` | clean |
| `#![no_std]` + `#![forbid(unsafe_code)]` | preserved |
| no new dependency (I-5) | `cargo tree -e no-dev -p brokkr-core` = `brokkr-core` alone |

## What was NOT tested, and why (§7)

- **Returning `Expired`/`ReplayedNonce` on an expired/replayed decision** — that enforcement (checking
  expiry, tracking accepted nonces per escalation) is the `brokkr-sentinel` adoption revision; this revision
  places the vocabulary only. The load-bearing `test_oqgf_p_8_5_replayed_nonce_is_refused` lands there.

— End of functionality test record FUNC-2026-08-14-R1.
