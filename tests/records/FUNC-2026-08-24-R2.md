# Test Record — Phase 15A (Grey-Hat Red Team, High Noise)

**Record ID:** FUNC-2026-08-24-R2
**Date:** 2026-08-24
**Crate:** `brokkr-cli` (`tests/redteam_15a.rs`) + governance-source confirmations (cited)
**Author:** Claude Code (builder / red team)
**Report:** `reports/REDTEAM-15A-2026-08-24-R1.md`

## Commands and results

| Command | Result |
|---|---|
| `cargo test --locked --workspace` | **353 passed, 0 failed** (343 baseline + 10 redteam_15a) |
| `cargo test -p brokkr-cli --test redteam_15a` | **10 passed** (G1 4, G2 5, combined 1) |
| `cargo clippy --workspace --all-targets --locked` | 0 warnings / 0 errors |
| `cargo fmt --all -- --check` | clean |

## Runtime attacks (redteam_15a.rs)

**G1 — architecture-informed outsider**
- `g1_1_1` — non-proposing model safe both paths (useless proposal → denied; backend `Err` → `HopResult::Error`).
- `g1_1_2` — scope IS enforced: a {read} chain denies `write_file` (`OutOfScope`), a {write} chain grants it → root breadth is the DAP's trust decision (F-25).
- `g1_1_5` — HEIMDALL trusts the orchestrator's constructed `Hop` (`executed == authorized` by copy, not measurement) → orchestrator in the TCB (F-23).
- `g1_1_6` — a genome declaring `dangerous_tool` grants it → the closed vocabulary is the wiring's choice (F-24).

**G2 — insider (tool layer)**
- `g2_2_1` — a `Gate` denial with a recorded `Proposal` leaks that BIFRÖST cleared + MÍMIR proposed (insider-informed F-21).
- `g2_2_2` — malicious suffix after the first pair: inert, and recorded in SAGA **sanitized** (no ESC/NUL/CSI).
- `g2_2_3` — an in-root symlink passes lexical `resolve` (F-2 boundary); `..`/absolute still rejected. `cfg(unix)`.
- `g2_2_4` — a permissive orchestrator admits a replay (same chain twice → both execute).
- `g2_2_6` — **F-22**: genome-refusal reason ("tool X not declared") vs SINDRI anergy reason distinguishable despite the uniform `Gate` stage.

**Combined**
- `combined_3_1` — undeclared / unknown-identity / expired-chain all denied; combined knowledge adds no bypass.

## Governance-source confirmations (behind the attacker's knowledge boundary)

- **SINDRI enforces all four conjuncts** — `brokkr-gate/src/sindri.rs:147-194` (Signal 1, Signal 2, action-in-scope → `OutOfScope`, action-respects-invariants → `InvariantViolated`). Basis for **F-25**.
- **Denial reason divergence** — `brokkr-cli/src/lib.rs:501` (genome `refusal.detail`) vs `:525` (`"architectural anergy: …"`). Basis for **F-22**.
- **`AuthorizedAction` unforgeable** from the tool layer — re-confirmed via 14A (E0451 private field, E0624 private mint); no new doctest needed (2.5 documented).

## Findings (documented, not fixed)

- **F-22** (LOW) — the denial reason string defeats F-5's uniform stage (genome-enumeration oracle re-opened on the reason). A genuine incompleteness the prior phases missed; concrete fix named.
- **F-23** (INFO) — the orchestrator is in the TCB (feeds HEIMDALL unverifiable observations); not named in §13.
- **F-24** (INFO) — no signed REGIN genome wired into the orchestrator; the per-hop vocabulary is the composition root's (deployment gap).
- **F-25** (INFO) — SINDRI enforces all four conjuncts; the §6.4/§13 "deferred conjunct" residual is met/closed in code — the spine is stricter than the design-informed attacker assumes.

## Test-infrastructure notes

Real dual-family PQC signed once serially in `fx()` (13A F-4): three chains — `legit` {write}/far, `read_only` {read}/far, `expired` {write}/expiry 500. Under `Guards::permissive()` denials report their **true** stage (so an undeclared tool shows `Genome`, not the production-uniform `Gate`); tests assert accordingly. `g2_2_6` uses `Guards::production()` to exercise the uniform-stage path where F-22 lives.

## Environment

Rust on PATH; real wolfSSL v5.9.2 (non-FIPS) via `.cargo/config.toml` rpath. Hermetic — no live model, no network. redteam_15a ≈ 14 s; workspace ≈ 7 min.
