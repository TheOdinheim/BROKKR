# Functionality Test Record — FUNC-2026-08-11-R1

**Phase:** 1 (revision) — `brokkr-core` host-harm types (Rev 1.11 §6.7)
**Date:** 11 August 2026
**Commands:** `cargo test --offline --locked -p brokkr-core`; `cargo test --offline --locked --workspace`
**Nature:** types-only core revision — no new runtime tests are added (the two placed types carry no
logic to test at this phase; their behavior is Phase 8). Verification is: the workspace still builds and
all existing tests still pass unchanged, the doctest count is unchanged, `Action: Clone` holds, and the
discipline checks are clean.

## Result: brokkr-core 15 + 6 tests + 12 doctests pass; workspace 0 failures

```
=== brokkr-core ===
     Running tests/negative_invariants.rs
test result: ok. 15 passed; 0 failed
     Running tests/risk_shape.rs
test result: ok. 6 passed; 0 failed
   Doc-tests brokkr_core
test result: ok. 12 passed; 0 failed          (baseline 12 → 12, unchanged)

=== workspace (all crates) ===
brokkr-crypto  : 5 + 6 passed
brokkr-intent  : 8 + 6 passed
brokkr-gate    : 10 passed
brokkr-genome  : 13 passed
brokkr-barrier : 21 passed
brokkr-audit   : 13 passed
brokkr-core    : 15 + 6 + 12 passed
result: ok — 0 failed across the workspace
```

## What this establishes

| Check | Result |
|---|---|
| `brokkr-core` builds `--locked` | clean |
| existing brokkr-core tests unchanged | 15 negative_invariants + 6 risk_shape, all pass |
| **doctest count before → after** | **12 → 12** (no runnable doc block added; baseline captured against HEAD, which `git diff --stat HEAD -- brokkr-core/` confirmed equal before editing) |
| **`Action` derives `Clone`** | confirmed in `gate.rs`; `HostHarmIncident` owns an `Action` and itself derives `Clone`/`Eq` — no workaround needed |
| downstream crates unchanged | crypto/intent/gate/genome/barrier/audit test counts identical to before; nothing constructs the new types yet |
| clippy `--all-targets` (§6 deny-list) | 0 |
| `cargo fmt -- --check` | clean |
| `#![no_std]` + `#![forbid(unsafe_code)]` | preserved |
| no new dependency (I-5) | `cargo tree -e no-dev -p brokkr-core` = `brokkr-core` alone |

## What was NOT tested, and why (§7)

- **Host-harm rate, bound comparison, autoimmunity, storm blast-radius** — all Phase 8 (`brokkr-sentinel`);
  this revision places the numerator *type* only, with no logic to exercise.
- **`Observation` / `Detector` / `DetectionVerdict` / `SelfSetCorpus`** — `brokkr-sentinel` types (§6.7),
  not part of this revision; nothing about them is placed or tested here.

— End of functionality test record FUNC-2026-08-11-R1.
