# Test Record — Phase 14B (White-box Red Team, Medium Noise)

**Record ID:** FUNC-2026-08-23-R3
**Date:** 2026-08-23
**Crates:** `brokkr-reasoner` (`ollama.rs` parser unit tests), `brokkr-cli` (`tests/redteam_14b.rs`)
**Author:** Claude Code (builder / red team)
**Full analysis:** `reports/REDTEAM-14B-2026-08-23-R1.md`

## Commands and results

| Command | Result |
|---|---|
| `cargo test --locked --workspace` | **327 passed, 0 failed** (301 baseline + 20 redteam_14b + 6 parser unit tests) |
| `cargo test -p brokkr-cli --test redteam_14b` | **20 passed** (categories 1, 3, 4, 5, 6) |
| `cargo test -p brokkr-reasoner ollama::tests::attack_2` | **6 passed** (category 2 strict-parser edge cases) |
| `cargo clippy --workspace --all-targets` | 0 warnings / 0 errors |
| `cargo fmt --all -- --check` | clean |

## Attacks (all confirm behavior; 0 critical, 0 high)

- **Cat 1 SINDRI** (redteam_14b): 1.1 empty-caps grants on empty scope; 1.2 no-op invariant grants; 1.3 earliest-conjunct-wins (`IdentityUnverified` before `InvariantViolated`); 1.4 `map_intent_error` `ChainExpired` exercised, all 6 variants mapped (Backend/HopKeyMissing unreachable-from-SINDRI, fail-closed).
- **Cat 2 parser** (ollama.rs): 2.1 `TOOL:`-no-value→unknown; 2.2 `PATH:`-no-value→empty; 2.3 last-pair-wins; 2.4 Cyrillic homoglyph byte-exact→unknown; 2.5 escaped inner JSON→unknown; 2.6 truncated→partial value / no-key→None.
- **Cat 3 FFI**: 3.1 bad cert paths→TlsError; 3.2 closed port→TlsError; 3.4 empty message signs+verifies; 3.5 1 MiB message signs+verifies; 3.6 wrong-key verify fails. No panic/UB.
- **Cat 4 guards**: 4.2 rate window boundary (6th denied @60_000, 7th admitted @60_001); 4.3 monotonic strict-less (equal time passes); 4.4 guard order monotonic→rate→replay (replay fires last).
- **Cat 5 chain**: 5.1 root-only verifies; 5.2 identical-scope attenuation grants; 5.3 deep 8-hop verifies, **≈4.54 ms/hop, linear** (100 hops ≈0.5 s), iterative (no stack risk).
- **Cat 6 sandbox**: 6.1 in-root symlink not resolved (F-2 limit); 6.2 `.`/`..` normalized; 6.3 absolute rejected; 6.4 `..` rejected; 6.5 empty→root dir (F-15).

## Findings (documented, not fixed — all LOW/info)

**F-13** replay-nonce map never evicts a `u64::MAX`-expiry chain (memory, fail-safe; MAX-expiry is itself a misconfig). **F-14** `sanitize_for_audit` drops the remainder after an unterminated ANSI CSI (fail-safe; drops untrusted text, not decisions). **F-15** `resolve("")` accepts the sandbox root dir (no escape, no write — fs rejects a dir target). **F-16** parser last-pair-wins (13B residual restated; still gated). **F-17** `json_string_field` returns a partial value on a truncated string (fail-safe downstream). Each carries a concrete optional hardening in the report.

## Test-infrastructure notes

- Real dual-family signing done **once, serially** in `fx()` (13A F-4 concurrent-signing avoidance); attestation `signatures` use a hand-built (unverified) `DualSignature` since SINDRI does not verify that field (Rev 1.3 §6.4).
- **Test bug found and fixed during the run:** `sandbox()` originally used one dir name per process → parallel tests raced on create/`remove_dir_all` (6.1 failed once). Fixed with a per-call atomic-counter unique dir. Not a finding — a test defect.
- Deep chain kept at 8 hops (not 100) to bound suite runtime; timing recorded shows linear scaling. 3.5 kept at 1 MiB (not 10 MiB) for the same reason.
- Category 2 co-located in `ollama.rs` (private `parse_action`), per the 13B/14A precedent; a `.expect()` there tripped the crate's `deny(clippy::expect_used)` and was rewritten.

## Environment

Rust on PATH; real wolfSSL v5.9.2 (non-FIPS) via `.cargo/config.toml` rpath. Hermetic — no live model, no network. redteam_14b ≈74 s (per-test keygen + deep chain); workspace ≈433 s.
