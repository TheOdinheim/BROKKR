# Test Record — Phase 15B (Grey-Hat Red Team, Medium Noise)

**Record ID:** FUNC-2026-08-24-R3
**Date:** 2026-08-24
**Crate:** `brokkr-cli` (`tests/redteam_15b.rs`) + governance-source / corpus confirmations
**Author:** Claude Code (builder / red team)
**Report:** `reports/REDTEAM-15B-2026-08-24-R1.md`

## Commands and results

| Command | Result |
|---|---|
| `cargo test --locked --workspace` | **360 passed, 0 failed** (353 baseline + 7 redteam_15b) |
| `cargo test -p brokkr-cli --test redteam_15b` | **7 passed** (G1 2, G2 4, combined 1) |
| `cargo clippy --workspace --all-targets --locked` | 0 warnings / 0 errors |
| `cargo fmt --all -- --check` | clean |

## Runtime attacks (redteam_15b.rs)

**G1 — inference**
- `g1_1_2_invariant_inference_precision` — a `network` tool denied `InvariantViolated`, a `write` tool granted; the invariant NAME (`no-network`) is never leaked → inference yields the forbidden capability, not the invariant identity.
- `g1_1_4_nonce_is_dap_controlled_not_model` — a replay is denied under production guards regardless of the proposal; the nonce is in the DAP-signed chain, outside the model's reach.

**G2 — targeted exploitation of the F-22 oracle**
- `g2_2_1_reason_string_distinguishes_under_production_guards` — undeclared (`"tool X not declared"`) vs declared-out-of-scope (`"architectural anergy: OutOfScope"`), both uniform `Gate` stage.
- `g2_2_2_genome_fully_enumerable_via_reason_strings` — **F-26**: 5 declared + 5 undeclared names classified by reason under production guards; perfect classification → whole vocabulary enumerable.
- `g2_2_3_denial_reason_signature_table` — five pairwise-distinct reasons (genome `not declared`, `IdentityUnverified`, `OutOfScope`, `InvariantViolated`, `ChainExpired`); the reason alone names the failing stage.
- `g2_2_6_tool_output_is_an_unvalidated_covert_channel` — **F-27**: a tool smuggles a marker + the action detail through `HopResult::Executed { output }`.

**Combined**
- `combined_3_1_enumerate_exploit_audit_intact` — map the genome → run a declared in-scope tool → SAGA records the proposal AND the granted authorization; `HopResult` gives no read/alter path → succeed but cannot hide.

## Corpus / source confirmations

- **Corpus is AMD-001 … AMD-009** (verified `ls governance/`). The task's §1.3 AMD-010 … AMD-017 **do not exist**; no coverage was fabricated for them (report §4). Actual-corpus coverage tabulated in report §4 from BROKKR-ARCH §14 cross-checked against the tree.
- **Conjunct 4 (invariants)** — `brokkr-gate/src/sindri.rs` resolves each chain invariant to `ResolvedInvariant { forbids_capabilities, forbids_privilege }`; a tool requiring a forbidden capability → `InvariantViolated` (name not leaked). Basis for 1.2.
- **Reason divergence** — `lib.rs:501` (genome `refusal.detail`) vs `:525` (`"architectural anergy: …"`). Basis for F-26.

## Findings (documented, not fixed)

- **F-26** (LOW) — the F-22 reason-string oracle enumerates the entire genome (perfect declared/undeclared classification); the quantified impact of F-22. Fold into the F-22 fix.
- **F-27** (INFO) — `ToolOutcome.output` is an unvalidated trust boundary (covert/exfil channel to the caller). Inherent; OS-sandbox concern.
- **F-28** (INFO) — tool filesystem *reads* are unrestricted (`SandboxedTool` gates writes only); a file-backed SAGA/secret would be reachable. In-memory today; deployment boundary.

## Test-infrastructure notes

Real dual-family PQC signed once serially in `fx()` (13A F-4): four chains — `write_scope`, `read_scope` (the enumeration classifier chain: a declared write-tool becomes `OutOfScope`), `net_scope` ({write,network} + `no-network` invariant), `expired` (expiry 500). Enumeration probes build a fresh rig per tool name (fixed genome, varying proposal); no re-signing. `g2_2_2`/`combined_3_1`/`g2_2_1` use `Guards::production()` to exercise the uniform-stage path where F-26 lives.

## Environment

Rust on PATH; real wolfSSL v5.9.2 (non-FIPS) via `.cargo/config.toml` rpath. Hermetic — no live model, no network. redteam_15b ≈ 18 s; workspace ≈ 7 min.
