# Test Record — F-32 closure + corpus conformance enumeration — FUNC-2026-09-02-R3

**Record ID:** FUNC-2026-09-02-R3
**Date:** 2026-09-02
**Type:** Construction (DAP-directed) — closes F-32; records the §5.3 corpus-growth conformance enumeration.
**Author:** Claude Code (builder)

## F-32 fix

`brokkr-cli/src/lib.rs::with_envelope` (`:362`) now hard-`assert!`s `envelope.validate().is_ok()`
before installing — an invalid envelope (`TierMismatch` / `TierTooLow`) is rejected, never governs a
run (I-14). Fail-closed, same pattern as `permissive()` (F-33); `assert!` not `.expect()` per §6.

## Result

- **Workspace GREEN — 409 passed, 0 failed.**
- `cargo clippy -p brokkr-cli --all-targets` + `cargo fmt --all -- --check` — clean.
- Regressions (both `#[should_panic(expected = "capability envelope must be valid")]`), replacing the
  Phase-16 tests that documented F-32:
  - `redteam_16::fix_with_envelope_rejects_tier_too_low`
  - `redteam_16::fix_with_envelope_rejects_tier_mismatch`
  Both fail against the pre-fix code (which installed the invalid envelope and ran).
- All existing `with_envelope(..)` call sites use valid envelopes (`brokkr_envelope`, `permissive`,
  `envelope_with_rule`, empty/under-declared but tier-consistent), so none newly panics.

## Conformance enumeration (§5.3)

`conformance/CONF-2026-09-02-CORPUS-R1.md` — 14 requirements (OQGF-A-8…A-12, OQGF-P-12.1…P-12.8,
OQGF-A-1 evidence-provenance) verified against source: **5 n.a., 7 satisfied, 2 partial, 0 absent**.
Each verdict cites file:line + the test that fails if the check were removed (RISK-2026-0007).

## Disposition

F-32 **closed**. This closes the last of the seven Phase-16 findings that were code defects
(F-35/F-36/F-33/F-34 fixed earlier; F-32 now; F-37/F-38 accepted by design).
