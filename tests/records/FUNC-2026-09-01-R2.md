# Test Record — Phase 16-FIX — FUNC-2026-09-01-R2

**Record ID:** FUNC-2026-09-01-R2
**Date:** 2026-09-01
**Phase:** 16-FIX (harden post-AMD red-team findings; construction)
**Author:** Claude Code (builder)

---

## Result

- **Workspace GREEN — 406 passed, 0 failed** (404 + 2 net new regressions).
- `cargo clippy -p brokkr-cli -p brokkr-core --all-targets` + `cargo fmt --all -- --check` — clean.
- No new dependency; no cert/key material staged; no production logic weakened.

## Fixes and their regressions (each fails without the fix)

| Fix | Finding | Code path | Regression test |
|---|---|---|---|
| 1 | F-36 | `brokkr-cli` `KillSwitch` (latch, hardcoded `Release`/`Acquire`); `Orchestrator::kill_switch()`; `run_hop` reads `is_killed()` | `redteam_16::fix_kill_switch_is_a_latch` |
| 2 | F-35 | `brokkr-cli` `check_egress` + `normalize_destination` (localhost/127.0.0.1/::1 fold) | `redteam_16::fix_egress_ip_hostname_equivalence`, `fix_egress_still_denies_unknown` |
| 3 | F-32 area | `brokkr-core` `CapabilityEnvelope::permissive()` `debug_assert!(validate().is_ok())` | `redteam_16::fix_permissive_validates` |

## Tests converted in place (renamed, not deleted)

- `f35_egress_host_representation_sensitive` → `fix_egress_ip_hostname_equivalence` (now asserts the normalized destination is admitted).
- `f36_kill_flag_is_not_a_latch` → `fix_kill_switch_is_a_latch` (now asserts the latch).
- `f37_kill_flag_is_pre_hop_not_mid_hop` — kept; kill call converted to `KillSwitch` (behaviour unchanged).
- `tests/capability.rs` kill tests — kill call converted to `KillSwitch`; unused `Ordering` import removed.

## Accepted (documented, not fixed)

F-32 (caller contract, mitigated by Fix 3), F-33 (fail-open default), F-34 (port/protocol needs a `Destination::Network` type change), F-37 (pre-hop by design), F-38 (in-memory trajectory). F-32/F-33/F-34 referred to the DAP — see `reports/SECURITY-BOUNDARIES-2026-09-01-R1.md`. Conformance records P-12.4 and P-12.1/2 as **PARTIAL** (F-34, F-33) rather than overclaiming.

## Artifacts

- Report: `reports/PHASE-16FIX-2026-09-01-R1.md`
- Conformance: `conformance/CONF-2026-09-01-P16FIX-R1.md`
- Boundaries: `reports/SECURITY-BOUNDARIES-2026-09-01-R1.md`
