# Functionality Test Record — FUNC-2026-08-12-R1

**Phase:** 8 (`brokkr-sentinel` / HEIMDALL + EIR)
**Date:** 12 August 2026
**Command:** `cargo test --offline --locked -p brokkr-sentinel`
**Backend:** real wolfSSL v5.9.2 (non-FIPS) through `brokkr-crypto` where signatures are involved — no
mocks. Tolerance grants and resolution decisions are signed with a real `DualKeyPair` over the real
canonical encoding; verification imports raw public bytes (`DualPublicKey::from_public_bytes`).

## Result: 18 tests passed, 0 failed

```
     Running tests/sentinel.rs
test test_oqgf_p_2_tolerance_refuses_deterministic_target ... ok
test test_oqgf_p_4_valid_grant_attaches_on_heuristic ... ok
test test_oqgf_p_4_expired_grant_refused ... ok
test test_oqgf_p_4_forged_grant_refused ... ok
test test_oqgf_p_4_blanket_scope_refused ... ok
test test_oqgf_p_3_single_firing_fails_screening ... ok
test test_screening_passes_when_detector_clears ... ok
test test_oqgf_p_3_substituted_corpus_is_refused ... ok
test test_host_harm_zero_denominator ... ok
test test_oqgf_p_5_storm_needs_declared_radius ... ok
test test_host_harm_autoimmunity_is_sustained_not_single ... ok
test test_oqgf_m_12_mismatch_deviates_and_none_does_not ... ok
test test_oqgf_p_8_5_cannot_lower_above_baseline_without_dap ... ok
test test_resolve_succeeds_with_dap_and_eligibility ... ok
test test_forged_signal_cannot_stand_down ... ok
test test_oqgf_p_8_3_hysteresis_not_satisfied ... ok
test test_oqgf_p_8_6_chronic_is_flagged ... ok
test test_sentinel_domain_separated_from_genome_barrier_audit ... ok
test result: ok. 18 passed; 0 failed; finished in 12.36s
```

## What the tests prove

| Test | Requirement | Establishes |
|---|---|---|
| **`test_oqgf_p_2_tolerance_refuses_deterministic_target`** | **P-2 (central property)** | `grant(_, Deterministic)` → `Err(NonSuppressibleGate)` via core's sealed `grant`; never a silent no-op, and `grant_heuristic` is never reached for a Deterministic class |
| `test_oqgf_p_4_valid_grant_attaches_on_heuristic` | P-4 | a signed, scoped, unexpired grant on a Heuristic target attaches |
| `test_oqgf_p_4_expired_grant_refused` | P-4 | `now > expiry` → `Err(Expired)` |
| `test_oqgf_p_4_forged_grant_refused` | P-4 | a grant signed by a non-DAP key → refused (`OutOfScope`, fail-closed) |
| `test_oqgf_p_4_blanket_scope_refused` | P-4 | an empty (blanket) scope → `Err(OutOfScope)` (OQGF-P-4 "never blanket") |
| `test_oqgf_p_3_single_firing_fails_screening` | P-3 | a detector firing on **one** corpus observation fails — not a threshold |
| `test_screening_passes_when_detector_clears` | P-3 | a detector that never fires over a verified corpus passes |
| **`test_oqgf_p_3_substituted_corpus_is_refused`** | **P-3 (substituted-corpus attack)** | a corpus whose recomputed digest ≠ the declared `SelfSet.corpus_digest` is refused, and the spy detector is **never observed** — nothing runs |
| `test_host_harm_zero_denominator` | P-1 | zero governed actions → `rate.is_nan()` (not `0.0`), autoimmunity `false` — no false safety, no panic |
| **`test_oqgf_p_5_storm_needs_declared_radius`** | **P-5b** | with no declared radius, `assess_response` → `Unenforceable` (says so), never a silent "no storm"; with a radius, `Exceeds`/`Within` |
| `test_host_harm_autoimmunity_is_sustained_not_single` | P-5a | one over-bound window is not autoimmunity; the injected threshold of consecutive over-bound windows is |
| `test_oqgf_m_12_mismatch_deviates_and_none_does_not` | M-12 | a `Hop` mismatch deviates → raise Signal; `executed: None` does **not** deviate; a matching hop does not |
| **`test_oqgf_p_8_5_cannot_lower_above_baseline_without_dap`** | **P-8.5 (load-bearing asymmetry)** | a decision signed by a non-DAP key → `Err(NeedsDapConfirmation)` — a forged/replayed decision cannot stand the system down |
| `test_resolve_succeeds_with_dap_and_eligibility` | P-8.2/8.5 | a DAP-signed decision + eligibility → `Ok(ReturnedToBaseline)` |
| `test_forged_signal_cannot_stand_down` | P-7.4 | structural — `PostureEffect` has only `Raise`; an emitted signal only raises posture |
| `test_oqgf_p_8_3_hysteresis_not_satisfied` | P-8.3 | clear condition met but hold window unsatisfied → `NotYet` |
| `test_oqgf_p_8_6_chronic_is_flagged` | P-8.6 | past `max_duration` → `may_resolve` `Chronic` and `scan_chronic` returns it |
| **`test_sentinel_domain_separated_from_genome_barrier_audit`** | Task 2 (four-crate) | sentinel bytes begin with `brokkr-sentinel:corpus:v1` and none of the genome/barrier/audit tags; a signature over sentinel bytes does not verify over foreign-tagged bytes, and vice versa |

## Discipline checks

- Clippy (§6 deny-list), `--all-targets`: **0** (`large_enum_variant` on `Observation::Crossing` allowed
  with justification — matches §6.7's placed shape). `cargo build --locked`: clean. `cargo fmt --check`:
  clean.
- **No `unsafe`:** `#![forbid(unsafe_code)]`; no FFI of its own.
- **I-5:** `cargo tree -e no-dev -p brokkr-sentinel` = `{brokkr-core, brokkr-crypto}` — no sibling crate,
  not even as a dev-dependency (domain separation tested by tag distinctness at the byte level).
- **I-6:** no model calls. **I-9 (sentinel):** `DetectionVerdict` has no action-permitting variant.
- **Workspace unchanged:** `cargo test --locked --workspace` → **135 passed, 0 failed**.
- No mocks (real crypto). No `unwrap`/`expect` in `brokkr-sentinel` production paths (the `Mutex` guard is
  recovered via `unwrap_or_else(|p| p.into_inner())`; tests may use `unwrap`).

## What was NOT tested, and why (§7)

- **Concrete detectors / a production Self Set** — out of scope (§6.7); test doubles serve the tests.
  Screening is inert until a real Self Set exists (§13).
- **The live reconciliation stream** — `executed` is `None` until the executor (Phase 11); M-12 is PARTIAL.
- **Emission to other organs / recording to SAGA** — HEIMDALL produces Signals as values; the orchestrator
  (Phase 11) delivers them.
- **The OQGF-M-6 reconciliation statistic** — Phase 11 (Rev 1.11).

— End of functionality test record FUNC-2026-08-12-R1.
