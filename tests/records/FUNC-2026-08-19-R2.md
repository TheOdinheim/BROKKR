# Test Record — brokkr-sentinel Revision R3 (ScreenPass::produced_at)

**Record ID:** FUNC-2026-08-19-R2
**Date:** 2026-08-19
**Crate:** `brokkr-sentinel` (HEIMDALL), ARCH Rev 1.17 §6.12
**Author:** Claude Code (builder)

## Commands and results

| Command | Result |
|---|---|
| `cargo build --locked -p brokkr-sentinel` | Finished, clean |
| `cargo test --locked -p brokkr-sentinel` | **28 passed, 0 failed** (was 27; +1 new) |
| `cargo build --locked --workspace` | Finished — **green** |
| `cargo test --locked --workspace` | **160 passed, 0 failed** |
| `cargo clippy --locked --workspace --all-targets` | Finished, no warnings |
| `cargo fmt --all -- --check` | clean |

## Workspace test breakdown (real wolfSSL v5.9.2 non-FIPS)

Runtime (143): audit 13, barrier 21, bifrost 8, core 16+3+6, crypto 5+6, gate 10, genome 13,
intent 8+6, **sentinel 28**. Doctests (17): barrier 1, bifrost 1, core 14, genome 1. Grand total 160,
0 failed. Prior recorded green was 159 (PHASE-8-REV-2026-08-18-R2); the +1 is the new Task-4 test.

## New / changed tests

- **New:** `test_oqgf_p_6_3_i13_screen_pass_carries_screened_version_and_call_site_now` — a clearing
  detector screened against `matching_selfset` (version `v1`) with `now = Timestamp(4242)`; asserts the
  returned `ScreenPass` carries `version == v1` (the Self Set actually screened against, not a default)
  and `produced_at == Timestamp(4242)` (the per-call `now`, never held). Names OQGF-P-6.3 + I-13.
- **Changed:** the three existing `screen` call-sites (`test_oqgf_p_3_single_firing_fails_screening`,
  `test_screening_passes_when_detector_clears`, `test_oqgf_p_3_substituted_corpus_is_refused`) now pass
  a `now` argument. Their assertions are unchanged and still pass.
- **Re-verified:** `test_oqgf_p_2_tolerance_refuses_deterministic_target` (I-4/P-2) passes against the
  now-inherent `grant` — the Deterministic refusal is preserved.

## Held-clock grep (SHALL be empty)

`grep -rn "SystemTime::now\|Instant::now\|self\.now\|Mutex<Timestamp>\|set_now\|now_guard" brokkr-*/src/`
→ **empty** (exit 1, no matches). Every `now: Timestamp` in src is a call parameter, not a struct field.

## Invariants / hygiene

- I-5: `brokkr-sentinel` deps = `brokkr-core` + `brokkr-crypto` only (Cargo.toml unchanged).
- I-6: HEIMDALL makes no model call.
- No new external dependency.

## Environment

- Rust on PATH; real crypto backend (wolfSSL rpath via `.cargo/config.toml`).
