# Functionality Test Record — FUNC-2026-08-18-R3

**Phase:** 8.5 (rebuild) — BIFRÖST adopts the I-13 surface; held clock removed
**Date:** 18 August 2026
**Commands:** `cargo test --locked -p brokkr-bifrost`; `cargo test --locked --workspace`; the
four-pattern held-clock grep across all crates.
**Backend:** real wolfSSL v5.9.2 (non-FIPS) through `brokkr-crypto` — real dual-family BCR
signatures, the real HÚÐ gate. No mocks.

## Result: bifrost 8 passed + 1 doctest; WHOLE WORKSPACE 153 passed, 0 failed

```
cargo test --locked -p brokkr-bifrost
  bifrost   : 8 passed; 0 failed   (was 7 — +1: the two-verdict freshness test)
  doc-test  : 1 passed; 0 failed   (compile_fail,E0451 no-minting)

cargo test --locked --workspace
  153 passed; 0 failed   (every suite "test result: ok"; no FAILED, no error[)
```

**First green workspace since the I-13 core revision.**

## The new test — the two-verdict freshness proof

| Test | Proves |
|---|---|
| **`test_oqgf_m_14_i13_i9_bcr_expiry_evaluated_against_call_time_not_a_held_clock`** | ONE `Bifrost` instance, SAME context + destination, `clear` twice: `now=1_000` (< BCR expiry 5_000) **clears** (mints a `ClearedContext`); `now=10_000` (> 5_000) **Deny(Expired)**. A held clock yields one verdict per crossing per instance, so two verdicts from one instance is impossible against the pre-I-13 design — this test cannot pass against a stored clock. |

**Every original test shared the defect's assumption (reviewed per Task 3).** All seven built
`Bifrost::new(…, Timestamp(NOW))` — time at **construction** — and evaluated **once**; none could
detect a held clock. Their fix: the time became a **call-site** argument (`Timestamp(NOW)` on each
`evaluate_context`/`clear`), correct under I-13, but single-evaluation tests cannot catch a held
clock — that is the new test's job. The seven keep their meaning (mTLS collapse, effective-is-min,
no-BCR deny, public-personal-no-short-circuit, crossing-record, no-payload-inspection, pqc-permits).

## Invariant / discipline checks

- **I-13 (this crate):** `brokkr-bifrost` holds no clock. Four-pattern grep on `brokkr-bifrost/src`
  (`self\.now`, `now:.*Timestamp,` field form, `Mutex<Timestamp>`, `fn now(`) — **empty**. Struct is
  `{ huth, ceiling }`; no interior mutability; the only `now: Timestamp` is the `evaluate_context`
  parameter.
- **I-12:** no `fn clear` override; the one `ClearedContext {` is the `compile_fail,E0451` doctest
  that proves the construction fails.
- `clippy --all-targets`: clean. `fmt --check`: clean. **I-5/I-6:** deps `{core, barrier, crypto
  (transitive)}`; no model call, no network, no `unsafe`. **No new external dependency.**

## Workspace held-clock grep — the finding (GAP-2026-08-18-001)

The four-pattern grep across **all** crates (RISK-2026-0006's closure tool): `self\.now`,
`Mutex<Timestamp>`, `fn now(` **empty workspace-wide**; `now:.*Timestamp,` matches only fn parameters
**except two struct fields** — `EirState.now` and `HeimdallState.now` in `brokkr-sentinel`. Both
engines check an expiry against the held clock (EIR resolution-decision expiry, `eir.rs:242`;
HEIMDALL tolerance-grant expiry, `heimdall.rs:268`). A third I-13 held clock, out of scope, filed as
a gap. RISK-2026-0006 is therefore **not** closed (workspace grep not empty).

## What was NOT tested, and why (§7)

- **brokkr-sentinel's held clocks** — out of scope (the scope lock forbids modifying it); the finding
  is filed as GAP-2026-08-18-001 for a DAP-placed sentinel revision, not tested/fixed here.
- **A real TLS handshake / negotiated-group readback** — out of scope (§6.10); the negotiated group
  is an input on `Destination::Reasoner`. No sockets.
- **SAGA recording of the crossing / a real model call** — Phase 11 / Phase 10.

— End of functionality test record FUNC-2026-08-18-R3.
