# Functionality Test Record — FUNC-2026-08-14-R2

**Revision:** `brokkr-sentinel` + `brokkr-audit` Rev 1.12/1.13 adoption
**Date:** 14 August 2026
**Commands:** `cargo test --offline --locked -p brokkr-sentinel`; `-p brokkr-audit`; `--workspace`
**Backend:** real wolfSSL v5.9.2 (non-FIPS) through `brokkr-crypto` where signatures are involved — no
mocks. Resolution decisions are signed with a real `DualKeyPair` over **core's** canonical encoding
(`brokkr_core::resolution::resolution_signed_content`), the same bytes EIR verifies over.

## Result: sentinel 21, audit 13; workspace 141 passed, 0 failed

```
     Running tests/sentinel.rs
test test_oqgf_p_8_5_expired_decision_is_refused ... ok
test test_oqgf_p_8_5_replayed_nonce_is_refused ... ok
test test_sentinel_and_core_encodings_agree ... ok
test test_oqgf_p_4_forged_grant_reports_signature_invalid ... ok
test test_oqgf_p_8_5_cannot_lower_above_baseline_without_dap ... ok
test test_resolve_succeeds_with_dap_and_eligibility ... ok
… (21 total)
test result: ok. 21 passed; 0 failed

     Running tests/audit.rs        (brokkr-audit)
test result: ok. 13 passed; 0 failed        (write_resolution now records nonce/expiry; no test regressed)

     workspace: 141 passed; 0 failed
```

## What the new / changed tests prove

| Test | Requirement | Establishes |
|---|---|---|
| **`test_oqgf_p_8_5_replayed_nonce_is_refused`** | **P-8.5 (THE replay test)** | a decision resolves once; presented again with the same nonce — **after `e1` was re-raised** — is refused `ReplayedNonce`. Proves the accepted nonce persists across resolution and re-raising (not reusable), the property the revision stopped for rather than shipping without |
| `test_oqgf_p_8_5_expired_decision_is_refused` | P-8.5 | a decision valid in every respect except `now > expiry` → `Err(Expired)` — latency, named as latency |
| `test_sentinel_and_core_encodings_agree` | Task 1 | a decision signed over core's encoding resolves (EIR verifies over those exact bytes); one signed over unrelated bytes → `NeedsDapConfirmation`. Guards against the sentinel's verify path and core's encoding ever diverging again |
| `test_oqgf_p_4_forged_grant_reports_signature_invalid` | P-4 | a forged grant → `Err(SignatureInvalid)`, not `OutOfScope` — **corrected expectation** (renamed from `_forged_grant_refused`; Phase 8 reported `OutOfScope` for lack of a variant, which now exists) |
| `test_oqgf_p_8_5_cannot_lower_above_baseline_without_dap` | P-8.5 | unchanged behaviour: an attacker-signed decision → `NeedsDapConfirmation` (signature checked first) |
| `test_resolve_succeeds_with_dap_and_eligibility` | P-8.2/8.5 | a DAP-signed, fresh, unreplayed, eligible decision → `Ok` (helper now signs over core's encoding) |

## Discipline checks

- Clippy (§6 deny-list), `--all-targets`: **0**, no warnings. `cargo build --locked --workspace`: clean.
  `cargo fmt --check`: clean.
- **I-5 (both crates):** `cargo tree -e no-dev` = `{brokkr-core, brokkr-crypto}` for `brokkr-sentinel` and
  `brokkr-audit` — no dependency added. **I-6:** no model calls; `#![forbid(unsafe_code)]`.
- **Encoding unification:** the sentinel-local `resolution_signed_content` (tag `brokkr-sentinel:resolution:v1`,
  no nonce/expiry) is **deleted**; EIR and the test helper both use core's. Grep confirms no
  `brokkr-sentinel:resolution` tag remains.
- No mocks. No `unwrap`/`expect` in production paths (the `Mutex` guard is recovered; tests may `unwrap`).

## What was NOT changed, and why (§7)

- **`brokkr-core`** — untouched; the two `ResolveError` variants and the encoding were placed by prior
  revisions (Rev 1.12/1.13). **`brokkr-audit` tests** — none hardcodes resolution bytes, so recording
  nonce/expiry changed no expected value; all 13 pass over the amended encoding.

— End of functionality test record FUNC-2026-08-14-R2.
