# Test Record — Phase 9 (brokkr-adapt / KVASIR)

**Record ID:** FUNC-2026-08-19-R4
**Date:** 2026-08-19
**Crate:** `brokkr-adapt` (built under ARCH Rev 1.17 §6.12)
**Author:** Claude Code (builder)

## Commands and results

| Command | Result |
|---|---|
| `cargo build --locked -p brokkr-adapt` | Finished, clean |
| `cargo test --locked -p brokkr-adapt` | **13 passed + 1 doctest, 0 failed** (real crypto) |
| `cargo build --locked --workspace` | Finished — **green** |
| `cargo test --locked --workspace` | **175 passed, 0 failed** |
| `cargo clippy --locked -p brokkr-adapt --all-targets` | Finished, no warnings |
| `cargo fmt --all -- --check` | clean |

## Workspace breakdown (real wolfSSL v5.9.2 non-FIPS)

Runtime (157): **adapt 13**, audit 13, barrier 21, bifrost 8, core 17+3+6, crypto 5+6, gate 10,
genome 13, intent 8+6, sentinel 28. Doctests (18): **adapt 1**, barrier 1, bifrost 1, core 14,
genome 1. Total 175, 0 failed. (Prior green 160 + core R12's +1 integration test + adapt's 13
runtime + 1 doctest = 175.)

## The 13 tests

- Positive: `test_full_pipeline_activates_and_returns_prior_generation` — generate → select →
  activate, asserting `PriorGeneration{1}` and a Heuristic detector.
- Gate 1 (P-6.1): `test_oqgf_p_6_1_unconfirmed_seed_refused`,
  `test_oqgf_p_6_1_seed_confirmed_by_a_different_dap_refused`.
- Gate 2 (P-6.2): `test_oqgf_p_6_2_substituted_corpus_refused` (spies assert **nothing measured**),
  `_stale_corpus_refused`, `_corpus_containing_seed_refused`, `_overfit_refused`,
  `_coverage_regression_refused`.
- Gate 3 (P-6.3): **`test_oqgf_p_6_3_raises_host_harm_refused`** — real gains (select passes) yet
  `FailsTolerance` (fires on the Self Set): no trade. `_stale_screen_pass_refused` — a superseded
  Self Set version, however recent, → `FailsTolerance`.
- Gate 4 (P-6.6): `test_oqgf_p_6_6_activation_without_dap_approval_refused` — impostor-signed
  provenance → `NeedsApproval`.
- I-9: `test_i9_refined_detector_cannot_be_deterministic` (runtime Heuristic) + the `compile_fail`
  doctest in `lib.rs` (a Deterministic `RefinedDetector` does not compile — the 1 doctest above).
- Task 6: `test_adapt_domain_separated_from_all_siblings` — both adapt tags ≠ all 17 sibling tags,
  and a real provenance encoding begins with the adapt tag.

## Structural / hygiene

- four-pattern held-clock grep (`SystemTime::now`, `Instant::now`, `self.now`, `Mutex<Timestamp>`,
  `set_now`, `now_guard`) across all `brokkr-*/src` — **empty**.
- `cargo tree -p brokkr-adapt` — core + crypto + sentinel; **no `brokkr-reasoner`** (I-9).
- `Cargo.lock` diff — one `brokkr-adapt` entry (three path deps); **no new external dependency**.
- `#![forbid(unsafe_code)]`; no `unwrap`/`expect`/`panic` in `src`.

## Environment

- Rust on PATH; real crypto backend (wolfSSL rpath via `.cargo/config.toml`).
- The full `cargo test --locked --workspace` takes ~10 min (real PQC signing/verify across crates);
  per-crate `-p` runs are far faster for iteration.
