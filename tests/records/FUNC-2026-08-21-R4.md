# Test Record — Phase 13C (Black-box Red Team, Low Noise)

**Record ID:** FUNC-2026-08-21-R4
**Date:** 2026-08-21
**Crate:** `brokkr-cli` (`tests/redteam_13c.rs`)
**Author:** Claude Code (builder / red team)
**Full analysis:** `reports/REDTEAM-13C-2026-08-21-R1.md`

## Commands and results

| Command | Result |
|---|---|
| `cargo test -p brokkr-cli --test redteam_13c` | **18 passed, 0 failed** — 19s (real dual-family PQC signing, hermetic) |
| `cargo test --locked --workspace` | **276 passed, 0 failed** (258 prior + 18; real crypto) |
| `cargo clippy --workspace --all-targets` | 0 warnings / 0 errors |
| `cargo fmt --all -- --check` | clean |

## What the 18 tests establish

~20 vectors, 5 categories. **0 critical, 0 high, 0 panics, 0 lost records, 0 concurrency corruption.**

- **Cat 1 (clock, 5):** `now=0` → granted; `now=u64::MAX` → DENIED (ChainExpired); `now==expiry` → **granted (inclusive boundary)**; backward clock → both granted (**F-9**); TOCTOU → granted, **`now` frozen per hop (I-13 atomicity)**.
- **Cat 2 (scope creep, 3):** same `{write}` capability escalates `/tmp` → `~/.ssh` → `/etc/shadow`, **all granted** (§13 — write authority = write-anywhere); 50 hops → all granted (**F-7 no rate limit**); alternating targets → HEIMDALL saw Hop, fired nothing (empty detector set).
- **Cat 3 (audit content, 3):** misleading multi-line detail + NUL/ANSI rationale → recorded **verbatim** (**F-8**); 500 hops → **1000 events, none lost**.
- **Cat 4 (concurrency, 3):** 10 concurrent same-chain hops → all granted consistently; 5-fresh/5-expired → exactly 5/5; SAGA → 20 events, none lost. **Confirms the verify path is thread-safe — contrast 13A F-4's signing race.**
- **Cat 5 (semantics, 4):** tool-impl≠declaration → granted (trust assumption); piggyback shell string → granted literal; undeclared capability in scope → granted/ignored (**F-10**); empty-name invariant → **DENIED, fail-closed**.

## Findings (documented, not fixed — per protocol)

0 critical, 0 high. **F-7** (LOW) no per-hop rate limiting. **F-8** (LOW-MEDIUM) audit records carry unsanitized control chars / misleading content (render-layer concern). **F-9** (LOW) non-monotonic `now` accepted (not model-controlled; driver concern). **F-10** (INFORMATIONAL) undeclared capability in Root Intent scope silently accepted (harmless/unusable). See report for the consolidated 13A–13C hardening ranking.

## Confirmed strengths

I-13 atomicity closes the expiry TOCTOU; the **verify/read path is concurrency-safe** (the 13C counterpoint to 13A F-4); fail-closed on degenerate invariants; well-defined expiry boundary; no dropped audit records under load/concurrency. The §13 residual (target/behavior not gated) is re-confirmed under a patient threat model and located precisely at the **tool** and **audit-render** layers.

## Test-infrastructure notes

Duplicated 13A/13B harness pattern; all real signatures once, serially in one `OnceLock` fixture (13A F-4 avoidance). Concurrency tests use `std::thread::scope` sharing `&Orchestrator` (it is `Send + Sync`). Spy SAGA/tool are crypto-free and the tool spy **never writes**. `PuppetReasoner` gained a configurable `rationale` for the audit-content tests.

## Environment

Rust on PATH; real wolfSSL v5.9.2 (non-FIPS) via `.cargo/config.toml` rpath. Hermetic — no live model, no network.
