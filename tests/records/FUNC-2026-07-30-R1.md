# Functionality Test Record — FUNC-2026-07-30-R1

**Phase:** 1 revision R4 (`brokkr-core` — the Personal-Data Tag, Rev 1.7)
**Date:** 30 July 2026
**Commands:** `cargo test --locked -p brokkr-core` and `cargo test --locked --workspace`
**Backend:** brokkr-core is `#![no_std] + alloc`, zero dependencies. Downstream crates test against
real wolfSSL v5.9.2 (non-FIPS), unchanged.

## Result: all pass; downstream unchanged; doctest count unchanged (12 → 12)

### brokkr-core

```
     Running unittests src/lib.rs         — 0 passed
     Running tests/negative_invariants.rs — 12 passed, 0 failed
     Running tests/risk_shape.rs          — 6 passed, 0 failed
   Doc-tests brokkr_core                  — 12 passed, 0 failed
```

### Workspace (downstream STILL PASSES, UNCHANGED)

`cargo test --locked --workspace` — every crate green, **0 failures**: `brokkr-crypto` (6),
`brokkr-intent` (8 + 6), `brokkr-gate` (10), `brokkr-genome` (13 + 1 doctest). No downstream test
modified; none broke.

## Doctest count — before and after

| | Before (committed HEAD) | After |
|---|---|---|
| `brokkr-core` running doctests | **12** | **12** |

Unchanged, measured by stashing the change and running `--doc` against HEAD (12), then again with the
change (12). Expected: this revision adds a type and required fields, no doctest.

## The BarrierVerdict compile-fail doctest (still passes, unchanged)

`BarrierVerdict` is not changed by this revision — only `PersonalDataTag` is added and the `personal`
field placed on `BoundaryFlow`/`BoundaryCustodyRecord`. The `compile_fail,E0599` doctest
(`Deny.into_allow()`) still passes (I-2). Verified by running.

## Why nothing downstream (or in brokkr-core's own tests) broke — investigated per instruction

A workspace grep showed **no construction of `BoundaryFlow` or `BoundaryCustodyRecord` anywhere**
outside `barrier.rs`: `reasoner.rs` uses `BarrierVerdict`/`Destination` (unchanged); and
`brokkr-core/tests/negative_invariants.rs` constructs neither `BoundaryFlow` nor
`BoundaryCustodyRecord`. So adding the **required** `personal` field to both broke no construction
site — confirmed by building (`negative_invariants.rs` compiles, its 12 tests pass unchanged). No
brokkr-core test needed editing.

## Discipline checks

- Clippy (§6 deny-list), `--all-targets`: **0** warnings/errors.
- `cargo build --locked -p brokkr-core`: clean. `cargo fmt`: clean.
- **No external dependency** — `[dependencies]` empty (I-5).
- **No new module import beyond in-crate** — `PersonalDataTag` composes
  `crate::personal_data::{Purpose, RetentionPeriod}`, already committed in core.
- `#![no_std]` preserved: `RetentionPeriod` holds a `core::time::Duration`, not a `std` type.

## What was NOT done

- **No evaluation logic** — the corrected egress conditions (1 and 9) and the Privileged-Context
  ingress rule are Phase 6.
- **`BarrierVerdict`, `Destination`, `DestinationClass`, `ContextClass` unchanged.**
- **`Purpose` / `RetentionPeriod` not re-exported again** — already re-exported at `lib.rs`
  (`pub use personal_data::{Purpose, RetentionPeriod};`); only `PersonalDataTag` was added, on the
  barrier re-export line.

— End of functionality test record FUNC-2026-07-30-R1.
