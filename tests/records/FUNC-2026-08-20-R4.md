# Test Record — Phase 11.5 (brokkr-cli / Real Subsystem Integration)

**Record ID:** FUNC-2026-08-20-R4
**Date:** 2026-08-20
**Crate:** `brokkr-cli` (integration-test addition)
**Author:** Claude Code (builder)

## Commands and results

| Command | Result |
|---|---|
| `cargo build --workspace` | green |
| `cargo test -p brokkr-cli --test real_integration` | **1 passed, 0 failed** — 19.4s (real PQC keygen + sign + verify) |
| `cargo test --locked --workspace` | **204 passed, 0 failed** (203 prior + 1 new; real crypto) |
| `cargo clippy -p brokkr-cli --all-targets` | no warnings |
| `cargo clippy --workspace --all-targets` | no warnings |
| `cargo fmt --all -- --check` | clean |

## The test — `real_governed_hop_writes_a_file_with_real_pqc`

One governed hop through the full cycle with **real** subsystems except BIFRÖST/MÍMIR (controlled doubles). Real components:

- **SINDRI** — real `Sindri` verifying a real dual-family (ML-DSA-65 + SLH-DSA-SHAKE-192s) Root-Intent signature via `RegistryResolver` + `Skuld::verify_chain_public`, enforcing all four OQGF-M-11 conjuncts.
- **SAGA** — real `Saga`, dual-signing every appended record.
- **HEIMDALL** — real `Heimdall` (empty detector set), observe loop over the real reconciliation feed.
- **HÚÐ** — real `Huth` (`InMemoryCeiling`/`InMemoryAcceptances`), allowing the Public local crossing.
- **tool** — real `FsWriteTool` writing an actual file.
- **SKULD** — real `Skuld::sign_root`.

## Assertions (all pass)

1. `HopResult::Executed` naming the written path.
2. **A real file** exists at `temp_dir()/brokkr-test-<pid>.txt` with content `hello from BROKKR` (`read_to_string` equality). Removed on drop (`TempFile` guard, even on panic).
3. SAGA recorded a **`Proposal`** event.
4. SAGA recorded a **`Granted`** `Authorization` — the grant came from real SINDRI verifying the real signed chain (a wrong key / expired chain / undeclared tool / missing capability would have produced `Anergy` and no file).
5. **Every** SAGA record carries ≥1 real dual-family `GenerationSignature` (OQGF-R-1 exercised end-to-end).
6. Real HEIMDALL received `Observation::Hop { executed: Some(..) }` (via the `RecordingHeimdall` tap around the real `observe`).

## Test infrastructure (in the test file — no subsystem source changed, no new crate)

- `DeclaredGenome` — backs **both** SINDRI's `GenomeResolver` and the orchestrator's `GenomeCheck` port from one declared `HashMap` (write_file → {cap: write, Privileged}); fail-closed.
- `FsWriteTool` — `ToolExecutor` writing to the action's `detail` path (no committed filesystem tool exists).
- `EmptyCorpus` — trivial `SelfSetCorpus` (not exercised: no detectors, orchestrator calls `observe` not `screen`).
- `SagaSink` / `RecordingHeimdall` / `RecordingSignals` — adapters to the orchestrator's `AuditSink` / `Sentinel` / `SignalRouter` ports.
- `ClearAll` (BIFRÖST) / `FixedProposer` (MÍMIR) — controlled doubles.

## Structural / hygiene

- **Held-clock grep** over `brokkr-cli/src` — unchanged: one match, `Orchestrator::now()` (the §4 reader). The test file holds no clock (`now = Timestamp(1000)` passed to `execute_hop`; I-13 intact).
- No `unsafe` in `brokkr-cli`.
- `DualKeyPair`/`Saga`/`Heimdall`/`Huth` are `Send + Sync` (FFI key types carry `unsafe impl Send/Sync`), so they fit the orchestrator's `Box<dyn Trait: Send + Sync>` ports.
- No new external dependency; no subsystem source change; `main.rs` unchanged.

## Honest limitations (see the phase report §4)

BIFRÖST/MÍMIR are doubles; HÚÐ takes its Public short-circuit (custody-record path not exercised by this local write); HEIMDALL runs with no detectors (feed real, detection content not exercised); the genome is declared test data, not a signed REGIN genome; the attestation `signatures` field is real but unverified by SINDRI (Rev 1.3 §6.4, consistent with M-1 PARTIAL).

## Environment

- Rust on PATH; real wolfSSL crypto backend (rpath via `.cargo/config.toml`). The 19.4s runtime is dominated by four real `DualKeyPair::generate()` calls plus real dual-family signing/verification.
