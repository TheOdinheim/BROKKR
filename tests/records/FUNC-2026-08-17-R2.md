# Functionality Test Record — FUNC-2026-08-17-R2

**Phase:** 8.5 (`brokkr-bifrost` / BIFRÖST — the guarded crossing)
**Date:** 17 August 2026
**Command:** `cargo test --offline --locked -p brokkr-bifrost`; `--workspace`
**Backend:** real wolfSSL v5.9.2 (non-FIPS) through `brokkr-crypto` for the BCR signatures HÚÐ verifies, and
the **real HÚÐ gate** (no mock barrier — "one gate, one logic"). Every BCR is signed with a real
`DualKeyPair` over the real canonical encoding (`brokkr_barrier::canonical::bcr_signed_content`); HÚÐ
verifies it under exported public bytes.

## Result: 7 tests + 1 doctest pass; workspace 149 passed, 0 failed

```
     Running tests/bifrost.rs
test test_oqgf_m_5_classical_group_collapses_to_public ... ok
test test_pqc_group_permits_declared_ceiling ... ok
test test_effective_is_min_not_endpoint ... ok
test test_above_public_without_bcr_is_denied ... ok
test test_public_personal_context_does_not_short_circuit ... ok
test test_crossing_record_carries_negotiated_group ... ok
test test_no_payload_inspection ... ok
test result: ok. 7 passed; 0 failed

   Doc-tests brokkr_bifrost
test result: ok. 1 passed; 0 failed   (the compile_fail,E0451 no-minting doctest)

     workspace: 149 passed; 0 failed
```

## What the tests prove

| Test | Requirement | Establishes |
|---|---|---|
| **`test_oqgf_m_5_classical_group_collapses_to_public`** | **M-5 (central)** | endpoint declared Secret, handshake on a **classical** group, Internal context with a valid signed BCR → **Deny (ChannelStrengthCollapse)**. The channel collapses the effective level to Public regardless of the endpoint's ceiling |
| `test_pqc_group_permits_declared_ceiling` | M-5 | a Secret context over a PQC-hybrid group at a Secret endpoint → **Allow**, and `bifrost.clear(...)` mints a `ClearedContext` via core's provided (sole-minter) `clear` |
| `test_effective_is_min_not_endpoint` | M-5 | a **Public**-ceiling endpoint over a strong PQC channel still caps at Public — the channel does **not** raise the endpoint; Internal denied |
| `test_above_public_without_bcr_is_denied` | I-9/I-10 | Internal with no BCR → **Deny (MissingCustodyRecord)** — condition 2 reaches the reasoner crossing |
| `test_public_personal_context_does_not_short_circuit` | P-11.1 | a **Public + personal** context with no BCR does **not** short-circuit to Allow — denied (`MissingCustodyRecord`); the Rev 1.7 correction at this boundary |
| `test_crossing_record_carries_negotiated_group` | I-13 (material) | the `CrossingRecord` names what was **agreed**: group, derived `ChannelStrength`, effective ceiling (`min` = Public here), classification, verdict; a non-reasoner destination yields no record |
| `test_no_payload_inspection` | I-12 | two contexts identical except `payload` → **identical verdict**; the decision does not depend on content |
| compile_fail doctest | I-12 (structural) | `ClearedContext { inner: … }` does not compile (`E0451`, private field) — BIFRÖST cannot mint one |

## Discipline checks

- Clippy (§6 deny-list), `--all-targets`: **0**. `cargo build --locked`: clean. `cargo fmt --check`: clean.
- **No `unsafe`:** `#![forbid(unsafe_code)]`; no FFI of its own.
- **I-5:** `cargo tree -e no-dev -p brokkr-bifrost` = `{brokkr-core, brokkr-barrier, brokkr-crypto (transitive
  via brokkr-barrier)}` — no `brokkr-reasoner`/`brokkr-tools`/`brokkr-cli`. Matches CLAUDE.md I-5 / ARCH §9.
- **I-6:** no model calls. **I-12:** no `ClearedContext` construction in `src` (grep); the doctest proves it.
- **Structural greps (in the report):** no code in `brokkr-bifrost/src` reads `Context::payload`; no code
  constructs `ClearedContext`.
- **Workspace:** `cargo test --locked --workspace` → **149 passed, 0 failed**.
- No mocks for the gate — the tests use the **real** `Huth` with real signed BCRs. No `unwrap`/`expect` in
  `brokkr-bifrost` production paths (the `Mutex` guard is recovered; tests may `unwrap`).

## What was NOT tested, and why (§7)

- **The TLS handshake / real mTLS / negotiated-group readback** — out of scope (§6.10); the negotiated group
  is an input carried on `Destination::Reasoner`. No sockets.
- **Recording to SAGA** — Phase 11; BIFRÖST produces the record material as a value (I-13 PARTIAL).
- **A real model call** — Phase 10; BIFRÖST clears a context, it does not use one (I-6).

— End of functionality test record FUNC-2026-08-17-R2.
