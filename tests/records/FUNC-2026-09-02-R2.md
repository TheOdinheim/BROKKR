# Test Record — A-1 Closure (DAP-directed) — FUNC-2026-09-02-R2

**Record ID:** FUNC-2026-09-02-R2
**Date:** 2026-09-02
**Type:** Construction (DAP-directed) — closes audit finding A-1 from `SECURITY-REVIEW-2026-09-02-R1`.
**Author:** Claude Code (builder)

## Fix

`brokkr-crypto/src/ffi.rs` — `MlKem768::{encapsulate, ciphertext_size, shared_secret_size}` now
guard `if self.destroyed { return Err(-99) }` **symmetric with `decapsulate`**, so after `destroy()`
they return an error instead of passing the post-destroy null `ptr` across the FFI. The three
`SAFETY:` comments are corrected to state the guard ("guarded above, so self.ptr is a live key").
Error (`-99`), not panic, matching `decapsulate`.

## Result

- **Workspace GREEN — 409 passed, 0 failed** (408 + 1 new regression).
- `cargo clippy -p brokkr-crypto --all-targets` + `cargo fmt --all -- --check` — clean.
- Regression: `brokkr-crypto/tests/roundtrip.rs::test_a1_mlkem768_methods_error_after_destroy` —
  after `destroy()`, each of the three methods (and `decapsulate`, the reference guard) returns
  `Err`; `destroy()` is idempotent. Fails against the pre-fix unguarded methods.
- No normative verdict moves — the fix reaffirms the §6 no-`unsafe`-UB posture (a null pointer no
  longer reaches wolfCrypt through the safe API); it does not change an OQGF conformance verdict.

## Disposition

A-1 **closed**. A-2 (INFO, `as Word32` truncation, unreachable) remains accepted, no action.
