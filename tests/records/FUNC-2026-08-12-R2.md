# Functionality Test Record — FUNC-2026-08-12-R2

**Phase:** 1 (revision) — `brokkr-core` Rev 1.12 resolution signed content + tolerance signature error
**Date:** 12 August 2026
**Commands:** `cargo test --offline --locked -p brokkr-core`; standalone `rustc` on the doctest snippets;
`cargo test --offline --locked -p brokkr-sentinel --no-run` (to capture the intended breakage)

## Result: brokkr-core 15 + 3 + 6 + 12 doctests pass; the workspace does not build (intended)

```
=== brokkr-core ===
     Running tests/negative_invariants.rs
test result: ok. 15 passed; 0 failed
     Running tests/resolution_encoding.rs        (NEW)
test result: ok. 3 passed; 0 failed
     Running tests/risk_shape.rs
test result: ok. 6 passed; 0 failed
   Doc-tests brokkr_core
test result: ok. 12 passed; 0 failed              (baseline 12 → 12, unchanged)
```

## Doctest error codes — verified by standalone compile (not assumed)

`compile_fail` passes on **any** compile error, so the doctest run alone does not prove the error *code*.
Each snippet was compiled standalone against the built `brokkr-core` rlib:

| Snippet | Emitted code | Meaning |
|---|---|---|
| `ResolutionDecision::new(any(), any(), None, any(), any(), any(), any())` (7 args, `None` in `dap` slot) | **`error[E0308]`** (only) | expected `Dap`, found `Option<_>` — I-8 second half still proven; **not** E0061 |
| `EscalationType::new(any(), any(), None, any(), any(), any())` (unchanged) | **`error[E0308]`** | I-8 first half unaffected |

The trap (wrong arity → E0061 → `compile_fail` still green while proving nothing) is avoided; the
annotations match the emitted codes.

## New encoding tests (`tests/resolution_encoding.rs`) — byte composition, no crypto backend

| Test | Establishes |
|---|---|
| `test_resolution_encoding_is_deterministic` | the same decision produces identical bytes (issuer and verifier agree) |
| `test_resolution_encoding_is_unambiguous` | `("ab","c")` and `("a","bc")` produce **distinct** bytes — the length-prefix collision case |
| **`test_resolution_signature_is_excluded`** | two decisions differing **only** in `signature` produce **identical** signed content — the property a refactor is most likely to break silently |

## The intended workspace breakage (Task 6) — precise scope

```
=== brokkr-sentinel test compile (constructs ResolutionDecision) ===
error[E0061]: this function takes 7 arguments but 5 arguments were supplied
   --> brokkr-sentinel/tests/sentinel.rs:197:17
   --> brokkr-core/src/resolution.rs:115:12   (ResolutionDecision::new)
```

- **brokkr-core:** PASS (lib + all tests + doctests).
- **brokkr-audit:** BUILDS (reads `ResolutionDecision` fields, never constructs one).
- **brokkr-sentinel lib:** BUILDS (reads fields; only the **test** constructs a decision).
- **brokkr-sentinel test:** FAILS — the single E0061 above, at the `signed_decision` helper.
- **Workspace:** does not build — **known, intended**; fixed by the follow-on `brokkr-sentinel` Rev 1.12
  revision (helper 5→7 args; adopt core's `resolution_signed_content`; enforce freshness + `SignatureInvalid`).

## Discipline checks

- Clippy (§6 deny-list), `--all-targets`: **0**. `cargo build --locked`: clean. `cargo fmt --check`: clean.
- `#![no_std]` + `#![forbid(unsafe_code)]` preserved; **no new dependency** (`cargo tree -e no-dev` =
  `brokkr-core` alone — I-5). The encoding is byte composition returning `Vec<u8>`; no hasher is reached for.
- The sealed `ToleranceController::grant` is **unchanged** (only a variant was added to `ToleranceError`).

## What was NOT tested here, and why (§7)

- **Signature verification / expiry refusal / nonce-replay refusal / returning `SignatureInvalid`** — these
  are the `brokkr-sentinel` Rev 1.12 revision's enforcement; this revision places the type surface only.
- **Real crypto over the encoding** — the encoding is pure byte composition; a signer hashes-and-signs these
  bytes downstream (sentinel), where the real-crypto round-trip already lives.

— End of functionality test record FUNC-2026-08-12-R2.
