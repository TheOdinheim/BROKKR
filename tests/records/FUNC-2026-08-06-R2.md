# Functionality Test Record — FUNC-2026-08-06-R2

**Phase:** 7 (`brokkr-audit` / SAGA — the Audit Spine)
**Date:** 6 August 2026
**Command:** `cargo test --offline --locked -p brokkr-audit`
**Backend:** real wolfSSL v5.9.2 (non-FIPS) through `brokkr-crypto` — no mocks. Every record is signed with
a real `DualKeyPair::sign_dual` (ML-DSA-65 + SLH-DSA-SHAKE-192s) over the real canonical encoding; every
verification imports raw public bytes (`DualPublicKey::from_public_bytes`). Erasure uses the real
`SubjectKey` ML-KEM crypto-shred primitive.

## Result: 13 tests passed, 0 failed (0 doctests)

```
     Running tests/audit.rs
test test_append_and_verify_multi_record_chain ... ok
test test_public_nonpersonal_record_round_trips ... ok
test test_export_verifies_under_exporter_key ... ok
test test_oqgf_a_6_resigning_does_not_break_the_chain ... ok
test test_oqgf_a_6_resigning_preserves_original_signature ... ok
test test_oqgf_p_11_5_erasure_preserves_chain ... ok
test test_oqgf_p_11_7_resigned_erased_record_stays_irrecoverable ... ok
test test_chain_break_emits_a_6_1_signal ... ok
test test_correction_does_not_alter_corrected_record ... ok
test test_oqgf_a_3_absent_authority_records_unavailable ... ok
test test_oqgf_a_3_present_authority_records_token ... ok
test test_audit_domain_separated_from_genome_and_barrier ... ok
test test_acceptance_register_assigns_id ... ok
test result: ok. 13 passed; 0 failed; finished in 25.90s
```

## What the tests prove

| Test | Requirement | Establishes |
|---|---|---|
| `test_append_and_verify_multi_record_chain` | A-5, chain | a multi-record chain appends (seq increments, first links to genesis) and verifies |
| `test_public_nonpersonal_record_round_trips` | I-13 | a barrier-crossing record (Public/non-personal Egress, Allow) encodes and verifies |
| `test_export_verifies_under_exporter_key` | A-7 | a signed export verifies under the exporter's key and **fails under a foreign key** |
| **`test_oqgf_a_6_resigning_does_not_break_the_chain`** | **A-6 (the Rev 1.10 correction, executable)** | re-sign an early record, then the whole chain still verifies and the following records' `prev` are **byte-identical** — appending a signature disturbs no link, because the chain links over signed content only |
| `test_oqgf_a_6_resigning_preserves_original_signature` | A-6 | after re-signing, the original `GenerationSignature` (gen 1) is still present, first, and verifies; gen 2 is appended (accumulate, never replace) |
| `test_oqgf_p_11_5_erasure_preserves_chain` | P-11.5 | after crypto-shred erasure + tombstone, the chain verifies and the **erased record is byte-for-byte unchanged**; a tombstone names its seq |
| **`test_oqgf_p_11_7_resigned_erased_record_stays_irrecoverable`** | **P-11.7 (most important)** | shred the subject key, erase, **re-sign the erased record**, then the data key **still** fails to unwrap and the chain still verifies — re-signing cannot resurrect erased data (structural: SAGA holds no subject key, no decrypt path) |
| `test_chain_break_emits_a_6_1_signal` | A.6.1 | a tampered record fails verification and **emits a constructed, signed `Signal`** — `source == OrganId::Audit`, `class == ThreatDetected`, `severity == Critical` |
| `test_correction_does_not_alter_corrected_record` | §6.9 corrections | a `Correction` is an appended event; the corrected entry's bytes and digest are unchanged |
| `test_oqgf_a_3_absent_authority_records_unavailable` | A-3 | with no TSA, the record appends with `Timestamping::Unavailable { reason }` (recorded, not omitted, not refused) |
| `test_oqgf_a_3_present_authority_records_token` | A-3 | with an injected authority (test double), the record carries `Timestamping::Token` |
| **`test_audit_domain_separated_from_genome_and_barrier`** | Task 1 (load-bearing) | the audit signed content begins with `brokkr-audit:record:v1` and **none** of the `brokkr-genome:*` / `brokkr-barrier:*` tags; a signature over audit bytes does not verify over barrier-tagged bytes, and vice versa |
| `test_acceptance_register_assigns_id` | P-9.5 | recording a `RiskAcceptance` yields a SAGA-assigned `RiskAcceptanceId` that resolves back to the same record; a second yields a distinct id; the inventory reports both |

## Discipline checks

- Clippy (§6 deny-list), `--all-targets`: **0** warnings/errors. `cargo build --locked`: clean.
  `cargo fmt -- --check`: clean.
- **No `unsafe`:** `#![forbid(unsafe_code)]`; no FFI of its own (the FFI is `brokkr-crypto`'s). `brokkr-audit/src`
  contains no `SubjectKey` (grep) — the structural basis for P-11.7.
- **I-5:** `cargo tree -e no-dev -p brokkr-audit` = `{brokkr-core, brokkr-crypto}` — no sibling crate, not
  even as a dev-dependency (domain separation is tested by asserting tag distinctness at the byte level,
  without importing `brokkr-genome`/`brokkr-barrier`).
- **I-6:** no model calls.
- **Workspace unchanged:** `cargo test --locked --workspace` green across all crates, **0 failures**
  (barrier 21, gate 10, genome 13, intent 8+6, crypto 5+6, core 15+6, plus the existing compile-fail
  doctests).
- No mocks. No `unwrap`/`expect` in `brokkr-audit` production paths (the `Mutex` guard is recovered via
  `unwrap_or_else(|p| p.into_inner())`; tests may use `unwrap`).

## What was NOT tested, and why (§7)

- **A production timestamp authority** — out of scope; only the seam and a test-double `EchoTsa` exist.
  A-3's timestamp half is PARTIAL by design.
- **A real MÍMIR proposal** — MÍMIR is Phase 10; the A-1 `Proposal` variant is exercised only by a
  test-constructed proposal.
- **The later-emitting subsystems** (tolerance grants, BIFRÖST crossings, detector activations, resolutions)
  — their `AuditEvent` variants are placed and compile; their emitters are Phases 8–10.
- **The full two-register-vs-tolerance distinctness demonstration** (P-9.4) — tolerance grants are Phase 8;
  distinctness is shown structurally now (unrelated types).
- **Disk/network persistence** — the store is in-memory (§6.9); persistence is a future phase.

— End of functionality test record FUNC-2026-08-06-R2.
