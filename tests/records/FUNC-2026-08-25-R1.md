# Test Record — Phase 15-FIX (Harden Grey-Hat Findings)

**Record ID:** FUNC-2026-08-25-R1
**Date:** 2026-08-25
**Crate:** `brokkr-cli`
**Author:** Claude Code (builder)
**Report:** `reports/PHASE-15FIX-2026-08-25-R1.md` · **Conformance:** `conformance/CONF-2026-08-25-P15FIX-R1.md`

## Commands and results

| Command | Result |
|---|---|
| `cargo build --workspace --locked` | green |
| `cargo test --locked --workspace` | **368 passed, 0 failed** (367 baseline + 1 new `fix_f22`) |
| `cargo test -p brokkr-cli --test redteam_15fix` | 1 passed (F-22) |
| `cargo test -p brokkr-cli --test redteam_14fix` | 5 passed (incl. the rewritten `fix_f13` — F-29 mechanism) |
| `cargo test -p brokkr-cli --test redteam_15c` | 7 passed (incl. the updated F-29 test) |
| `cargo test -p brokkr-cli --test redteam_15a --test redteam_15b` | 10 + 7 passed (updated reason-oracle assertions) |
| `cargo clippy --workspace --all-targets --locked` | 0 warnings / 0 errors |
| `cargo fmt --all -- --check` | clean |

## The two fixes (each with a regression that fails without it)

- **F-22 / F-26** — `pub const UNIFORM_DENIAL_REASON = "action denied"`; under production guards both the genome-refusal and SINDRI-anergy paths in `execute_hop` return it, so `(stage, reason)` is identical for undeclared and declared-but-denied tools. SAGA still records the specific `AnergyReason` + attempted tool. **Regression:** `redteam_15fix::fix_f22_uniform_reason_string_under_production`.
- **F-29** — `check_guards` evicts only expired entries; a ledger full of valid nonces **rejects** the new chain ("nonce ledger full") rather than evicting a fresh one. **Regression:** `redteam_15c::g2_2_4_nonce_flood_no_longer_opens_a_replay_window` (fails without the fix — the "ledger full" path does not exist pre-fix and the flood evicted the target).

## Tests updated (behavior changed by a fix; none deleted)

| Test | Fix | Change |
|---|---|---|
| 15A `g2_2_6_...defeats_uniform_stage` → `...is_uniform_under_production` | F-22 | reasons now identical ("action denied") |
| 15B `g2_2_1_...distinguishes...` → `...is_uniform...` | F-22 | reasons now identical |
| 15B `g2_2_2_...fully_enumerable...` → `...no_longer_enumerable...` | F-26 | every probe returns the uniform reason |
| 15B `g2_2_3_...signature_table` → `..._permissive_only` | F-22 | table is now permissive-only (production uniforms it) |
| 15B `combined_3_1_...` | F-22 | enumeration closed under production; survives only under permissive misconfig |
| 15C `g2_2_4_...opens_a_replay_window` → `...no_longer_opens...` | F-29 | replay stays denied; overflow rejected |
| 14-FIX `fix_f13_nonce_ledger_bounded` | F-29 | mechanism changed evict→reject; now asserts the reject-on-full bound + no fresh eviction |

## Accepted with no code change (documented as trust boundaries)

F-23 (orchestrator TCB), F-24 (unsigned genome wiring), F-25 (spine stricter than the doc's residual — an architecture-doc update, not code), F-27 (tool output ungoverned), F-28 (tool reads unrestricted), F-30 (SAGA end-truncation needs an external witness), F-31 (content-blind auditing by design). Report §0.

## Invariant / interaction notes

- **F-29 superseded the F-13 eviction** with reject-on-full; both bound the ledger to `MAX_NONCE_ENTRIES`, so the memory property F-13 established holds by a stricter mechanism. The 14-FIX `fix_f13` regression was updated accordingly (the workspace run caught it — a single failure, corrected).
- **I-1/I-12/I-13 untouched** — both fixes are at the orchestrator/driver face; no held clock added, nothing minted.
- **The trusted audit is unchanged by F-22** — the returned reason was never an audit input.

## Environment

Rust on PATH; real wolfSSL v5.9.2 (non-FIPS) via `.cargo/config.toml` rpath. Hermetic — no live model, no network. redteam_15fix ≈ 8 s; redteam_15c (nonce flood) ≈ 43 s; workspace ≈ 7 min.
