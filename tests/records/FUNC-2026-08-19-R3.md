# Test Record — brokkr-core Revision R12 (AdaptError corpus-validity variants)

**Record ID:** FUNC-2026-08-19-R3
**Date:** 2026-08-19
**Crate:** `brokkr-core` (types only; disposes GAP-2026-08-19-001)
**Author:** Claude Code (builder)

## Commands and results

| Command | Result |
|---|---|
| `cargo build --locked -p brokkr-core` | Finished, clean |
| `cargo test --locked -p brokkr-core` | lib 0 + integration **17** + encoding 3 + risk 6 + doc 14 — **all pass, 0 failed** |
| `cargo test --locked -p brokkr-core --doc` (HEAD baseline via `git stash`) | 14 pass |
| `cargo test --locked -p brokkr-core --doc` (working) | 14 pass |
| `cargo build --locked --workspace` | Finished — **green** |
| `cargo clippy --locked --workspace --all-targets` | Finished, no warnings |
| `cargo fmt --all -- --check` | clean |

## Counts

- Doctests: **14 → 14** (the new variant docs contain no code fences).
- Integration tests (`negative_invariants.rs`): **16 → 17** (+ the corpus-validity message test).

## New test

`test_oqgf_p_6_2_corpus_validity_errors_name_the_evidence_not_the_candidate` — the executable form
of GAP-2026-08-19-001's principle:
- `AdaptError::SubstitutedCorpus.to_string()` contains "corpus" and "digest";
- `AdaptError::StaleCorpus.to_string()` contains "corpus" and "current version";
- **neither** contains "candidate" (a message reworded to imply a candidate defect fails here);
- `SubstitutedCorpus != StaleCorpus`, and both messages differ from `Overfit`/`CoverageRegression`.

## Workspace / hygiene

- No exhaustive match on `AdaptError` anywhere outside `adapt.rs` (only two `Err(AdaptError::…)`
  comparisons in the core test double), so the additive variants break nothing — workspace green.
- One **pre-existing** rustdoc warning at `adapt.rs:197` (`ScreenPass` link from the R11 `activate`
  doc, committed `6219fa1`) — not introduced by this revision, left untouched (reported in the
  revision report).
- `#![no_std]` + `#![forbid(unsafe_code)]` preserved; no new external dependency.

## Environment

- Rust on PATH; real crypto backend (wolfSSL rpath via `.cargo/config.toml`) — not exercised by
  this types-only change.
