# Test Record — Phase 14C (White-box Red Team, Low Noise — final)

**Record ID:** FUNC-2026-08-23-R4
**Date:** 2026-08-23
**Crate:** `brokkr-cli` (`tests/redteam_14c.rs`) + code-reading verifications across the workspace
**Author:** Claude Code (builder / red team)
**Full analysis:** `reports/REDTEAM-14C-2026-08-23-R1.md`

## Commands and results

| Command | Result |
|---|---|
| `cargo test --locked --workspace` | **334 passed, 0 failed** (327 baseline + 7 redteam_14c) |
| `cargo test -p brokkr-cli --test redteam_14c -- --nocapture` | **7 passed** (panic safety, timing, cross-subsystem) |
| `cargo clippy --workspace --all-targets` | 0 warnings / 0 errors |
| `cargo fmt --all -- --check` | clean |

## Runtime attacks (redteam_14c.rs)

- **3.1** tool panics on first call → caught (no abort); a second hop on the SAME orchestrator executes → **not poisoned** (no lock held across `tool.execute`).
- **3.4** concurrent panic + normal (two orchestrators, `thread::scope`) → the normal hop completes; panic is per-thread.
- **4.1** Signal-1 denial **≈1.6 µs** vs Signal-2 (crypto) denial **≈4.19 ms** — ~2600× (timing side channel, F-21).
- **4.2** replay-guard denial **≈10 µs** vs SINDRI crypto grant **≈4.31 ms** — ~415× (F-21).
- **5.1** tool `Err` → `HopResult::Error`, and **no `Observation::Hop`** reaches HEIMDALL (F-20 reconciliation gap).
- **5.2** SINDRI denies → SAGA has **both** the Proposal and the anergic Authorization; tool never ran.
- **5.3** BIFRÖST deny → SAGA `BarrierCrossing` recorded, **reasoner call count 0** (I-12).

## Code-reading verifications (in the report)

- **1.1** `TlsClient` constructed only after `wolfSSL_connect` succeeds → `Drop` never sees null.
- **1.2** `write_all` cast is `.min(c_int::MAX)`-bounded → no truncation.
- **1.4** signature buffers **dynamically sized** via `sig_size()`; pubkey constants correct (1952, 48) → no overflow.
- **1.5** every FFI key type impls `Drop` → no partial-keygen leak.
- **2.1–2.4** all arithmetic `saturating`/`.min()`-bounded or `isize::MAX`-bounded.
- **3.2** both SAGA `state()`/`signer()` recover from poison (`into_inner()`).
- **3.3** no lock-order inversion: `append` is the only dual-hold (state→signer), `export` releases state first; guard_state released before SAGA.

## Findings (documented, not fixed)

**F-18** (INFO) SLH-DSA signer lacks the ML-DSA `sig_size()==const` cross-check (defense-in-depth asymmetry; not a memory bug — buffer is library-sized). **F-19** (LOW) `read_until_close` unbounded allocation (DoS via infinite stream; bounded by the mTLS-authenticated gateway). **F-20** (LOW) tool execution failure emits no HEIMDALL observation (reconciliation gap). **F-21** (INFO) evaluation-time side channel leaks the failing stage (inherent variable-cost work; observer is the trusted driver, not the model). Each carries an optional hardening.

## Test-infrastructure notes

- Real dual-family signing done once, serially in `fx()` (13A F-4). Two chains: `legit` (p1-signed) and `forged` (principal p1, attacker-signed → slow Signal-2 crypto denial for the 4.1 timing test).
- `FlipPanicTool` panics on its first call then succeeds (proves recovery); the caught-panic stderr messages during the run are **expected** (`catch_unwind` prints them), not failures.
- Timing assertions are qualitative (`sig > id`, `replay < grant`) with orders-of-magnitude margin (~2600×, ~415×) — robust against noise.

## Environment

Rust on PATH; real wolfSSL v5.9.2 (non-FIPS) via `.cargo/config.toml` rpath. Hermetic — no live model, no network. redteam_14c ≈10.5 s; workspace ≈428 s.
