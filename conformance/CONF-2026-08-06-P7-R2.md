# Conformance Check CONF-2026-08-06-P7-R2

**Phase:** 7 (`brokkr-audit` / SAGA — the Audit Spine) — **BUILD check**
**Subject:** built crate `brokkr-audit`, under BROKKR-ARCH §6.9 Rev 1.10
**Date:** 6 August 2026
**Method (CLAUDE.md §5.3):** requirements enumerated **from the corpus** (OQGF-1.0 §A.5; AMD-005/006/007/
008/009), quoted, never from BROKKR-ARCH §14. Each is checked against the built crate — the file, type,
function, and the test that proves it. **A verdict may move backward** (§5.3): four requirements BLOCKED in
the R1 pre-build audit (`CONF-2026-08-06-P7-R1`) move to satisfied here now that Rev 1.10 disposed
GAP-2026-08-06-001.

**What was checked, and against what (§7):** the eleven Organ-5 / cross-organ requirements the prompt
scoped, against the built `brokkr-audit` (`src/{event,canonical,saga}.rs`) and its 13 real-crypto tests
(`tests/audit.rs`). The finding is **0 absent, 1 partial (A-3), 2 n.a. (A-2, A-4)**; A-7's 72h is
operational; A-1's proposal variant is placed and exercised only by a test-constructed proposal (MÍMIR is
Phase 10).

---

## 1. OQGF-1.0 §A.5 — Organ 5 (Memory)

| Req | Text (quoted, abridged) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **A-1** | *"For every regulated AI/ML decision the system SHALL record: the model identifier and version, the AIBOM digest, the input (or a privacy-preserving derivative thereof), the output, the explanation artifact, the timestamp, and the DAP."* | `event::ProposalRecord { model: ModelIdentity, aibom_digest: Digest, input: RecordedInput, output, explanation }` + the header's `at`/`dap` = A-1's seven fields. Scoped to the one AI/ML decision (a MÍMIR proposal); other events carry the header + their own payload (no `ModelIdentity` on a gate verdict). | Encoded and round-tripped via `record_signed_content`; exercised by a **test-constructed** proposal (`shredded_proposal` in the erasure tests). No real MÍMIR — Phase 10. | **satisfied (recording surface complete; unexercised by a real proposal).** Backward: BLOCKED in R1 → satisfied. |
| A-2 | quantum-computation records | — | — | **n.a.** (no quantum workload — §1.4) |
| **A-3** | *"…signed under at least two PQC families **and timestamped via an RFC 3161-compliant authority that itself supports PQC signing**."* | Dual-family by construction (`DualKeyPair::sign_dual` → ML-DSA-65 + SLH-DSA-SHAKE-192s). Timestamp half is the injected `TimestampAuthority` **seam**; absent → `Timestamping::Unavailable { reason }`, present (test double) → `Token`. **No production authority.** | `test_oqgf_a_3_absent_authority_records_unavailable`, `test_oqgf_a_3_present_authority_records_token`; every record carries ≥1 dual-family signature (verified by `verify_chain`). | **PARTIAL** — signing half met; timestamp half a declared seam. **Not upgraded** (A-3 binds at Enhanced). |
| A-4 | quantum explanation artifacts | — | — | **n.a.** (§1.4) |
| **A-5** | *"…a named DAP … a natural person, not an entity."* | `AuditRecord.dap: Dap` required on every record; `Dap { name, id }` is a natural person by type. | Present on all records; `verify_chain` covers it in the signed content. | **satisfied** |
| **A-6** | *"…re-signed under the prevailing cryptographic generation … **preserving the original signatures and chain**."* | `Saga::resign` **appends** a `GenerationSignature`, never replaces/removes; never alters `seq/prev/at/dap/event`. Because the chain links over signed content only, downstream `prev` values are unchanged. | `test_oqgf_a_6_resigning_preserves_original_signature` (original present + verifies), `test_oqgf_a_6_resigning_does_not_break_the_chain` (whole chain verifies; following `prev` byte-identical). | **satisfied.** Backward: **BLOCKED in R1 → satisfied** (Rev 1.10 disposed the linkage contradiction). |
| **A-7** | *"…read-only, signed export … within 72 hours."* | `Saga::export` → `SignedExport` (records + signature under the exporter's key), verified by `SignedExport::verify`; read-only by construction (a value, no path back to a mutable store). Same interface answers a subject (`subject_report`). | `test_export_verifies_under_exporter_key` (verifies under the right key; a foreign key fails). | **satisfied (capability);** the **72-hour window is operational** (Odin's ops, split like A.6.1). |

## 2. AMD-009 (OQGF-P-11) — personal-data lifecycle at the audit layer

| Req | Text (quoted, abridged) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **P-11.5** | *"Erasure … by destroying the quantum-safe key … SHALL NOT be … deleting the record."* | `Saga::record_erasure` appends a signed `ErasureTombstone { erased, classification, at, dap }`; the erased record is never modified. Key destruction is `SubjectKey::shred` (crypto layer, caller-held). | `test_oqgf_p_11_5_erasure_preserves_chain` — after erasure the chain verifies and the erased record is **byte-for-byte unchanged**; a tombstone naming its seq is present. | **satisfied.** Backward: BLOCKED in R1 → satisfied. |
| **P-11.6** | subject rights via the OQGF-A-7 interface | `Saga::subject_report(&SubjectId)` returns each held datum (crypto-shredded proposal input) with its declared Purpose/Retention and whether it has been erased; erasure via `record_erasure` on the same store. | Exercised structurally by the erasure tests (which build shredded inputs and tombstones); `SubjectDatum` carries Purpose/Retention. | **satisfied** |
| **P-11.7** | *"a re-signed record of erased Personal Data SHALL remain irrecoverable."* | Structural: `Saga` holds no `SubjectKey`; `resign` takes only a `&DualKeyPair` (no decrypt); `record_signed_content` copies ciphertext verbatim. A decrypt path is unrepresentable in the crate. | `test_oqgf_p_11_7_resigned_erased_record_stays_irrecoverable` — shred, erase, **re-sign**, then the data key still fails to unwrap; the chain still verifies. **The most important test in the phase.** | **satisfied.** Backward: BLOCKED in R1 → satisfied. |

## 3. AMD-006 P-9.5, AMD-008 P-10.6 — the two registers

| Req | Text (quoted, abridged) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **P-9.5** | Risk-Acceptance Entries reviewed; the standing inventory reportable on demand | `Saga::record_acceptance` assigns a `RiskAcceptanceId` **on record** (the resolver's `(id, record)` pair, §6.9); `resolve_acceptance`, `acceptance_inventory`; append-only. Distinct **type** from `ToleranceGrant`. | `test_acceptance_register_assigns_id` — id resolves back to the record; a second yields a distinct id; inventory reports both. | **satisfied** (the two-register-vs-tolerance demonstration lands fully when tolerance grants exist, Phase 8). |
| **P-10.6** | the Risk Register reviewed, reportable; closed/superseded entries annotated and retained, never deleted | `impl RiskRegister for Saga` (`record(&self)` via interior mutability; `inventory`); append-only — superseding is a new push, nothing removed. | Type-checked against the committed `brokkr_core::risk::RiskRegister` trait (the crate compiles as an implementor); append-only by construction. | **satisfied** (the append-only Organ-5 home the interim `risks/` markdown register migrates into). |

## 4. AMD-007 I-13, AMD-005 P-8.4

| Req | Text (quoted, abridged) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **I-13** | *"Every Barrier decision … recorded … with the BCR digest or a record of its absence, the classification, the destination or origin, the deciding policy, and … the accountable DAP."* | `AuditEvent::BarrierCrossing { verdict, flow, bcr_digest: Option<Digest>, dap: Option<Dap> }` — the verdict, the full flow (classification + destination/origin), the BCR digest **or its absence**, and the DAP where `AcceptedRisk`. | `test_public_nonpersonal_record_round_trips` exercises the crossing variant; the variant encodes the flow losslessly. | **satisfied (recording surface);** Phase 6 decided, Phase 11 wires. |
| **P-8.4** | de-escalation SHALL NOT erase the incident record nor revert a learned detector | SAGA is **append-only and never deletes** (no delete method anywhere); resolution and detector-activation events persist independently of any later posture change — §6.9 line 905 names SAGA "the source of truth for EIR's incident preservation." | Structural (no deletion path); EIR is Phase 8. | **satisfied (the non-deletion mechanism);** exercised when EIR exists. |

## 5. Invariants

| Inv | Verdict |
|---|---|
| **I-2** | Holds — SAGA records `BarrierVerdict` values, never converts `Deny`→`Allow`. |
| **I-5** | Holds — `cargo tree -e no-dev` = `{brokkr-core, brokkr-crypto}`; no sibling-crate dep (domain separation tested without importing them). |
| **I-6** | Holds — no model call, no network, no FFI of its own; `#![forbid(unsafe_code)]`. |

## 6. Result

**0 absent.** satisfied: A-1 (surface), A-5, A-6, A-7 (capability), P-11.5, P-11.6, P-11.7, P-9.5, P-10.6,
I-13, P-8.4, and I-2/I-5/I-6. **partial: A-3** (timestamp seam unwired — not upgraded). **n.a.: A-2, A-4**
(§1.4). A-7's 72h is operational. **Four verdicts moved backward-then-forward** relative to the R1 pre-build
audit — A-1, A-6, P-11.5, P-11.7 were BLOCKED behind GAP-2026-08-06-001 and are now satisfied under
Rev 1.10; recording that movement, rather than only ever improving, is the §5.3 discipline. This is a build
check with real-crypto proofs, not an assertion: every "satisfied" above names the test or the structural
property that establishes it.

— End of conformance check CONF-2026-08-06-P7-R2.
