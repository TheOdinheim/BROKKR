# Conformance Check CONF-2026-08-14-P1-REV-R8

**Phase:** 1 (revision) — `brokkr-core` `ResolveError` variants, Rev 1.13
**Subject:** the two placed variants (`ResolveError::Expired`, `ResolveError::ReplayedNonce`) in
`brokkr-core/src/resolution.rs`
**Date:** 14 August 2026
**Method (CLAUDE.md §5.3):** requirements enumerated **from the corpus** (AMD-005 OQGF-P-8.2/P-8.5; AMD-001
OQGF-M-14 as it bears on freshness), quoted, never from BROKKR-ARCH §14. This is a **types-only** placement:
the verdict for placed vocabulary is *placed*, **not** *satisfied*, because the enforcement (check expiry,
track nonces, return these variants) is the `brokkr-sentinel` adoption revision. Per §5.3 a verdict may move
backward; none does (additive).

**What was checked, and against what (§7):** the resolution-refusal vocabulary obligation of OQGF-P-8.5 and
its relation to OQGF-M-14 freshness, against the two placed variants and the committed `ResolveError`.
Finding: **0 absent**; refusal vocabulary now complete, enforcement correctly deferred and declared so.

---

## 1. AMD-005 — OQGF-P-8.5 (fail-safe asymmetry + freshness refusals)

> §6.8 (Rev 1.12): *"EIR SHALL refuse a decision whose `now > expiry`, and SHALL refuse a `nonce` it has
> already accepted for that escalation."* — Rev 1.13: *"A refusal SHALL name the reason it actually is."*

| Aspect | Placed here? | Where enforcement lands |
|---|---|---|
| A variant naming an **expired** refusal | **Yes** — `ResolveError::Expired` | this revision |
| A variant naming a **replayed** refusal | **Yes** — `ResolveError::ReplayedNonce` | this revision |
| Two variants (not one `Stale`); neither reuses `NeedsDapConfirmation` | **Yes** — enum-level doc records both rationales | this revision |
| **Check** `now > expiry`; **track** accepted nonces; **return** these variants | **No** | `brokkr-sentinel` (EIR `resolve`) — the adoption revision |

**Verdict: refusal vocabulary complete; P-8.5 NOT claimed satisfied.** Rev 1.12 placed the fields and the
signed-content encoding; Rev 1.13 completes the *vocabulary* a truthful refusal needs; the *enforcement* is
the adoption revision. Proof the vocabulary is present and correct: brokkr-core builds and its tests pass;
the two variants carry corpus-grounded messages and the two-events-not-one / not-`NeedsDapConfirmation`
rationales §6.8 requires.

## 2. AMD-005 — OQGF-P-8.2 (resolution is an act)

**Verdict: traced — unchanged.** Adding refusal variants does not make resolution a timeout; an expired or
replayed decision is *refused*, not resolved-by-clock. Resolution remains an explicit signed act.

## 3. AMD-001 — OQGF-M-14 (freshness pattern) as it bears

> *"Intent Provenance Chains SHALL carry a freshness nonce and an expiry."*

**Verdict: traced.** Resolution freshness now has **both** halves in core: the fields (`nonce`, `expiry` —
Rev 1.12) and the refusals (`Expired`, `ReplayedNonce` — Rev 1.13). Neither is *enforced* until the sentinel
adopts them (the adoption revision). This revision completes the type surface M-14's pattern needs for the
resolution decision.

## 4. Invariants

| Inv | Verdict |
|---|---|
| **I-5** | Holds — `cargo tree -e no-dev -p brokkr-core` = `brokkr-core` alone; two enum variants add no dependency; `#![no_std]` preserved. |
| **I-8** | Holds, and **its proof is unaffected** — the two compile_fail doctests (`EscalationType`, `ResolutionDecision`) concern struct-construction arity, not the `ResolveError` enum; both still pass (verified by running, not asserted). A `ResolveError` variant addition is orthogonal to the I-8 doctests. |

## 5. Result

**0 absent.** OQGF-P-8.5: **refusal vocabulary placed, not satisfied** — enforcement (check/track/return) is
the `brokkr-sentinel` adoption revision, and saying so is the honest record, not silence. OQGF-P-8.2 and
OQGF-M-14 **traced**. I-5 and I-8 hold; the I-8 doctests were re-run and are unaffected. No verdict moved
backward — this revision only adds two error variants. It disposes GAP-2026-08-12-001 by completing the
vocabulary the adoption revision was blocked for.

— End of conformance check CONF-2026-08-14-P1-REV-R8.
