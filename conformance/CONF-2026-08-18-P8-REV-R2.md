# Conformance Check CONF-2026-08-18-P8-REV-R2

**Phase:** 8 (revision) — `brokkr-sentinel` builds the evaluation loop (§6.7), retains grants,
removes both held clocks (I-13), and reorders EIR's checks (§6.8), under ARCH Rev 1.16.
**Subject:** the revised `brokkr-sentinel` (`heimdall.rs`, `eir.rs`, `lib.rs`, `tests/sentinel.rs`).
**Supersedes the STOP record `CONF-2026-08-18-P8-REV-R1`** (annotated there as the build-stopped
audit; this is the completed build).
**Date:** 18 August 2026
**Method (CLAUDE.md §5.3):** requirements enumerated **from the corpus** — AMD-002 (OQGF-P-1…P-5),
AMD-005 (P-8.2, P-8.3, P-8.5, P-8.6), AMD-001 (M-12, M-14), OQGF-I-6 — quoted. Per §5.3 verdicts may
move; two move **from a false satisfied to a genuine one on end-to-end proof**, and that is stated.

**What was checked, and against what (§7):** the heuristic layer's runtime behaviour (a detector
firing, a grant suppressing), against the **loop tests** — not the type surface — and the exhaustive
comparison table (every time comparison traced to its source). Finding: **0 absent**; I-6 and P-4
move to satisfied on real proof; I-13 holds (no clock).

---

## 1. OQGF-I-6 (the heuristic sentinel layer) — a verdict that was wrong at the time

> *"The sentinel network SHALL include … a data-content sentinel capable of identifying … "* and
> the heuristic detection layer generally (OQGF-I-6): a detector must actually **run**.

**Phase 8 recorded I-6 satisfied on a crate where a detector never detected.** Rev 1.11 specified a
`Detector`, an `Observation`, a `DetectionVerdict` — and never said *when a detector runs*. Phase 8
consulted detectors only during screening; no live observation ever reached one. The verdict was
recorded on the type surface, not on an end-to-end path (Rev 1.16 §13).

**Verdict now: SATISFIED — on the loop, proven end to end.** `Heimdall::observe` runs every
registered detector over every observation; `test_oqgf_i_6_registered_detector_fires_on_observation`
is the test that **could not have been written before** (nothing ran detectors) and now passes: a
registered detector that fires produces a `Detection` with a raise-only Signal. **The Phase-8
verdict was wrong at the time** (§5.3): it moved to a genuine satisfied only when the composition —
not the components — was built and tested.

## 2. OQGF-P-4 (tolerance grants suppress) — also wrong at the time

> *"A confirmed false positive … MAY be suppressed at runtime … Expired or out-of-scope grants
> SHALL have no effect."*

**Phase 8 recorded P-4 satisfied on a crate where a grant suppressed nothing** — `grant_heuristic`
validated a grant and dropped it; nothing retained it, so nothing could attach suppression to a
firing. **Verdict now: SATISFIED — on the loop.** Grants are retained (`HeimdallState.grants`);
`observe` consults them per firing. Proven by `test_oqgf_p_4_live_grant_suppresses_and_records` (a
live grant suppresses and the Detection is still recorded), `..._out_of_scope_grant_does_not_
suppress`, and — load-bearing — **`test_oqgf_p_4_i13_expired_grant_stops_suppressing`**: one
instance, one grant, one firing, suppressed before expiry and **raising after**. The Phase-8 verdict
was wrong at the time and moves to satisfied on the end-to-end proof.

**The I-13 dimension here fails toward blindness** (Rev 1.16 §6.7): a grant whose expiry is checked
against a held clock never expires, silencing a detector forever, and a silenced detector produces
no evidence. The expiry check now reads the per-call `now` in `observe` (table §6). SATISFIED.

## 3. OQGF-P-8.5 (fail-safe asymmetry; freshness) and §6.8 reorder

> *"EIR SHALL refuse a decision whose `now > expiry`, and SHALL refuse a `nonce` it has already
> accepted."*

**Verdict: SATISFIED.** Signature verified first; expiry (`now` a per-call parameter of `resolve`,
I-13) evaluated **before** replay (Rev 1.16); replay still refused. Proven by
`test_oqgf_p_8_5_expired_decision_reports_expired`, `..._replayed_nonce_still_refused`, and the
reorder proof `..._expired_before_replay_reports_expired` (an accepted-then-expired decision now
reports `Expired`, not `ReplayedNonce` — the arm that was unreachable before Rev 1.16). The
raise-only asymmetry and DAP-confirmation are unchanged (`..._cannot_lower_above_baseline_without_
dap`).

## 4. OQGF-P-8.3 (hysteresis), P-8.6 (chronic), P-8.2 (recorded act)

**Verdict: SATISFIED.** Dwell/hold and chronic-duration are computed from the per-call `now`
(`classify` fed the parameter, not `st.now`). `test_oqgf_p_8_3_i13_hysteresis_ages` is the EIR
two-verdict: one instance, one escalation, `may_resolve` NotYet before the hold window, Eligible
after — a held clock could not produce two verdicts. P-8.6 chronic flagged
(`test_oqgf_p_8_6_chronic_is_flagged`, `scan_chronic(now)`). P-8.2 (resolution is a recorded act,
not a timeout) unchanged.

## 5. OQGF-M-12 (cross-hop reconciliation) — verdict unchanged in substance

**Verdict: PARTIAL, unchanged** (as Rev 1.11 recorded: the mechanism now, the statistic at Phase
11). What changed is *placement*: reconciliation runs **inside** the loop as one firing subject to
suppression, not as a side method. `test_oqgf_m_12_mismatch_deviates_and_none_does_not` now exercises
it through `observe`. A `Hop` with `executed: None` is still not a deviation. The per-supplier pass
rate remains unmeasurable until Phase 11.

## 6. Invariants

| Inv | Check | Verdict |
|---|---|---|
| **I-13** | `brokkr-sentinel` holds no clock. `EirState`/`HeimdallState` have no `now` field; `self.now`/`st.now`/`Mutex<Timestamp>`/`fn now(` empty. **The exhaustive comparison table (report §6) is CLEAN** — all 13 comparisons read a per-call `now`, including the `classify` row a grep missed. | **holds — and workspace-wide now** |
| **I-9** | No path through `observe` permits anything: `DetectionVerdict` = `Clear\|Fired`, `PostureEffect` = `Raise` only, `Detection` carries at most a raise-only Signal. The heuristic layer cannot open a gate. | **holds** |
| **I-5 / I-6** | deps `{core, crypto}`; no reasoner/tools/cli; no model call, no network, no `unsafe`. | **holds** |

## 7. Result

**0 absent.** **SATISFIED (moved from a false Phase-8 satisfied to a genuine one on end-to-end
proof, §5.3):** OQGF-I-6, OQGF-P-4. **SATISFIED:** P-8.5 (+ §6.8 reorder), P-8.3, P-8.6, P-8.2.
**PARTIAL (unchanged):** M-12 (statistic is Phase 11). **Holds:** I-13 (no clock; table clean), I-9,
I-5. **RISK-2026-0006:** its closure condition — the exhaustive comparison table — is now **clean
across three crates**; the treatment is executed, the two residuals stand (report Task 8). No
requirement is absent. This is a real check whose central evidence is the **loop tests and the
source-traced table**, not the type surface that let the Phase-8 verdicts be recorded on nothing.

— End of conformance check CONF-2026-08-18-P8-REV-R2.
