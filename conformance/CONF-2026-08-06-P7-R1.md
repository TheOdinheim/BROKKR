# Conformance Check CONF-2026-08-06-P7-R1

**Phase:** 7 (`brokkr-audit` / SAGA — the Audit Spine) — **pre-build Step-0 audit**
**Subject:** BROKKR-ARCH §6.9 Rev 1.9 + the committed `brokkr-core`/`brokkr-crypto` types, audited for
buildability against the governance corpus
**Date:** 6 August 2026
**Method (CLAUDE.md §5.3):** requirements enumerated **from the corpus** (OQGF-1.0 §A.5; AMD-005/006/007/
008/009), never from the architecture's own §14 traceability table. Each is checked against what the
committed types can build and what §6.9 specifies. Because the build **stopped at Step 0**
(GAP-2026-08-06-001), no requirement is `satisfied` by a running crate; each in-scope requirement is
marked *buildable-not-built*, *blocked* (behind the gap), *partial*, or *n.a. with justification*.

**What was checked, and against what (§7 — zero findings is not a pass):** the eleven Organ-5 /
cross-organ requirements the prompt scoped, quoted below from the corpus, against (a) §6.9's specified
surface and (b) the committed `brokkr-core`/`brokkr-crypto` types read this session. The finding is **one
blocker** (the chain-linkage contradiction) plus the routine phase-boundary `partial`/`n.a.` set.

---

## 1. OQGF-1.0 §A.5 — Organ 5 (Memory)

| Req | Text (quoted) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **A-1** | *"For every regulated AI/ML decision the system SHALL record: the model identifier and version, the AIBOM digest, the input (or a privacy-preserving derivative thereof), the output, the explanation artifact, the timestamp, and the DAP."* | The `AuditEvent` reasoner-proposal variant carries A-1's full field list over committed types (`ModelIdentity`, a `Digest` for AIBOM, input derivative, output, explanation, `Timestamp`, `Dap`); §6.9 line 1033 scopes A-1 to the one AI/ML decision in BROKKR — a MÍMIR proposal. Other events carry the header `at`/`dap` + their own payload, correctly (a gate verdict has no model). | Variant placed but **unexercised** — MÍMIR is Phase 10. And the variant lives in the record whose chain linkage is **blocked**. | **blocked / buildable-not-built** (field surface determinable; sits in the blocked record) |
| A-2 | quantum-computation records (circuit, calibration, full sampling distribution) | — | — | **n.a.** (BROKKR runs no quantum workload — §1.4) |
| **A-3** | *"Audit records SHALL be signed under at least two PQC families **and timestamped via an RFC 3161-compliant authority that itself supports PQC signing**."* | Dual-family half by construction (`DualKeyPair::sign_dual` → ML-DSA-65 + SLH-DSA-SHAKE-192s, R-1). Timestamp half is a defined **seam** (`TimestampAuthority` trait; `Timestamping::{Token, Unavailable{reason}}`), no production authority. | Seam designed (Task 8, buildable); the dual-family signing is `brokkr-crypto`'s, tested at Phase 2. | **PARTIAL** — signing half met, timestamp half a declared seam. **Not upgraded** (A-3 carries no level qualifier; binds at Enhanced). Residual named. |
| A-4 | quantum-appropriate explanation artifacts | — | — | **n.a.** (no quantum model in the decision path — §1.4) |
| A-5 | *"Every regulated AI/ML system SHALL have a named DAP recorded in the audit record; the DAP SHALL be a natural person, not an entity."* | `AuditRecord.dap: Dap` is a required field (§6.9 line 923); `Dap { name, id }` is a natural person by shape. | Type-level; the record it sits on is blocked. | **buildable-not-built** |
| **A-6** | *"Audit signatures SHALL be re-signed under the prevailing cryptographic generation at intervals not exceeding five years, **preserving the original signatures and chain**."* | §6.9 "Re-signing": append a `GenerationSignature`, never replace; do not alter `seq/prev/at/dap/event`. The **no-resurrection** half (re-signer holds no `SubjectKey`, no decrypt path) is structurally clean (report §2.4). | The **"preserving … chain"** half is exactly the contradiction: appending a signature changes the full bytes the next `prev` digested (§6.9 says `prev` covers "full bytes including signatures"), so re-signing breaks the chain §6.9 says it preserves. | **BLOCKED — GAP-2026-08-06-001** |
| A-7 | *"A regulatory query interface SHALL be available within 72 hours … read-only, signed export."* | The **capability** — read-only signed export of the chain, same interface serving a subject — is architectural and buildable (Task 6). The **72-hour** response-time commitment is operational (§6.9 line 1013). | Buildable given a chain (blocked); the split is recorded so the operational half is not dropped. | **buildable-not-built** (capability); 72h **operational** (split, not n.a.) |

## 2. AMD-009 (OQGF-P-11) — personal-data lifecycle at the audit layer

| Req | Text (quoted, abridged) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **P-11.5** | *"Erasure … SHALL be performed by destroying the quantum-safe key … and SHALL NOT be performed by deleting the record from the append-only store."* | `SubjectKey::shred` destroys the ML-KEM key; ciphertext + audit skeleton preserved; a signed `ErasureTombstone {erased, classification, at, dap}` appended. `brokkr-crypto` primitives committed and tested (Phase 2). | Task 5 buildable; but `test_oqgf_p_11_5_erasure_preserves_chain` verifies the **chain is intact** after erasure — which needs the blocked linkage definition. | **buildable-not-built; the chain-intact proof is BLOCKED** |
| **P-11.6** | subject rights — what personal data is held, its Purpose/Retention, erasure on request, via the OQGF-A-7 interface | Same export/query interface, extended to an authenticated subject (Task 6). | Buildable given a chain. | **buildable-not-built** |
| **P-11.7** | *"a re-signed record of erased Personal Data SHALL remain irrecoverable."* | The re-signer operates over ciphertext + canonical bytes only, holds no `SubjectKey`; the *type* of the re-signing fn makes decryption unrepresentable (report §2.4). | `test_oqgf_p_11_7_resigned_erased_record_stays_irrecoverable` (the phase's most important test) **re-signs and re-verifies the chain** — blocked on the linkage definition. | **buildable (no-resurrection) but the test is BLOCKED behind A-6** |

## 3. AMD-006 P-9.5, AMD-008 P-10.6 — the two registers

| Req | Text (quoted, abridged) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **P-9.5** | Risk-Acceptance Entries reviewed; the standing inventory of active accepted risks reportable on demand | Risk-Acceptance Register in SAGA, keyed by `RiskAcceptanceId` **which SAGA assigns on record** (§6.9 line 1019) — this is where HÚÐ's acceptance resolver gets its `(id, record)` pair. Append-only; `active()`-style inventory. | Task 7 buildable — independent of the chain linkage. `RiskAcceptanceId` is a committed `string_id!`; SAGA mints it. | **buildable-not-built** |
| **P-10.6** | the Risk Register reviewed periodically, reportable on demand; closed/superseded entries annotated and retained, never deleted | Implement `brokkr_core::risk::RiskRegister` (`record(&self, RiskEntry) -> RiskId`, `inventory(&self) -> Vec<RiskEntry>`) with interior mutability (report §4b); append-only; supersede by annotation. | Task 7 buildable — independent of the chain linkage. `std::sync::Mutex` store (`brokkr-audit` is `std`). | **buildable-not-built** |

## 4. AMD-007 I-13, AMD-005 P-8.4

| Req | Text (quoted, abridged) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **I-13** | *"Every Barrier decision … SHALL be recorded in Organ 5 with the BCR digest or a record of its absence, the classification, the destination or origin, the deciding policy, and … the accountable DAP."* | The barrier-crossing `AuditEvent` variant carries `BarrierVerdict` + `BoundaryFlow` + the BCR digest **or its recorded absence** + (for `AcceptedRisk`) the DAP (§6.9 lines 1023–1027). | Variant buildable (Task 2); Phase 6 decided, Phase 11 wires; sits in the blocked record. | **buildable-not-built** |
| **P-8.4** | de-escalation SHALL NOT erase the Organ-5 incident record nor revert a learned detector — memory preserved on stand-down | SAGA is append-only and never deletes; the resolution-decision and detector-activation events persist independently of any later posture change. §6.9 line 905: SAGA is "the source of truth for … EIR's incident preservation." | Structural (append-only); EIR is Phase 8. | **buildable-not-built** (SAGA's non-deletion is the mechanism; exercised when EIR exists) |

## 5. Invariants

| Inv | Status |
|---|---|
| **I-2** | Not SAGA's to introduce — SAGA records verdicts, never converts a `Deny` to `Allow`. Holds by not doing anything. |
| **I-5** | **Will hold.** Deps fixed to `brokkr-core` + `brokkr-crypto`; every payload type is in core, every crypto call in crypto; SAGA offers a recording surface and does not reach into other crates (§6.9 line 1041). To confirm with `cargo tree -e no-dev` when built. |
| **I-6** | **Will hold.** Pure logic; no model call, no network; `#![forbid(unsafe_code)]` (FFI is `brokkr-crypto`'s). |

## 6. Result

**Not a clean pass, and not asserted as one.** Of the eleven scoped requirements: **A-6 is BLOCKED**
behind GAP-2026-08-06-001 (the §6.9 chain-linkage contradiction), and A-1's chain placement plus
P-11.5/P-11.7's chain-intact proofs are blocked with it; **A-3 is PARTIAL** (dual-family met, timestamp
seam unwired — not upgraded); **A-7** capability buildable / 72h operational; **A-2, A-4 n.a.**; the
remaining requirements (A-5, P-9.5, P-10.6, I-13, P-8.4) are **buildable-not-built**, determinable from
committed types and ready the moment the gap is disposed. **Zero requirements are `satisfied`, because
nothing was built** — the build stopped at Step 0, correctly, on a specification contradiction the
architecture must resolve before SAGA's spine can be written.

— End of conformance check CONF-2026-08-06-P7-R1.
