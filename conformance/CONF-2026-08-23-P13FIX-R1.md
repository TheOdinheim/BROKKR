# Conformance Check — Phase 13-FIX (Harden Black-Box Findings)

**Record ID:** CONF-2026-08-23-P13FIX-R1
**Date:** 2026-08-23
**Scope:** hardening of the ten red-team findings (F-1 … F-10). This phase **adds defenses**; it relaxes no requirement and changes no normative verdict. It enumerates the corpus requirements the fixes touch and records that each is **strengthened or unaffected**.
**Enumerated from the corpus** (not the traceability table): OQGF-P-1 (host-harm bound), OQGF-M-14 (chain freshness), OQGF-R-1 (dual-family signing), OQGF-A (audit integrity), the prime directive §2; invariants I-1, I-12, I-13. Corpus is OQGF-1.0 + AMD-001…AMD-009.
**Author:** Claude Code (builder).
**Per RISK-2026-0007:** each item names the code path and the regression test that fails without the fix.

## What was checked, and against what

The hardened orchestrator, crypto, tool, and parser, against the requirements the fixes bear on. No verdict moves backward; several defenses that were *documented residuals* are now *enforced or bounded*.

---

## OQGF-M-14 — chain freshness / no replay

> "Intent Provenance Chains SHALL carry a freshness nonce and an expiry."

| Field | Content |
|---|---|
| Code path | The nonce+expiry were carried and expiry-checked (SINDRI Signal 2). **F-1** adds a driver-layer replay ledger in `Orchestrator::check_guards`: `(principal, nonce)` used within expiry → `Denied`. SINDRI remains stateless/deterministic; the ledger is the driver's, like EIR's resolution-nonce ledger. |
| Test | `fix_f1_replay_denied`; 13A `attack_2_4`. |
| Verdict | **strengthened.** The nonce is now *consumed*, not merely *carried* — replay within expiry is denied, closing red-team F-1. |

## OQGF-P-1 — host-harm bound (no over-denial) / availability

> "Disruption of a legitimate operation is a governance failure of equal standing to a missed threat."

| Field | Content |
|---|---|
| Code path | The new guards are calibrated **not** to over-deny: **F-10** `validate_scope` warns rather than denies (an undeclared capability grants nothing); **F-7** rate limiting and **F-3** size cap are resource bounds with generous production defaults (100/60 s, 1 MiB); **F-2** rejects only paths that *escape the sandbox*, not legitimate in-root writes (`fix_f2_legitimate_path_within_root`). |
| Test | `fix_f10_...`, `fix_f7_...`, `fix_f3_normal_detail_allowed`, `fix_f2_legitimate_path_within_root`. |
| Verdict | **satisfied.** Every new guard admits legitimate work; the fixes add safety without metastasizing into autoimmunity. |

## OQGF-R-1 — dual-family signing integrity

> "…at least two PQC signature families … in parallel for any signature relied on for legal evidence."

| Field | Content |
|---|---|
| Code path | **F-4** makes `DualKeyPair::sign_dual` `&mut self` and removes the type's `Sync`, so concurrent signing on one keypair — which corrupted the dual-family signature — is a compile error. SAGA/HEIMDALL hold the signer behind a `Mutex`. Dual-family verification is unchanged. |
| Test | two `compile_fail` doctests in `brokkr-crypto` (E0596, E0277). |
| Verdict | **strengthened.** The soundness gap behind red-team F-4 is closed by construction — a corrupted-signature data race is now unrepresentable in safe code. |

## OQGF-A — audit integrity and reviewability

> Organ 5 records are the accountability spine; a record must be trustworthy for review.

| Field | Content |
|---|---|
| Code path | **F-8** sanitizes the audit **copy** of model-supplied `detail`/`rationale` (control chars, ANSI escapes stripped) so a recorded value cannot deceive or corrupt a reviewer/log viewer; the signed *decision* content and the real action are untouched. **F-5** keeps the true (genome) denial reason in SAGA while returning a uniform stage to the caller — the audit stays complete, the external oracle closes. |
| Test | `fix_f8_control_chars_stripped`, `fix_f8_unit_newline_and_tab_preserved`, `fix_f5_genome_denial_presents_as_gate`. |
| Verdict | **strengthened.** Audit records are now render-safe; the trusted audit still carries the full truth. |

## Prime directive §2 / invariants I-1, I-12, I-13

| Field | Content |
|---|---|
| Code path | The model is still untrusted and reached only through the cycle. The orchestrator still mints nothing (`AuthorizedAction` only from `gate.authorize`, `ClearedContext` only from `clear`) — I-1/I-12 untouched. **I-13:** `now` is still a per-call parameter threaded to every gate; F-9's `last_now` is the *remembered previous* time used only to detect a regressing clock, never read to evaluate a freshness/expiry — the held-clock grep matches only `Orchestrator::now()`. |
| Test | the full red-team suite (57 tests) + `redteam_fix` (11). |
| Verdict | **satisfied — unchanged.** No structural invariant is weakened; the guards sit at the driver layer around the unchanged spine. |

---

## Findings

**Zero requirements ABSENT. Zero verdicts moved backward.** This is a hardening phase: it enforces or bounds defenses that were documented residuals (F-1 replay, F-2 path, F-3 size, F-4 signing soundness, F-8 audit content) and closes low-severity oracles/gaps (F-5, F-7, F-9, F-10), each with a regression test that fails without the fix. The four behavioral guards are configurable with production-safe defaults; the spine-verifying test rigs opt into permissive so the red-team suite keeps probing the deterministic gates — a documented separation (report §0, §4), not a relaxation. Two honest boundaries are named: F-2's lexical target-canonicalization (symlinks inside the root) and the guards' default-on-in-production / off-in-spine-rigs split.
