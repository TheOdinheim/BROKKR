# Conformance Check — Phase 11.5 (brokkr-cli / Real Subsystem Integration)

**Record ID:** CONF-2026-08-20-P11.5-R1
**Date:** 2026-08-20
**Scope:** `brokkr-cli` integration test `real_integration.rs`. **This phase adds no source and enforces no new requirement.** It provides **integration-level evidence** — with real subsystems and real PQC crypto — for verdicts already recorded against the individual crates. This check enumerates the in-scope requirements from the corpus, states what the real-integration test demonstrates for each, and records that **no verdict moves** (the enforcement lives in the subsystems, tested in their own phases; this strengthens the evidence, it does not re-decide it).
**Enumerated from the corpus** (not the traceability table): the prime directive (§2), OQGF-M-11, OQGF-M-8, OQGF-R-1, OQGF-A-1 (quoted below). Corpus is OQGF-1.0 + AMD-001…AMD-009.
**Author:** Claude Code (builder).
**Per RISK-2026-0007:** each item names the end-to-end code path and the assertion that would fail if the property did not hold.

## What was checked, and against what

The one governed hop in `real_governed_hop_writes_a_file_with_real_pqc`, run with real SINDRI / SAGA / HEIMDALL / HÚÐ / SKULD / filesystem tool and real ML-DSA-65 + SLH-DSA-SHAKE-192s keys (wolfSSL). BIFRÖST and MÍMIR are controlled doubles (no model endpoint).

---

## Prime directive (BROKKR-ARCH §2) — the model is never in the trust path

> "The reasoning model is never in the trust path for any decision to block, permit, raise, or lower posture."

| Field | Content |
|---|---|
| Code path | MÍMIR (a double) proposes "write to the temp file". The proposal's action passes real REGIN (`GenomeCheck`), **real SINDRI** (`authorize`), and real HÚÐ (`evaluate`) before the real tool runs. The write happens only because the deterministic gates cleared — not because the model asked. |
| Assertion (fails otherwise) | The file exists with the expected content **only after** `HopResult::Executed`; the grant is a real `Granted` from SINDRI recorded in SAGA. If the model's proposal alone caused execution, the file would appear without a real grant. |
| Verdict | **satisfied — integration evidence** (no change). Demonstrated end-to-end with a real gate, not a spy. |

## OQGF-M-11 — the costimulation gate (real verification)

> "A privileged action SHALL require both a valid identity attestation per OQGF-M-1 and a valid Intent Provenance Chain per OQGF-M-8 (Signal 2)."

| Field | Content |
|---|---|
| Code path | Real `Sindri::evaluate`: Signal 1 resolves `principal` in the `RegistryResolver` and binds it to `root.principal`; Signal 2 verifies the **real dual-family Root-Intent signature** via `verify_chain_public` against the declared public root; conjunct 3 confirms `write ∈ scope`; conjunct 4 finds no invariant. Then `authorize` mints the `AuthorizedAction` the tool consumes. |
| Assertion (fails otherwise) | SAGA records a `Granted` authorization, and the file is written. A wrong key / expired chain / undeclared tool / missing capability would yield `Anergy` and no file — the test's grant is earned by real cryptography. |
| Verdict | **satisfied — integration evidence** (no change; M-11 enforcement was decided in Phase 4 / the conjunct revision). |

## OQGF-M-8 — Intent Provenance Chain (public-roots verification)

> "A verifier SHALL be able to reconstruct the complete chain back to the Root Intent using only declared public roots of trust."

| Field | Content |
|---|---|
| Code path | SINDRI's Signal 2 calls `Skuld::verify_chain_public(root, &[], root_public, &[], now)` with `root_public` resolved from the declared registry — a real dual-family verification of the real `sign_root` output. |
| Assertion (fails otherwise) | The grant (and the file) exist only if the real public-roots verification passed. |
| Verdict | **satisfied — integration evidence** (no change). |

## OQGF-R-1 — dual PQC families for audit signatures

> "High-Assurance systems SHALL employ at least two PQC signature families … in parallel for any signature relied on for legal evidence." (Enhanced binds SAGA to dual-family per BROKKR-ARCH §1.4.)

| Field | Content |
|---|---|
| Code path | Real `Saga::append` signs each record's canonical content with `DualKeyPair::sign_dual` (ML-DSA-65 + SLH-DSA-SHAKE-192s) and pushes a `GenerationSignature`. |
| Assertion (fails otherwise) | The test asserts **every** recorded `AuditRecord` carries a non-empty `signatures` vector — a real dual-family signature per record. |
| Verdict | **satisfied — integration evidence** (no change; R-1 decided in Phase 7). |

## OQGF-A-1 — decision records (the AI/ML decision)

> "For every regulated AI/ML decision the system SHALL record: the model identifier and version, the AIBOM digest, the input (or a privacy-preserving derivative thereof), the output, the explanation artifact, the timestamp, and the DAP."

| Field | Content |
|---|---|
| Code path | The orchestrator records `AuditEvent::Proposal(ProposalRecord{ … input: Derivative(sha384(payload)) … })` for the one AI/ML decision (the MÍMIR proposal), dual-signed by SAGA. |
| Assertion (fails otherwise) | The test asserts a `Proposal` event is present in `saga.records()`. |
| Verdict | **PARTIAL — unchanged.** As recorded in the Phase-11 check: `model.version`/`provider` and `aibom_digest` are placeholders pending production AIBOM wiring. This phase does not change that; it only demonstrates the derivative-input record lands in a real SAGA. |

---

## Findings

**Zero requirements ABSENT. Zero verdicts moved.** This is an evidence phase: it adds no source and enforces nothing new. It demonstrates, with real subsystems and real PQC cryptography, that the properties already decided against the individual crates hold **when composed** — a real signed Root Intent, real SINDRI verification, real dual-signed SAGA records, and a real filesystem effect, all reached only through the governed cycle. Four honest limitations are recorded in the phase report (BIFRÖST/MÍMIR doubles; HÚÐ's Public short-circuit not exercising the custody path; HEIMDALL's empty detector set; declared-test genome vs signed REGIN genome), none of which weakens a recorded verdict — each is a boundary of *what this test exercises*, stated rather than hidden.
