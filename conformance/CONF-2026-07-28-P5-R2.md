# Conformance Check — Phase 5, brokkr-genome (REGIN) — BUILD check

**Record ID:** CONF-2026-07-28-P5-R2
**Phase:** 5 (`brokkr-genome` / REGIN — the Genome and the OQGF-G-4 promotion gate), §6.2 Rev 1.5
**Date:** 28 July 2026
**Subject:** the **built** `brokkr-genome` crate, tested against the real wolfSSL v5.9.2 `.so` (non-FIPS) through `brokkr-crypto` — no mocks.
**Ground truth:** the governance corpus — OQGF-1.0 §A.1 (G-1…G-9) and AMD-001…AMD-009 (CLAUDE.md v1.6 §5.3) — quoted, never from BROKKR-ARCH §14.
**Auditor:** Claude Code. **For:** Jeremy Rose, DAP — who verifies the source and the live test output.
**Relationship to R1:** the pre-build surface audit `CONF-2026-07-27-P5-R1.md` **stands** (it recorded the predicate-5 gap, since disposed by ARCH Rev 1.5). This R2 is the build check.

---

## 0. What was checked, and against what (§7)

Not a zero-findings report. Every OQGF-G-* and related corpus requirement in scope for REGIN is
checked against the **built** gate, with the test that proves it (or the reason it is n.a./partial).
Enhanced obligations apply. All six §6.2 predicates are now buildable (the Rev 1.5 vocabulary closed
the predicate-5 gap) and are tested with real crypto.

## 1. In-scope requirements (corpus, quoted)

| Req | Text (quoted) | Implementation | Proof (test) | Verdict |
|---|---|---|---|---|
| **OQGF-G-4** (non-bypassable promotion gate) | "The build pipeline SHALL enforce, as a non-bypassable gate, that no artifact may be promoted … unless its CBOM and AIBOM are present, signed, and **free of disallowed algorithms**" | `gate::promote` — six predicates, evaluates all and returns all findings; `Blocked` has no path to `Promoted` (I-2) | `test_oqgf_g_4_valid_genome_promotes` (all pass → Promoted); `test_oqgf_g_4_disallowed_algorithm_blocks`; `test_oqgf_g_4_tampered_register_fails_signature`; `test_all_findings_returned`; `test_blocked_verdict_has_no_promotion_path` (doctest) | **satisfied** — the gate's core requirement, built and tested |
| **OQGF-G-1** (CBOM present/signed) | "SHALL maintain a current CBOM … listing every cryptographic primitive …" | `Cbom` present in `Genome` (type-level, I-10); its signature is verified (predicate 2); its typed `algorithms` are evaluated (predicate 3) | `test_oqgf_g_4_valid_genome_promotes`, `test_oqgf_g_4_disallowed_algorithm_blocks` | **satisfied** (present, signed, evaluated) |
| **OQGF-G-2** (AIBOM present/signed) | "SHALL maintain a current AIBOM …" | `Aibom` present (I-10); its signature is verified (predicate 2) | `test_oqgf_g_4_valid_genome_promotes`, `test_oqgf_g_4_tampered_register_fails_signature` (via any register) | **satisfied** (present, signed) |
| **OQGF-G-3** (signed artifacts embed CBOM/AIBOM digests) | "Every release artifact … SHALL be cryptographically signed … and SHALL embed or reference its CBOM and AIBOM digests" | The genome signature covers each register's **full** bytes including its signature (`canonical::genome_signed_content`), so the genome commits to which signed registers it was built from. The *release-artifact* embedding of digests is Phase 11 | `test_oqgf_g_4_tampered_register_fails_signature` (genome sig also breaks); `test_wrong_dap_key_blocks_all_signatures` | **partial** — the genome-commits-to-registers surface is here; artifact-level embedding is the release pipeline (Phase 11). Traced, not claimed closed. |
| **OQGF-G-5** (typed algorithm identifiers) | "no algorithm identifier may be hard-coded … a typed enum, never a string" | `AlgorithmId` evaluated by predicate 3; the canonical encoding uses **exhaustive** enum tags (no catch-all) for `AlgorithmId`, `SignatureAlg`, `HashAlg`, `KemAlg`, `PrivilegeClass`, `ResponseClass`, `Classification`, `NamedGroup` — a new variant breaks the build | `test_oqgf_g_4_disallowed_algorithm_blocks` (typed set intersection) | **satisfied** |
| **OQGF-G-8** (policy as code, signed) | "Policy SHALL be expressed as code, version-controlled, signed …" | `PolicyRegister` (capabilities + invariants + disallowed) is verified (predicate 2) and evaluated (predicates 3, 5, 6) | `test_invariant_forbids_unknown_capability_blocks`, `test_invariant_forbidding_nothing_blocks`, `test_tool_capability_not_in_vocabulary_blocks` | **satisfied** (the signed policy surface is verified and evaluated) |
| **OQGF-G-9** (BOM regeneration/retention) | "regenerated and re-signed on every release … retained …" | No REGIN surface — regeneration cadence and retention are the release pipeline and SAGA (Phase 7) | none | **n.a. — justified: REGIN gates a genome value; it does not schedule regeneration or own retention** |
| **OQGF-M-5** (mutual auth at registration) | "one-sided TLS SHALL NOT satisfy this requirement" | Type-level — `ModelEndpoint::client_cert` is a required field (I-11, committed). REGIN gates such endpoints; it adds no M-5 surface | (committed-type doctest in `brokkr-core`) | **satisfied at the type level (traced) — enforced by the type, not this gate** |
| **OQGF-M-6** (vendor trust score, five factors, quarterly) | "computed per supplier based on … and statistical reconciliation pass rate; reviewed quarterly" | Predicate 4 enforces the **quarterly-review (staleness)** obligation (90-day bound; saturating; future fails). The **reconciliation_pass_rate factor is unmeasured** until HEIMDALL (Phase 8) | `test_oqgf_m_6_stale_trust_score_blocks`, `test_oqgf_m_6_future_trust_score_blocks` | **PARTIAL** — staleness enforced and tested here; the reconciliation factor is a placeholder. **Not upgraded.** Consistent with ARCH §14. |
| **OQGF-M-13** (least-privilege tool declaration) | "scoped to the minimum authority required … the narrowest defensible" | Predicate 5 checks that a tool's `required_capabilities` are all in the vocabulary — making the declaration *checkable*. The runtime *scope* check (conjunct 3) is SINDRI's, **not built here** | `test_tool_capability_not_in_vocabulary_blocks` | **binding checkable here; the conjunct-3 CHECK is SINDRI's (later) — not claimed closed. RISK-2026-0004 unchanged.** |
| **OQGF-M-8** (chain to declared roots) | "reconstruct … using only declared public roots of trust" | The `RootsOfTrust` register's signature is verified (predicate 2). Its *consumption* (resolving keys, verifying chains) is SINDRI's, wired at Phase 4. Pre-shared residual stands (RISK-2026-0005) | `test_oqgf_g_4_tampered_register_fails_signature` (roots is a register), `test_wrong_dap_key_blocks_all_signatures` | **register present and gated (signature verified); consumption is SINDRI's — traced** |
| **OQGF-P-2** (deterministic gate non-suppressible) | "Deterministic Gates SHALL remain fail-closed and non-suppressible … a request to suppress … SHALL be refused" | **Structural**: the gate exposes no `DetectorId` and no tolerance surface (verified: `DetectorId`/`ToleranceGrant` appear only in doc comments, never in code); a `ToleranceGrant` targets a `DetectorId` and so cannot address the gate. And `Blocked` has no conversion to `Promoted` | the `compile_fail,E0599` doctest on `PromotionVerdict` (proving `.into_promoted()` does not exist); the grep-verified absence of any tolerance surface | **satisfied structurally** |
| **OQGF-P-10** (Risk Register) | continuous risk identification/disposition | none in REGIN | none | **n.a. — `brokkr-core`/`brokkr-audit` surface (Phases 1/7)** |
| **OQGF-P-11** (personal-data lifecycle) | minimization, purpose, retention, erasure | none in REGIN | none | **n.a. — no personal data in the genome** |

## 2. Backward verdict movement (v1.6 §5.3)

None required. The pre-build audit (`CONF-2026-07-27-P5-R1`) recorded G-4 as *blocked* on predicate 5;
Rev 1.5 disposed that gap and this build satisfies G-4. That is a forward move on the built subject,
not a downward correction of a prior claim. M-6 stays **PARTIAL** (unchanged) — the reconciliation
factor is still unmeasured; the gate enforces only staleness, and this record does not upgrade it.

## 3. Invariants (crate-level)

| Inv | Statement | Status |
|---|---|---|
| **I-5** | `brokkr-genome` depends only on `brokkr-core` + `brokkr-crypto` | **Held.** `cargo tree -e no-dev` = `{brokkr-core, brokkr-crypto}`. No reasoner/tools/cli. |
| **I-6** | no model calls | **Held.** Pure logic; no network, no FFI, no reasoner dependency. `#![forbid(unsafe_code)]`. |
| **I-10** | no promotion without a present, signed genome | **Held.** Predicate 1 (present) is the committed type-level guarantee — `Genome` has no `Option` register, so the gate writes no runtime presence check (none could fail). Predicate 2 (signed) is verified for all six registers **and** the genome, non-circularly (the DAP key is a parameter). |
| **I-2** (mirrored) | a blocked verdict cannot become promoted | **Held.** `PromotionVerdict::Blocked` has no method/`impl From`/conversion to `Promoted`; proven by the `compile_fail,E0599` doctest and the grep-verified absence of any such path in `src`. |

## 4. Findings

- **absent: 0.** No §4 gap; the build does not stop.
- **satisfied: G-4, G-1, G-2, G-5, G-8, P-2; I-2/I-5/I-6/I-10.**
- **partial (recorded): G-3** (genome-commits-to-registers here; artifact embedding Phase 11),
  **M-6** (staleness enforced; reconciliation factor unmeasured — not upgraded), **M-13**
  (declaration checkable here; conjunct-3 check is SINDRI's).
- **n.a. with justification: G-9, P-10, P-11.** **Traced (type-level/other phase): M-5, M-8.**
- **No requirement relaxed. No completeness claim.** Real crypto, no mocks; 13 tests + 1 doctest;
  the highest-risk property (domain separation) is tested directly, both as byte-inequality and as
  cross-register signature non-verification.

— End of conformance check CONF-2026-07-28-P5-R2.
