# Conformance Check CONF-2026-08-14-P8-REV-R2

**Revision:** `brokkr-sentinel` + `brokkr-audit` Rev 1.12/1.13 adoption — BUILD check
**Subject:** the built adoption (freshness enforcement in EIR, `SignatureInvalid` in HEIMDALL, one shared
resolution encoding, audit records nonce/expiry)
**Date:** 14 August 2026
**Method (CLAUDE.md §5.3):** requirements enumerated **from the corpus** (AMD-005 OQGF-P-8.2/P-8.5; AMD-002
OQGF-P-4; AMD-001 OQGF-M-14 as it bears on freshness; AMD-007 OQGF-I-13 for the audit record), quoted, never
from BROKKR-ARCH §14. Per §5.3 a verdict may move backward; here two verdicts move **forward** —
placed-not-satisfied → satisfied — on test proof, and none moves backward. Every "satisfied" names the test
or property that establishes it.

**What was checked, and against what (§7):** the freshness-enforcement, forged-grant, encoding-agreement, and
audit-record obligations of Rev 1.12/1.13, against the built crates and their real-crypto tests (sentinel 21,
audit 13; workspace 141, 0 failed). Finding: **0 absent**; P-8.5 and P-4's reporting aspect now enforced.

---

## 1. AMD-005 — OQGF-P-8.5 (fail-safe asymmetry + freshness), now ENFORCED

> §6.8: *"EIR SHALL refuse a decision whose `now > expiry`, and SHALL refuse a `nonce` it has already
> accepted for that escalation."* + Rev 1.13: *"A refusal SHALL name the reason it actually is."*

| Obligation | Implementation | Proof |
|---|---|---|
| Verify the signature over the **one** shared encoding; refuse if it does not | `EIR::resolve` calls `brokkr_core::resolution::resolution_signed_content`; fail → `NeedsDapConfirmation` | `test_oqgf_p_8_5_cannot_lower_above_baseline_without_dap`; `test_sentinel_and_core_encodings_agree` |
| Refuse `now > expiry` **as `Expired`** | check 3 in `resolve` | **`test_oqgf_p_8_5_expired_decision_is_refused`** |
| Refuse a replayed nonce **as `ReplayedNonce`**, persisting across resolve/re-raise | engine-level `accepted_nonces`, recorded on success, never removed; check 2 in `resolve` (before expiry) | **`test_oqgf_p_8_5_replayed_nonce_is_refused`** (resolves, re-raises, still refused) |

**Verdict: SATISFIED (forward move).** OQGF-P-8.5 was recorded *placed-not-satisfied* across the core Rev
1.12 (fields + encoding) and Rev 1.13 (variants) revisions; this revision supplies the **enforcement** they
were placed for, proven by the tests. The refusals **name what they actually are** — a replayed-and-expired
decision reports the attack (`ReplayedNonce`), not the latency, because replay is checked before expiry.

## 2. AMD-005 — OQGF-P-8.2 (resolution is an act)

**Verdict: traced — unchanged.** Freshness refusals reject stale/replayed decisions; nothing resolves
because a timer expired. Resolution remains an explicit, signed, DAP-confirmed act.

## 3. AMD-002 — OQGF-P-4 (tolerance grant signed), reporting aspect now ENFORCED

> *"…PQC-signed… Expired or out-of-scope grants SHALL have no effect."*

**Verdict: SATISFIED (forward move for the truthful-reporting aspect).** `grant_heuristic` now returns
`ToleranceError::SignatureInvalid` for a forged grant — the true, first-failing reason — instead of the
`OutOfScope` Phase 8 used for lack of a variant. The signed/scoped/expiring gating was already satisfied at
Phase 8; Rev 1.12 completes the *reporting* so a refusal is not a false claim in the record. Proof:
`test_oqgf_p_4_forged_grant_reports_signature_invalid` (and `_expired_grant_refused`, `_blanket_scope_refused`
unchanged).

## 4. AMD-001 — OQGF-M-14 (freshness pattern) as it bears

**Verdict: traced — now complete for resolution.** Resolution freshness now has all three: the fields
(`nonce`, `expiry`, Rev 1.12), the refusal vocabulary (`Expired`, `ReplayedNonce`, Rev 1.13), and the
**enforcement** (this revision). The M-14 nonce+expiry pattern is applied end-to-end to the one act that
lowers a defence.

## 5. AMD-007 — OQGF-I-13 (Barrier/resolution decisions recorded in Organ 5), audit fidelity

> *"…recorded in Organ 5 with … the deciding policy …"* — a resolution audit record SHALL encode the
> decision faithfully.

**Verdict: SATISFIED (audit-fidelity aspect).** `brokkr-audit`'s `write_resolution` now records `nonce` and
`expiry`, so the audit chain no longer encodes two freshness-distinct decisions identically. The audit
**record** domain tag is unchanged, so four-crate domain separation is unaffected. Proof: audit's 13 tests
pass over the amended encoding (sign-then-verify, self-consistent).

## 6. Invariants

| Inv | Verdict |
|---|---|
| **I-5** | Holds for both crates — `cargo tree -e no-dev` = `{brokkr-core, brokkr-crypto}`; no dependency added (the sentinel calls a `brokkr-core` function it already depended on). |
| **I-6** | Holds — no model call, no network, no FFI; `#![forbid(unsafe_code)]`. |
| **I-9 (sentinel)** | Holds — unchanged; `DetectionVerdict` has no action-permitting variant; Signals raise-only. |

## 7. Result

**0 absent.** **OQGF-P-8.5 → satisfied** (freshness enforced: `NeedsDapConfirmation` / `Expired` /
`ReplayedNonce`, replay-before-expiry, persisting across re-raise). **OQGF-P-4 reporting aspect → satisfied**
(`SignatureInvalid`). **OQGF-I-13 audit-fidelity → satisfied** (nonce/expiry recorded). OQGF-P-8.2 and M-14
traced. I-5/I-6/I-9 hold. Two verdicts moved **forward** (placed → satisfied) on test proof; none moved
backward. This is a build check with real-crypto proofs, not an assertion — the workspace is green (141
passed, 0 failed) and each satisfied verdict names its test.

— End of conformance check CONF-2026-08-14-P8-REV-R2.
