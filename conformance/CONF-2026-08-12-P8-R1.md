# Conformance Check CONF-2026-08-12-P8-R1

**Phase:** 8 (`brokkr-sentinel` / HEIMDALL + EIR) — BUILD check
**Subject:** built crate `brokkr-sentinel`, under BROKKR-ARCH §6.7 / §6.8 Rev 1.11
**Date:** 12 August 2026
**Method (CLAUDE.md §5.3):** requirements enumerated **from the corpus** (AMD-002 OQGF-P-1…P-5; AMD-005
OQGF-P-8.1…P-8.7; AMD-004 OQGF-P-7; AMD-006 OQGF-P-9.4; OQGF-I-6; OQGF-M-12; OQGF-M-6 as it bears), quoted,
never from BROKKR-ARCH §14. Per §5.3 a verdict may move backward; none does (additive). Every "satisfied"
names the test or structural property that establishes it — nothing is asserted without a proof.

**What was checked, and against what (§7):** the tolerance, host-harm, screening, reconciliation, and
resolution requirements of Organ 2's heuristic layer and EIR, against the built crate
(`src/{observation,canonical,heimdall,eir}.rs`) and its 18 real-crypto tests. Finding: **0 absent**; the
Phase-boundary `partial`/`n.a.` set recorded honestly below.

---

## 1. AMD-002 — self-tolerance and host harm (OQGF-P-1 … P-5)

| Req | Text (quoted, abridged) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **P-1** | *"…SHALL continuously measure its host-harm rate, and SHALL keep that rate within a declared, documented bound. Disruption of a legitimate operation is a governance failure of equal standing to a missed threat."* | `Heimdall::host_harm` → `HostHarmReport{rate,bound,autoimmunity,storm}`; numerator = `HostHarmIncident`s, denominator = observed governed count; `bound` injected, never inferred | `test_host_harm_zero_denominator`, `test_host_harm_autoimmunity_is_sustained_not_single` | **satisfied (measurement machinery)** — caveat: rate is a **lower bound** (§13, counts only confirmed FPs); the live denominator stream is Phase 11 |
| **P-2** | *"Self-tolerance SHALL apply only to Heuristic Responses… a request to suppress a Deterministic Gate SHALL be refused, not honored silently."* | core's **sealed** `ToleranceController::grant` (NOT overridden) returns `NonSuppressibleGate` for a `Deterministic` class before `grant_heuristic` is reached | **`test_oqgf_p_2_tolerance_refuses_deterministic_target`** (the phase's central property) | **satisfied** |
| **P-3** | *"…every Heuristic Response detector SHALL be screened against the declared Self Set and SHALL NOT be deployed if its host-harm rate against that baseline exceeds the declared bound."* | `Heimdall::screen`: recompute the digest over `corpus.observations()`, **bind** it to `SelfSet.corpus_digest`, then run the detector — a **single** firing fails | `test_oqgf_p_3_single_firing_fails_screening`, `test_oqgf_p_3_substituted_corpus_is_refused` (spy never runs), `test_screening_passes_when_detector_clears` | **satisfied (machinery)** — caveat: screening is **inert until a real Self Set exists** (§13) |
| **P-4** | *"…suppressed at runtime only by a DAP issuing a Tolerance Grant… narrowly scoped… carry an expiry… PQC-signed… Expired or out-of-scope grants SHALL have no effect."* | `grant_heuristic`: verify the dual-family signature under the DAP key; refuse `Expired` (`now>expiry`) and blanket/empty scope (`OutOfScope`) | `test_oqgf_p_4_valid_grant_attaches_on_heuristic`, `_expired_grant_refused`, `_forged_grant_refused`, `_blanket_scope_refused` | **satisfied** — caveat: `ToleranceError` has no signature-failure variant, so a forged grant is refused as `OutOfScope` (fail-closed; naming imprecision flagged, §3 of the report) |
| **P-5** | *"(a) autoimmunity — a sustained rise in host-harm rate above the declared bound; (b) response storm — any single graded response whose magnitude exceeds a declared host-availability threshold."* | P-5a: `autoimmunity` = over-bound streak ≥ **DAP-injected** `sustained_threshold` (corpus gives no number). P-5b: `assess_response` → `Exceeds` past a declared radius, `Unenforceable` when undeclared (says so) | `test_host_harm_autoimmunity_is_sustained_not_single`, **`test_oqgf_p_5_storm_needs_declared_radius`** | **satisfied (machinery)** — "sustained" is a declared parameter, not a chosen threshold |

## 2. AMD-005 — resolution (OQGF-P-8.1 … P-8.7)

| Req | Text (quoted, abridged) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **P-8.1** | *"Every Escalation type SHALL declare… its Resolution Path… An Escalation with no declared Resolution Path SHALL NOT be permitted."* | `EscalationType` requires `resolution_criteria` + `baseline` (I-8, core); EIR reasons over them | `test_oqgf_p_8_6_chronic_is_flagged` (registers and reasons over a full `EscalationType`) | **satisfied** |
| **P-8.2** | *"Return to the Baseline Posture SHALL be an explicit, recorded decision… not an implicit or silent timeout."* | `resolve` acts only on a signed `ResolutionDecision`; nothing in EIR times out to baseline | `test_resolve_succeeds_with_dap_and_eligibility` | **satisfied** |
| **P-8.3** | *"…a minimum dwell time… and a hold window over which the clear condition must persist before de-escalation."* | `classify`: `elapsed ≥ dwell_min` **and** `held ≥ hold_window`, else `NotYet` | **`test_oqgf_p_8_3_hysteresis_not_satisfied`** | **satisfied** |
| **P-8.4** | *"De-escalation SHALL NOT erase the Organ 5 record… nor revert any… Refined Detector."* | EIR holds **no** incident records and **no** detectors; `resolve` flips `active=false` and erases nothing (structural); depends on neither SAGA nor KVASIR (I-5) | structural (no deletion path; no such fields) | **satisfied (structural)** |
| **P-8.5** | *"Autonomous action MAY raise posture but SHALL NOT autonomously de-escalate above Baseline… DAP confirmation. Where uncertain, it SHALL remain escalated."* | `resolve` verifies the decision's dual-family signature under the DAP key; an unverifiable decision → `NeedsDapConfirmation`; readiness additionally required | **`test_oqgf_p_8_5_cannot_lower_above_baseline_without_dap`**, `test_forged_signal_cannot_stand_down` (structural raise-only) | **satisfied** — the load-bearing asymmetry |
| **P-8.6** | *"An Escalation persisting beyond its declared maximum duration… SHALL be flagged as a Chronic Escalation… treated as a host-harm condition."* | `may_resolve` → `Chronic` past `max_duration`; `scan_chronic` returns the findings (surfaced as values; raised through the graded-response path at Phase 11) | **`test_oqgf_p_8_6_chronic_is_flagged`** | **satisfied (detection)** — the graded-response emission is Phase 11 |
| P-8.7 | proof of return (High-Assurance) | — | — | **n.a. at Enhanced** (§1.4) |

## 3. AMD-004 P-7, OQGF-I-6, OQGF-M-12, OQGF-M-6, AMD-006 P-9.4

| Req | Text (quoted, abridged) | Implementation | Proof | Verdict |
|---|---|---|---|---|
| **P-7 (raise-only, .4)** | *"An autonomous Signal MAY only raise defensive posture."* | reconciliation and chronic findings emit `Signal`s whose `PostureEffect` has **only** `Raise` — no lowering signal is representable | `test_oqgf_m_12_mismatch_deviates_and_none_does_not`, `test_forged_signal_cannot_stand_down` | **satisfied (raise-only property)** — full propagation/coupling/cascade-bound (P-7.1…7.6) is broader/Phase 11 |
| **I-6** | graded response engine (log / rate-limit / escalate) | HEIMDALL produces the **posture-raise inputs** (deviations, chronic, storm) as Signals/values | reconciliation + `scan_chronic` + `assess_response` tested | **partial** — the sentinel produces the inputs; the full graded-response wiring is Phase 11 |
| **M-12** | *"The action actually executed at each hop SHALL be reconciled against… the Root Intent."* | `reconcile`: a `Hop` whose `executed` ≠ `authorized` deviates → raise Signal; `executed: None` is not a deviation | `test_oqgf_m_12_mismatch_deviates_and_none_does_not` | **PARTIAL** — mechanism now; **live stream Phase 11** (Rev 1.11: nothing executes until the executor) |
| **M-6 (reconciliation factor)** | vendor trust score's fifth factor = statistical reconciliation pass rate | — | — | **NOT advanced** — Rev 1.11 corrected its timing to **Phase 11** (a statistic over reconciliation outcomes needs executed actions); not claimed here |
| **P-9.4** | *"…a Risk-Acceptance Entry is not a Tolerance Grant, no decision is expressible as both…"* | tolerance grants now exist as a distinct type (`ToleranceGrant`, HEIMDALL) vs risk acceptances (`RiskAcceptance`, SAGA) — unrelated types, no conversion, no shared path | structural (two distinct committed types); grant path tested | **structurally demonstrable now** (was "PARTIAL until tolerance grants exist" at Phase 7); the periodic-review/inventory operational demonstration spans both crates + orchestrator |

## 4. Invariants

| Inv | Verdict |
|---|---|
| **I-5** | Holds — `cargo tree -e no-dev` = `{brokkr-core, brokkr-crypto}`; HEIMDALL is fed observations, reaches nothing. |
| **I-6 (structural)** | Holds — no model call, no network, no FFI; `#![forbid(unsafe_code)]`. |
| **I-9 (sentinel aspect)** | Holds — `DetectionVerdict` has no action-permitting variant (a detector fires, never authorizes); emitted Signals are raise-only. |

## 5. Result

**0 absent.** satisfied (with named caveats): P-1, P-2, P-3, P-4, P-5, P-8.1…P-8.6, P-7 (raise-only), and
I-5/I-6/I-9. **PARTIAL: M-12** (mechanism now, live stream Phase 11), **I-6** (inputs produced; wiring
Phase 11). **NOT advanced: M-6** reconciliation factor (Rev 1.11 → Phase 11). **n.a.: P-8.7** (High-Assurance).
P-9.4 distinctness is now structurally demonstrable. No verdict moved backward. This is a build check with
real-crypto proofs, not an assertion.

— End of conformance check CONF-2026-08-12-P8-R1.
