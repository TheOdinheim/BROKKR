# Amendment-Gap Index

Amendment-gap reports filed under CLAUDE.md Section 4. When the framework does not
cover a situation, the build STOPS, the gap is reported to the DAP, a recommendation
is stated, and work waits. Each gap is a dated file; this index is appended to,
never rewritten.

| Date | ID | Phase | Summary | Recommendation | Status | Link |
|---|---|---|---|---|---|---|
| 2026-07-14 | GAP-2026-07-14-001 | 0.5 | Eight OQGF-1.0 Organ requirements (G-7, I-1, I-2, I-5, M-5, M-6, R-6, A.6.1) neither specified nor declared n.a. in the architecture | DAP to declare n.a., add a hook, or defer — placed as an architecture revision | ~~OPEN — build stopped~~ → **DISPOSED (Rev 1.2, `99b6c62`)** — G-7 via the Mosca calculation (§6.11); I-1/I-2/I-5/M-5 via **BIFRÖST** + I-11/I-12 (the finding underneath was that MÍMIR bypassed HÚÐ); M-6 via a signed quarterly gate-blocking trust score (the fold-into-R-2 recommendation **declined**); A.6.1 **split** — plan organizational, four triggers architectural (§11); R-6 PARTIAL, referred upward. **Status recorded 2026-09-07, 55 days late** — the row read "build stopped" while every subsequent phase was built and approved. **One disposition did not hold:** R-6's closure rested on an interim-posture sentence ("HSM-backed keys with dual-control issuance, custody model declared in the CBOM") that GAP-2026-09-06-001 established was **never true**; struck at Rev 1.20 (`3f9805d`), and **R-6.2 is now ABSENT**. The original finding was correct; its closure was not | [GAP-2026-07-14-001.md](GAP-2026-07-14-001.md) |
| 2026-07-15 | GAP-2026-07-15-001 | 1 | brokkr-core missing type-level obligations from AMD-008/009: Risk Register surface (RiskRegister/RiskEntry/Disposition/TreatmentPlan + RiskAcceptance into core) and personal-data classification dimension | DAP to approve a Phase 1 revision (R3) scoped to those types; settle RiskAcceptance placement and the personal-data dimension surface | CLOSED — types written (risk.rs, personal_data.rs); RiskAcceptance created in core; builds --locked, tests pass | [GAP-2026-07-15-001.md](GAP-2026-07-15-001.md) |
| 2026-07-16 | GAP-2026-07-16-001 | 2 | Phase-2 symbol list wrong: `wc_MlDsaKey_Sign`/`_Verify` not exported (header-only legacy); bound header-directed `wc_MlDsaKey_SignCtx`/`_VerifyCtx` (ctx=NULL). Also: OQGF-R-4 entropy is single-DRBG (partial) | Record only — binding is correct & tested. R-4 dual-source/SP-800-90B is a deployment hardening item for DAP disposition | RESOLVED-WITH-DEVIATION | [GAP-2026-07-16-001.md](GAP-2026-07-16-001.md) |
| 2026-07-24 | GAP-2026-07-24-001 | 4 | M-11/M-10 action-semantics absent from committed types: `Action{tool,detail}` has no capability; `ToolId`≠`Capability` (no committed map); `Invariant(String)` has no `(Action,Invariant)→bool`. SINDRI cannot build steps 3 (OutOfScope) / 4 (InvariantViolated) without inventing REGIN's vocabulary | DAP to place an action→capability binding (field on `Action` or a REGIN-owned resolver seam) + an invariant-evaluator seam; OR declare Phase-4 SINDRI scope = Signals 1–2 only. Additive options preferred; the scope-reduction option unblocks the builder (labeled) | DISPOSED (Rev 1.3, 612f4b5) — scope option selected; conjuncts 3–4 deferred to a Phase-11 deadline; RISK-2026-0004 | [GAP-2026-07-24-001.md](GAP-2026-07-24-001.md) |
| 2026-07-24 | GAP-2026-07-24-002 | 4 | Signal-1 intrinsic attestation-signature has no committed canonical signed-content; only `Attestation` encoding (`canonical::write_attestation`) is private and self-including | Define Signal 1 as resolve-to-declared-root + bind to chain's verified current hop (drops the redundant intrinsic check — unblocks, labeled); OR place a committed attestation signed-content encoding in core | DISPOSED (Rev 1.3, 612f4b5) — preferred construction adopted; intrinsic check dropped, M-1 PARTIAL; RISK-2026-0005 | [GAP-2026-07-24-002.md](GAP-2026-07-24-002.md) |
| 2026-07-27 | GAP-2026-07-27-001 | 5 | §6.2 promotion-gate predicate 5 references "the intent vocabulary" of recognized capabilities, uncommitted: `Capability` is an open `String`, no register enumerates valid capabilities, and the tool-union reading is vacuous (a can-never-fire predicate §6.2's own predicate 6 condemns). Predicates 1–4, 6 are determinable; predicate 5 is the sole blocker | DAP to commit a capability vocabulary (field on `PolicyRegister` or a 7th register, referenced by predicates 5 AND 6 — additive, preferred); OR redefine predicate 5 against a committed set; OR declare it structural like predicate 1 (unblocks, labeled). Placed as an ARCH revision | DISPOSED (Rev 1.5, 0b7d7f4) — additive option selected: `PolicyRegister::capabilities` committed; predicates 5 & 6 check against it; labeled unblock option NOT chosen; predicate 6 narrowed | [GAP-2026-07-27-001.md](GAP-2026-07-27-001.md) |
| 2026-07-30 | GAP-2026-07-30-001 | 6 | Barrier risk acceptance (AMD-006/OQGF-P-9) not buildable: `BarrierFinding {classification, reason}` has no identity and nothing links it to a `RiskAcceptance` (`finding: RiskAcceptanceId`) — matching needs an invented scheme; and `DeterministicGateId {Genome, Mhc}` has no barrier variant, so an acceptance can't name the OQGF-I-10 gate. Tasks 1–5,7 buildable; Task 6 the sole blocker | DAP to give `BarrierFinding` a committed identity + add `DeterministicGateId::Barrier` (additive, preferred); OR a committed match-key + resolver; OR defer the barrier `AcceptedRisk` path (unblocks, labeled). Placed as a core revision | DISPOSED (Rev 1.8, e4d38a6) — additive option selected: `BarrierFinding` gained `datum`+`condition`+`finding_id()`, `FindingId` placed, `RiskAcceptance::finding` retyped, `DeterministicGateId::Barrier` added; labeled unblock option NOT chosen; §13 notes promotion gate has same defect (scheduled) | [GAP-2026-07-30-001.md](GAP-2026-07-30-001.md) |
| 2026-08-06 | GAP-2026-08-06-001 | 7 | §6.9 defines the chain-linkage digest as covering "full canonical bytes **including signatures**" while requiring re-signing (which **appends** a `GenerationSignature`) to leave "the chain through it unchanged" — mutually exclusive: appending a signature changes the full bytes the next record's `prev` digests, breaking the link §6.9 says re-signing preserves. Resolution B (linkage over signed content) contradicts §6.9's text; Resolution C (freeze at original signature) invents an unstated convention. Tasks 2,5,6,7,8 buildable; Tasks 1(linkage),3,4 (the spine) blocked | DAP to revise §6.9 to define the linkage digest precisely: (preferred) over SIGNED CONTENT only, signatures + timestamp verified independently; OR freeze at seal-time signatures stated explicitly; OR move signatures to a side-log. All preserve tamper-evidence + re-signing; **all unblock me (labeled — the unblock is intrinsic to resolving the contradiction)**. Placed as an ARCH revision | DISPOSED (Rev 1.10, f76aae7) — preferred option selected: `prev` digests SIGNED CONTENT only; the "three attestations" model; §13 names the strip-an-attestation residual. Phase 7 built under it | [GAP-2026-08-06-001.md](GAP-2026-08-06-001.md) |
| 2026-08-12 | GAP-2026-08-12-001 | 8 (rev) | `ResolveError` has no variant for an **expired** or **replayed** resolution decision, but §6.8 (Rev 1.12) requires EIR to *refuse* both; `resolve` returns `Result<_, ResolveError>` so a refusal must be one of `{CriteriaNotMet, HysteresisNotSatisfied, NeedsDapConfirmation}` — none fits, and reusing one (esp. `NeedsDapConfirmation` for a replay that WAS DAP-signed) is the exact imprecision Rev 1.12 fixed for `ToleranceError`. Tasks 1/3/4 buildable; Task 2 (freshness enforcement) blocked; the fix is a brokkr-core change, out of scope | DAP to add two `ResolveError` variants in a brokkr-core revision — `Expired` + `ReplayedNonce` (additive, preferred; two not one, to keep attack/latency distinct); OR one combined `Stale` (weaker). **Unblocks me — labeled; intrinsic (refusal needs a fitting variant).** Placed as a core revision | DISPOSED (Rev 1.13, bb2997b) — additive option selected: `ResolveError` gains `Expired` + `ReplayedNonce` (two, not one); labeled-intrinsic-unblock confirmed; builder's refusal to ship green-but-unenforced vindicated. Core variants placed (REV-R8) | [GAP-2026-08-12-001.md](GAP-2026-08-12-001.md) |
| 2026-08-18 | GAP-2026-08-18-001 | 8.5 (rebuild) | A THIRD held clock (I-13): brokkr-sentinel EirState.now (eir.rs:54) + HeimdallState.now (heimdall.rs:42) are construction state; EIR checks resolution-decision expiry (eir.rs:242, P-8.5) and HEIMDALL tolerance-grant expiry (heimdall.rs:268, P-4) against them. Predates I-13 (sentinel is Phase 8; I-13 is Rev 1.15). Found by RISK-2026-0006's workspace closure grep during the bifrost rebuild; out of scope | DAP to place a brokkr-sentinel I-13 revision (thread `now` as a call parameter through resolve/may_resolve/scan_chronic + grant-application; remove held fields; add two-verdict tests) and expand RISK-2026-0006's scope workspace-wide. Unblocks the builder: NO (more work, correct not convenient) | OPEN — reported; blocks RISK-2026-0006 closure, not the bifrost deliverable | [GAP-2026-08-18-001.md](GAP-2026-08-18-001.md) |
| 2026-08-18 | GAP-2026-08-18-002 | 8 (rev) | brokkr-sentinel I-13 fix BLOCKED: EIR (`resolve`/`may_resolve`/`scan_chronic`) + HEIMDALL (`grant_heuristic`) hold clocks, but all four are **core trait methods** (`ResolutionEngine`, `ToleranceController`) that don't take `now`. Adding `now` per call changes brokkr-core traits — out of scope (E0050 compiler-confirmed). No in-scope partial fix (every held-clock read is a trait method) | DAP to place a brokkr-core trait rev adding `now` to `ResolutionEngine::{may_resolve,resolve,scan_chronic}` + `ToleranceController::grant_heuristic` (the `7d47d2a` pattern), then re-issue the sentinel revision. Unblocks builder: NO (correct, not convenient) | OPEN — build stopped; sentinel unchanged (experiment reverted) | [GAP-2026-08-18-002.md](GAP-2026-08-18-002.md) |
| 2026-08-19 | GAP-2026-08-19-001 | 9 | `AdaptError` has no **corpus-validity** variant, but §6.12 Gate 2 (`select`) SHALL refuse a **substituted corpus** (digest ≠ declared record) before measuring — a required poisoning defense with a required test — and `select` returns `Result<_, AdaptError>` so the refusal must be one of `{UnconfirmedSeed, Overfit, CoverageRegression, FailsTolerance, NeedsApproval}`. Reusing `Overfit`/`CoverageRegression` is a **positive false claim about the candidate** (a good candidate refused for a poisoned corpus, libeled as overfit) — the exact imprecision Rev 1.12 (`ToleranceError::SignatureInvalid`) and Rev 1.13 (`ResolveError::{Expired,ReplayedNonce}`) fixed. `activate`'s stale-corpus check shares the gap. Tasks 1,2,4,6 buildable; screen-staleness→`FailsTolerance` and forged-provenance→`NeedsApproval` honest (not gapped); the corpus-validity refusal is the sole blocker. Fix is a brokkr-core change, out of scope | DAP to add corpus-validity variant(s) to `AdaptError` in a core revision — `SubstitutedCorpus` (digest mismatch, `select`) + `StaleCorpus` (version mismatch, `activate`) (two, not one, to keep tampering distinct from drift; additive, preferred); OR a single `CorpusInvalid` (weaker); OR clarify corpus validity is a crate-local error (option D). **Unblocks me — labeled; intrinsic (a refusal needs a fitting variant).** Placed as a core revision or ARCH clarification | DISPOSED (Rev R12, PHASE-1-REV-2026-08-19-R12) — option A selected: `AdaptError` gains `SubstitutedCorpus` (tampering) + `StaleCorpus` (drift), two not one; containment stays `Overfit` (the claim is true); labeled-intrinsic-unblock confirmed; same principle applied a 4th time (SignatureInvalid, Expired/ReplayedNonce, now these two) | [GAP-2026-08-19-001.md](GAP-2026-08-19-001.md) |
| 2026-09-06 | GAP-2026-09-06-001 | AMD-018 corpus recheck (post-11) | **OQGF-R-6.2 (Enhanced key custody) is ABSENT** — all three elements checked individually and none satisfied: **no hardware boundary** (zero HSM/PKCS#11/`cryptoki` in the workspace; keys generated in software by `DualKeyPair::generate` at `brokkr-cli/src/main.rs:335-338`, SAGA signer held as `Mutex<DualKeyPair>` in process memory, `brokkr-audit/src/saga.rs:145`), **no dual control** (a single operator generates every long-lived key; AMD-018 §A.4.5's "confirm single-operator issuance is refused" would succeed), **no CBOM declaration** (`Cbom`, `brokkr-core/src/genome.rs:79-87`, has no custody field; Rev 1.2-1.19 §1.4 cited §6.11 and §6.11 cited the CBOM — circular). `Partial` unavailable: no element satisfied. **R-6.1 (Baseline) also unmet** — it permits software keys only if the CBOM declares them as such; BROKKR meets **no tier of R-6**. Second finding: the architecture asserted "HSM-backed with dual-control issuance, custody model declared in the CBOM" from **Rev 1.2 through Rev 1.19** — an unevidenced security-posture claim in the governing document, surviving 18 revisions and a 16-phase red-team campaign; struck at Rev 1.20 (`3f9805d`). It survived because the tier ambiguity ("PARTIAL, referred upward") read as rigor and closed the question — a named gap concealing an unnamed one. Consequence: SECURITY-REVIEW-2026-08-30-R1 finding **M-3's stated compensating control ("an HSM with dual-control issuance") does not exist**. Reopens part of GAP-2026-07-14-001, whose R-6 closure rested on the false claim | (A, preferred) accept the ABSENT verdict as placed in Rev 1.20 and treat closure as deployment work with a `brokkr-crypto` seam; (B, additive, NOT a substitute) DAP to place a typed CBOM custody field as a 7th OQGF-G-4 promotion-gate predicate — **would not move R-6.2 off ABSENT**; (C) record a Risk Register entry (OQGF-P-10), suggested RISK-2026-0008, disposition not proposed; (D) re-examine M-3's acceptance. **Unblocks the builder: NO — not blocked, no phase in flight, and (B) creates work. Coincidence labeled per §4: the "two of three elements are not code" framing is correct AND assigns the builder least work.** `satisfied` was available on paper by trusting the architecture's own false sentence — the circular audit §5.3 names — and was refused | OPEN — Enhanced conformance claim, production deployment, and High-Assurance readiness blocked; no in-flight phase to stop | [GAP-2026-09-06-001.md](GAP-2026-09-06-001.md) |
| 2026-09-07 | GAP-2026-09-07-001 | Rev 1.21 KeyCustody impl (Commits 1+2) | **Two Rev 1.21 design defects, found at Step 0 before any code.** **Blocker 1 (fatal to Commit 2):** predicate 7 is placed as a tier-consistency check against **`genome.tier`**, which does not exist — `Genome` (`genome.rs:270-281`), `Cbom`, and `PolicyRegister` carry no tier, and `promote(genome, dap_key, now)` (`gate.rs:30`) takes none. `ConformanceTier` **exists** (`capability.rs:26`) but only on AMD-011's `CapabilityEnvelope` in `brokkr-cli`; reaching it inverts I-5. **Blocker 2:** `ExtractionProtection::EncryptedAtRest { kek: KeyRef }` — `KeyRef` is not committed anywhere in `brokkr-core` (0 hits). Rest of Commit 1 checks out (`Digest`/`Timestamp`/`DualSignature` exist; one `Cbom` site at `promotion.rs:138`). **ROOT CAUSE — existence is not reachability:** Rev 1.21's verification note said *"ConformanceTier ... already exists ... so predicate 7 needs no new tier vocabulary"* — true, and not the check that mattered; the type's definition was verified, the evaluator's path to an instance was not. **Fourth instance of the pattern Rev 1.21's own change log enumerates** (Rev 1.4 predicate 5 / Rev 1.6 egress / Rev 1.7 finding-identity), committed in the document that listed the other three, by the party that listed them — naming the pattern did not prevent repeating it | **DAP-directed at filing.** **B1 → Option A:** `Genome` gains `tier: ConformanceTier` inside the signed content (tamper-evident; genome-level fact). Refused: **(C)** caller-supplied tier parameter — the party being checked could pass `Baseline`, a check whose threshold its subject chooses is not a check; **(D)** `CapabilityEnvelope.governing_tier` — inverts I-5; **(B)** tier on `Cbom` — system property in a crypto-inventory type. **B2:** `KeyRef` NOT to be defined; `ExtractionProtection` simplified so no variant needs an uncommitted type — the KEK ref changes no tier verdict (encrypted-at-rest and plain software custody both fail R-6.2, both satisfy R-6.1's declaration), so it does not earn a type. Builder also proposes (not adopts) a Step-0 reachability rule — **labeled §4: costs the builder work on every future placement, proposed against own convenience** | OPEN — awaiting **Architecture Rev 1.22** (both dispositions) before implementation resumes; no code written, nothing committed. R-6.2 unchanged: **ABSENT** | [GAP-2026-09-07-001.md](GAP-2026-09-07-001.md) |
| 2026-09-08 | GAP-2026-09-08-001 | Rev 1.24 A-3 client impl (Commits 1+2) | **Rev 1.24 §6.9 places rules against token fields the wolfSSL PKCS#7 API does not expose.** **wolfSSL implements CMS, not RFC 3161.** `wc_PKCS7_VerifySignedData` verifies the CMS signature and returns a code — that half works, no struct read. But RFC 3161's `TSTInfo`, carrying **`messageImprint`** and **`genTime`**, is the CMS eContent, reachable only via `pkcs7->content` (`pkcs7.h:247`). **All 54 exported `wc_PKCS7_*` symbols enumerated**: six getters (attribute value, enveloped KARI RID, no-certs, pad size, signer SID, stream mode) and **no accessor for `content`/`contentSz`/`publicKeyOID`** (`wc_PKCS7_SetContentType` is a setter for the type OID). Consequence: **`TimestampError::ImprintMismatch` — which §6.9 names "the one variant here that is an attack signature" — cannot be implemented**, nor can `gen_time`; `algorithm` has only a certificate-derived **inference** (`wc_ParseCert`, exported), not an observation of `SignerInfo`. Prerequisite genuinely met (PKCS7 54 symbols; PQC intact — MlDsaKey 35 / SlhDsaKey 31 / MlKemKey 17). **ROOT CAUSE — existence checked, reachability not:** Rev 1.24 verified wolfCrypt *has* CMS (source, `--enable-pkcs7`, zero symbols pre-rebuild) — all true, and not the check that mattered. **§5.4 exactly, one level down:** Rev 1.21 placed a predicate against a *tier* the gate could not reach; Rev 1.24 against a *field* the API does not expose. **FIFTH instance, and the first committed AFTER §5.4 was placed to prevent it** (Rev 1.4 / 1.6 / 1.7 / 1.21 precede it). Rev 1.24 **carries a §5.4 table** that covers the rules reading BROKKR-held values and omits those reading token-held ones — the check was applied where the answer was known and skipped where it was not. A rule satisfiable by tabulating the easy rows can be passed without being applied | **DAP-directed at filing: the C shim.** A small C translation unit exposing `content`/`contentSz`/`publicKeyOID`, **compiled with the library's own flags** so it sees the same struct layout wolfSSL does — the `#ifdef` fragility is eliminated, not reduced, because no second party forms an independent opinion of the layout. Refused: **offset-reading `wc_PKCS7` from Rust** (ffi.rs never reads C struct fields; `streamOutCb`/`streamCtx` are `#ifdef`-guarded *before* the needed fields, so a silently wrong offset yields a false `messageImprint` comparison in a signed record); **hand-walking the token DER** (parses unauthenticated bytes — §6.9's own "class of code where CVEs live"; `GetASNTag`/`GetLength` cannot help, since obtaining the position is the unauthenticated walk); **reducing the token record** (guts the attack case — **and is the route that unblocks the builder fastest, labelled per §4, and was not recommended**); **a Rust DER/CMS crate** (§6 supply-chain decision putting a parser outside our control into the audit path — hand-walking with a nicer wrapper). Nothing written, not even the buildable majority: a client that verifies signatures but cannot detect substitution would store a token attesting *other bytes* as a valid attestation — worse than the current `Unavailable` | OPEN — awaiting **Architecture Rev 1.25** (the shim) before implementation resumes; no code written, nothing committed. **A-3 unchanged: PARTIAL**, and will stay PARTIAL after the client ships on the **PQC clause** — no public RFC 3161 authority signs post-quantum | [GAP-2026-09-08-001.md](GAP-2026-09-08-001.md) |
| 2026-09-10 | GAP-2026-09-10-001 | Rev 1.29 P-11.2 impl (Commits 1+2) | **Rev 1.29 §6.5 asserts the `PersonalDataTag` change does not touch the audit chain; it does.** The paragraph: *"a BCR is not an `AuditRecord`, `record_signed_content` does not encode it, and no `prev` digest covers it."* First clause true, other two false. `PersonalDataTag` reaches the chain-linkage digest by **two** paths in `brokkr-audit/src/canonical.rs`: `record_signed_content:699` → `write_event:706` → `AuditEvent::BarrierCrossing:636` → `write_flow:370` → `write_personal:382/394` → `write_personal_tag:270`; and `RecordedInput::Shredded:512` → `write_personal_tag:519`. **`brokkr-audit` keeps its own copy of the encoder**, distinct from `brokkr-barrier/src/canonical.rs:90` — Rev 1.29 named the barrier's copy and did not look for a second. Adding `fields` therefore moves `record_signed_content` for any record carrying a `BarrierCrossing` or a `Shredded` input — the Rev 1.10 linkage basis. **The sentence answers a real question accurately and the wrong one**: *is a BCR an AuditRecord?* (no) rather than *is a BCR inside one?* (yes — **OQGF-I-13 requires it**, and `BarrierCrossing` carries a whole `BoundaryFlow`). **NOT a §5.4 failure** — every value is reachable and the design builds; it is the **§10 SLH-DSA class**: true-sounding, unverified, becomes furniture. **No existing rule would have caught it** (§5.4 governs a rule's inputs, not a claimed consequence; §5.3 governs verdicts; §7 binds the builder's summaries, not a placed revision) — **stated rather than filled with a proposed rule**. Second, smaller omission recorded: `BarrierFinding` needs a `classification` and `BoundaryFlow::Ingress` has none — resolves via `b.classification`, since conditions 10/11 are reached only once provenance is established; belongs in the §5.4 table. Blast radius measured: **no persisted chain** (`Saga.records: Vec<AuditRecord>`, in memory), fixtures at `audit.rs:79/:142` build in-process and move with the encoding | **DAP-directed at filing: option 1 — accept the encoding change.** Cost restated: moves `record_signed_content` for `BarrierCrossing`/`Shredded` records; zero now (in-memory SAGA), not zero once SAGA persists; the false paragraph goes. **Option 2 not available** — every candidate site is on the crossing and the crossing is what OQGF-I-13 records. **The property Rev 1.29 wanted is delivered more strongly than claimed**: the declaration is covered by the BCR signature **and** the audit chain — two attestations, not one; the revision undersold its mechanism while misdescribing its cost. **Unblocks the builder: NO — labeled §4.** It accepts a broader cost than the placement claimed; the cheaper route was to trust the reassuring sentence, implement, and note nothing, since nothing visibly breaks. That route was available and was not taken | DISPOSED IN DIRECTION at filing — implementation proceeds under the corrected cost; **Rev 1.30 carries the §6.5 text correction** (the builder does not edit the architecture, §0/§1); heuristic backstop stays future work; **P-11.2 verdict does not move** | [GAP-2026-09-10-001.md](GAP-2026-09-10-001.md) |
