# Phase 1 Revision Report R2 — brokkr-core capability vocabulary (Rev 1.5)

**Report ID:** PHASE-1-REV-2026-07-27-R2
**Phase:** 1 revision (`brokkr-core` — the capability vocabulary)
**Date:** 27 July 2026
**Builder:** Claude Code. **For ratification by:** Jeremy Rose, DAP.
**Status:** Complete; **one field**; builds `--locked`; all tests pass; downstream unchanged; not committed (awaiting DAP review).
**Places:** the Architecture Rev 1.5 capability vocabulary (commit `0b7d7f4`, §6.2). **Disposes GAP-2026-07-27-001.** Prerequisite for Phase 5.

---

## 1. What this revision does

Rev 1.5 places the closed capability vocabulary that Phase 5's promotion-gate predicate 5 assumed
and Rev 1.4 did not commit — the gap I filed at the Phase 5 surface check. It is exactly **one
field** plus its doc comment. Scope-locked to `brokkr-core`.

```rust
// PolicyRegister, first field (Rev 1.5)
pub capabilities: Vec<Capability>,
```

## 2. Pre-writing grounding (required) — §6.2 and committed types agree

| | Committed `PolicyRegister` (before) | §6.2 (Rev 1.5) | Agree? |
|---|---|---|---|
| fields | `invariants, disallowed, signature` (3) | + `capabilities: Vec<Capability>` as the **first** field (4) | Yes |
| constructor | none (plain struct, public fields) | — | Yes — no `new` to update |
| `Capability` import | already present (`genome.rs:17`, used by `ToolEntry`/`InvariantEntry`) | — | Yes — no import change |
| `Genome::new` | 10 args, takes a `PolicyRegister` value | unaffected | Yes — arity unchanged |

No disagreement, so no STOP. The field was simply not yet in the committed struct; this revision
adds it in the §6.2 position (first).

## 3. Verification

- `cargo build --locked -p brokkr-core`: clean. `cargo test --locked -p brokkr-core`: 12
  negative_invariants + 6 risk_shape + **12 doctests** pass. Detail in
  `tests/records/FUNC-2026-07-27-R2.md`.
- **Doctest count unchanged, 12 → 12** (measured by stashing and comparing) — expected for a struct
  field. The three genome `compile_fail,E0308` doctests still pass; `Genome::new`'s arity is
  unchanged at 10, so the E0308 they prove is unaffected. Verified by running, not assumed.
- `cargo test --locked --workspace`: all crates green; **downstream (crypto/intent/gate) unchanged
  and passing**. Grep confirms **no `PolicyRegister` literal exists anywhere** (Phase 5 unbuilt), so
  no downstream break was possible — and none occurred.
- Clippy `--all-targets` (deny-list): 0. `cargo fmt`: clean. **No external dependency; no import
  added** (`Capability` was already imported).

## 4. Structural invariants

- **I-5 held.** `Capability` is a same-crate type (`crate::intent`), already imported; no dependency
  added; `[dependencies]` remains empty.
- **I-10 held.** `capabilities` is a required `Vec<Capability>`, not an `Option`; a `PolicyRegister`
  without a vocabulary is unrepresentable.

## 5. Conformance and risk

- `conformance/CONF-2026-07-27-P1-REV-R2.md`: **G-8** satisfied at the shape level (the vocabulary is
  part of the signed policy register); **M-13** binding-support placed but the CHECK explicitly not
  closed (predicate 5 is Phase 5, SINDRI conjunct 3 is later). I-5/I-10 held. **0 absent.**
- **No risk-register change.** RISK-2026-0004's treatment is the **SINDRI enforcement** of conjuncts
  3/4, which this field does not perform — placing the vocabulary makes predicate 5 *buildable* but
  enforces nothing. The risk stays exactly as placed (Open, types placed, enforcement pending). Per
  the instruction, I say so rather than edit the register.

## 6. Disposition of GAP-2026-07-27-001

Appended a dated DISPOSITION section to `gaps/GAP-2026-07-27-001.md` (original body preserved, Status
line struck per §8) recording: the DAP selected the **additive** option (commit a capability
vocabulary); predicate 5 now checks tools against `policy.capabilities` and predicate 6 checks
invariants against it; **the builder's labeled self-interest disclosure was correct and the labeled
option (declare predicate 5 structural) was NOT selected** — the DAP chose the harder, additive
path; and predicate 6's failure condition was **narrowed** (forbidding a *recognized* capability no
tool currently declares is now permitted as forward-looking policy), the one non-additive change,
recorded in Rev 1.5's change log. `gaps/INDEX.md` updated OPEN → DISPOSED.

## 7. Auto-memory

No auto-memory change warranted. The design fact here (the vocabulary and predicate-6 narrowing) is
governance content living in the architecture, this report, and the disposition — not an
environment/tree fact for a self-note (§11).

## 8. Scope confirmation

Only `brokkr-core/src/genome.rs` touched (one field + doc). `brokkr-crypto`, `brokkr-intent`,
`brokkr-gate`, `governance/`, and `docs/BROKKR-ARCH-2026-001.md` are **untouched**. No `brokkr-genome`
crate created. Phase 5 not started. Not committed.

— End of Phase 1 revision report PHASE-1-REV-2026-07-27-R2.
